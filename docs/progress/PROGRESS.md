# 작업 진행 기록

> **이 파일은 자동 생성된다. 직접 수정하지 않는다.**
>
> ```bash
> npm run progress
> ```
>
> Git 커밋 이력을 읽어 날짜·담당자별로 정리한다. 재생성 방식이라 충돌이 발생하지 않는다.
> 커밋 메시지에 담기 어려운 맥락은 `docs/progress/sessions/<본인>.md`에 남긴다.

<!-- 아래는 tools/progress.mjs 가 생성한다 -->

_아직 생성되지 않았다._
