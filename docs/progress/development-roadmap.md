# 밭두렁난투 1차 빌드 개발 계획 (8/1 ~ 8/5)

작성 기준일: 2026-08-01
대상: 최수정(메인 개발), 김민주(보조 개발)
기획·아트: 전성민

이 문서는 **개발 실행 계획**이다. 시스템 규칙은 `AGENTS.md`와 결정 로그가 단일 원본이며
여기서 규칙을 새로 만들지 않는다. 규칙에 답이 없으면 `AGENTS.md` 5절로 전환한다.

---

## 0. 마감 역산

| 날짜 | 상태 |
|---|---|
| 8/1 (토) 밤 | 기획: UI/UX·CSV 확정 착수 / 개발: 기획과 무관한 코어 착수 (3절) |
| 8/2 (일) ~ 8/5 (수) | 본격 개발. **8/5 밤 = 1차 빌드(타이틀→엔딩 완주 가능) → `main` 병합** |
| 8/6 (목) ~ 8/7 (금) | 버그·밸런스·아트 적용·제출 문서 |
| 8/8 (토) | **시연 영상 촬영 가능 상태** |
| 8/10 (월) | 제출 |

8/5까지 기능을 전부 넣는 것이 목표다. 다만 두 사람의 가용 시간 합이 약 45시간이라
전 기능 완성이 빠듯한 것은 사실이므로, 범위를 미리 자르는 대신 **우선순위 티어(6절)와
컷 트리거**를 정해 두고 늦어질 때 무엇을 먼저 포기할지 미리 합의해 둔다.
당일에 판단하면 통합이 덜 된 채로 8/5을 넘긴다.

---


## 1. 작업을 나누는 기준

코드는 대부분 Claude Code가 쓴다. 그래서 분담 축은 **난이도가 아니라 "틀렸을 때 어떻게 알아채는가"** 다.

| | 최수정 | 김민주 |
|---|---|---|
| 맡는 성격 | 화면으로 확인이 안 되는 것 | 실행하면 눈으로 바로 보이는 것 |
| 구체적으로 | 씬 상태 머신, 자원·관계·공포도 상태, 원자적 처리, 대화·전투 판정 규칙, 데이터 계약, LLM 연동, 통합, 배포 | HUD·상점·제작·대화 화면, 기획자 UI 사진 대조 조정, 데이터 바인딩 확인, 플레이 테스트 |
| 사람이 해야 하는 일 | **AI가 쓴 코드를 읽어야 한다.** 하드코딩·임시 기본값·부분 반영은 실행해도 안 보이고 나중에 밸런스 붕괴로만 드러난다 | **실행해서 봐야 한다.** 결과가 화면에 드러나므로 AI 산출물을 병렬로 돌려도 안전하다 |

최수정 영역에서 특히 사람이 눈으로 확인해야 하는 것 (AI가 자주 어기는 지점):

- 작물·가격·확률·시간·좌표·보상 수치가 코드에 박혔는가 → 전부 런타임 JSON에서 읽어야 한다 (`DEC-PIPELINE-016`)
- 데이터가 없을 때 `?? 기본값`으로 넘어가는가 → 검증 오류로 보고해야 한다
- 거래·제작·자원 협상·보상 지급이 중간에 실패하면 절반만 반영되는가 → 원자적이어야 한다
  (`DEC-RESOURCE-011`, `DEC-CRAFT-004`, `DEC-RESIDENT-033`, `DEC-RESIDENT-042`)
- 엔딩·일지 프롬프트 문장이 코드에 복사됐는가 → 프롬프트 원본 파일 한 곳에서 읽어야 한다 (`DEC-PIPELINE-012`, `DEC-JOURNAL-004`)

---

## 2. 화면 구성 방식 (기획자 UI 사진을 반영하는 방법)

`index.html`이 이미 두 영역으로 나뉘어 있고, 이 구조를 그대로 쓴다.

```
#game  → 필드. Canvas 2D 한 장. 플레이어·작물·야생동물·주민·투사체 (src/render/)
#ui    → 오버레이. DOM. HUD·정비 허브·대화·결과·엔딩         (src/ui/)
```

`DEC-UI-014` 확정본이 이 구조와 그대로 맞는다. **정비는 별도 화면 전환이 아니라
셔터가 필드를 덮고 그 위에 정비 허브 UI가 올라오는 오버레이**이고, 전투 전 대화와 투항 대화도
필드(습격 모드) 위 오버레이다. 즉 이 둘이 열릴 때 **캔버스는 계속 살아 있고 시뮬레이션만 멈춘다**(9-2절).
독립 화면으로 전환되는 것은 타이틀·이름 입력·튜토리얼·일차 시작·조우 결과·밤 결과·런 실패·엔딩뿐이다.

런 실패와 엔딩은 **공유 템플릿으로 묶지 않고 각각 따로 만든다** (`DEC-UI-014`).

필드는 실시간 액션이라 캔버스가 맞고, UI는 DOM이어야 사진을 보고 위치를 빨리 옮길 수 있다.
**기획자 사진대로 조정하려면 다음 두 가지가 필요하다.**

1. **기준 해상도를 오늘 정한다.** (`DEC-UI-025` 보류 항목)
   예: 1280×720 고정 후 창 크기에 맞춰 비율 유지 스케일.
   기준이 없으면 사진의 "여기쯤"을 옮길 좌표계가 없다.
   **기획팀이 UI/UX와 아트 규격을 정해 전달할 예정**이므로 오늘 확정을 기다리지 않고 진행한다.
   월드 단위 → 픽셀 배율도 아트 규격과 함께 온다. 그때까지는 임시 배율 상수 하나로 그리되
   **`src/render/camera.ts` 한 곳에만 둔다.** 규격이 오면 그 값만 바꾸면 된다.
   (화면 배율은 표현이지 게임 데이터가 아니므로 CSV로 뺄 필요는 없다.)
2. **UI 수치를 한 파일에 모은다.** `src/ui/layout.css`의 CSS 커스텀 프로퍼티로
   패널 위치·크기·여백·색을 선언하고, 각 UI 파일은 변수만 참조한다.
   사진을 받으면 **이 파일의 값만 고친다.** TS 코드를 건드리지 않으므로 김민주 담당 영역 안에서 끝나고
   최수정 작업과 충돌하지 않는다.

보조로, 개발 빌드에서 UI 요소 경계선과 좌표를 겹쳐 보여주는 토글을 `src/ui/dev/`에 둔다.
사진과 대조할 때 픽셀 단위로 맞추기 쉽다. (`VITE_BUILD_MODE=dev`에서만 동작)

> UI 레이아웃 수치는 `DEC-PIPELINE-016`이 말하는 "변경 가능한 게임 데이터"가 아니다.
> CSV로 뺄 필요 없고 CSS 변수로 충분하다. 반대로 작물 성장 시간·가격·확률은 CSS에도 TS에도 두지 않는다.

---

## 3. 8/1 밤 (착수) — 기획과 개발을 동시에

이 절의 개발 항목은 8/1 밤에 시작한다. 자정을 넘기면 8/2 오전으로 이어서 진행하되,
②번(인터페이스 계약)만은 **8/2 김민주가 작업을 시작하기 전까지** 끝나 있어야 한다.

### 3-1. 기획에서 나와야 하는 것 (개발이 막히지 않을 최소선)

| 항목 | 없으면 막히는 작업 | 기한 |
|---|---|---|
| **1~2단계 CSV 승인** (`maps`, `farm_plots`, `crop_attributes`, `crops`, **`player_base_stats`**) | 8/2 데이터 로더·재배·플레이어 이동 전체 | **8/2 오전, 최우선** |
| `DEC-RUN-010` 전체 일수·습격 일정 | `run_schedules` → 화면 매니저 일차 흐름 | 8/2 오전 |
| CSV 10종 (10~12단계) | 대화·엔딩·일지 전부 | 8/2~8/3 순차 |
| `data/approved/` 승인 담당·시점 | 8/2 이후 모든 데이터 바인딩 | 8/2 오전 |

첫 줄이 특히 중요하다. 1~9단계 초안은 이미 `data/drafts/`에 있으므로 **전부 승인할 필요 없이
맵·작물·플레이어 수치 5종만 먼저 `data/approved/`로 올리면** 8/2 김민주의 로더와 재배 작업이 바로 돌아간다.
지금은 `data/approved/`가 비어 있어 `npm run data:build`가 아무것도 만들지 못한다.

`player_base_stats.csv`는 스키마만 신설됐고(`DEC-CONTENT-019`, `schema_version` 3) **행이 아직 없다.**
`move_speed`가 없으면 플레이어가 못 움직이고 `max_health`가 없으면 런을 시작할 수 없으므로
이 파일은 맵·작물과 같은 순위로 급하다. 승인 행은 정확히 하나여야 한다.

UI/UX(`DEC-UI-*`)와 아트 규격(`DEC-ART-001`)은 **기획팀이 작업 중이며 정해지는 대로 전달**한다.
개발은 기다리지 않고 진행한다. UI 위치·크기는 사진을 받은 뒤 `layout.css` 값만 고치면 되고(2절),
필드는 플레이스홀더로 그리므로 규격 확정 전에도 막히지 않는다.

CSV 10종은 한 번에 다 필요한 게 아니다. **필요한 순서**는 다음과 같다.

1. `run_schedules`, `run_schedule_days` → 8/2 오전 (일차 흐름)
2. `story_scenarios`, `story_infos`, `dialogue_choices`, `dialogue_choice_responses` → 8/3 밤 (조우 판정)
3. `fear_bands`, `endings`, `ending_conditions`, `journal_fallbacks` → 8/4 밤 (엔딩 판정)

이 순서대로만 나오면 개발은 안 막힌다.
현재 `data/approved/`는 비어 있고 초안도 22종뿐이라, **승인 흐름(`drafts → candidates → approved`)이
8/2 오전에 한 번은 끝까지 돌아본 상태**여야 한다.

### 3-2. 최수정 — 기획 확정과 무관하게 오늘 할 수 있는 것

확정 DEC만으로 만들 수 있고, 나중에 UI/데이터가 확정돼도 재작업이 없는 영역이다.
**아래 순서 그대로 진행한다.** 앞의 두 개가 8/2 아침에 김민주가 일을 시작할 수 있는지를 결정한다.

- [x] **⓪ 브랜치·CI 정리** (15~20분) — 7-1절 참고
  - `develop` 브랜치 생성, 이후 개발은 전부 여기서
  - `.github/workflows/ci.yml`의 `push.branches`에 `develop` 추가. **안 하면 develop에서 CI가 안 돈다**
  - `progress` job은 main 전용으로 제한 (개발 중 봇 커밋이 안 생겨 pull 충돌이 준다)
  - `docs/team-rules.md` 3절 갱신
- [x] **① 배포 파이프라인 뚫기** (30~45분) — 우선순위 최상
  - `vercel.json`, `api/health.ts` (표준 `Request`/`Response`만, 팀규칙 7절)
  - Vercel 프로젝트 연결 시 **Production Branch = `main`**. `develop`은 프리뷰 URL로 붙는다
  - Vercel 환경변수에 `ANTHROPIC_API_KEY` 등록 (Production·Preview 양쪽)
  - 완료 조건: develop 프리뷰 URL에서 화면이 뜨고 `/api/health`가 200을 반환한다
  - 왜 지금인가: 배포·서버리스 라우팅·키 설정은 처음 뚫을 때 반드시 한 번 막힌다. 8/5에 만나면 그날이 날아간다
- [x] **② 인터페이스 계약 커밋** (60~90분) — 8/2 김민주의 착수 조건
  - `src/data/types.ts` — 런타임 JSON 타입. `generated/runtime/`은 테이블별 JSON + `manifest.json`이고
    연결 CSV는 부모에 중첩된다(`points`, `days`, `conditions`, `responses` 등). 스키마(`schema/tables/*.json`)에서 유도
  - `src/state/types.ts` — 플레이어 체력·소지금, 보관함 4종, 퀵슬롯·회복 파우치(수량 복사가 아니라 참조 인터페이스: `DEC-RESOURCE-014~018`), 주민 런 상태, 공포도
  - 시스템 ↔ UI 이벤트 계약 — UI는 상태를 직접 쓰지 않고 이벤트로 요청, 시스템이 확정 후 결과 이벤트 발행
  - 완료 조건: `npx tsc --noEmit` 통과, push 완료, 김민주가 pull만 하면 UI·로직 착수 가능
- [x] **③ 게임 루프 + 화면 매니저 골격** (60~90분)
  - 고정 timestep 루프, 일시정지·포커스 이탈 정지 (`DEC-UI-022`, `DEC-INPUT-009`)
  - **화면은 평평한 씬 목록이 아니다** (`DEC-UI-014`). 세 층위로 나눠 선언한다
    - 독립 화면: 타이틀 / 이름 입력 / 튜토리얼 / 일차 시작 / 조우 결과 / 밤 결과 / 런 실패 / 엔딩
    - 베이스 화면: **필드 하나**. `재배 모드`와 `습격 모드` 두 가지로 동작한다
    - 필드 위 오버레이: 정비 허브(셔터 + 보관함·상점·제작) / 전투 전 대화 / 투항 대화 / 일시정지
  - 흐름은 `DEC-RUN-014`, 결과 화면 규칙은 `DEC-RUN-015`. **`취침` 단계는 없다.**
    습격 없는 날은 정비 종료(`아침까지 잔다`) → 밤 결과 → 다음 일차이고,
    `조우 결과`와 `밤 결과`는 같은 층위로 **하루에 하나만** 등장한다
  - 완료 조건: 콘솔에서 화면 전환이 순서대로 찍히고, 습격일/비습격일 두 경로가 갈린다. 내용은 비어 있어도 된다
- [x] **④ 입력 계층** (30~60분) — `DEC-INPUT-001~009` 확정본 그대로
  - WASD 이동, 마우스 조준, `E`, 우클릭(낫), 좌클릭(투척), `1~5`, 휠, `Q` 짧게/길게, `Esc`
  - 입력 잠금 상태(대화 중 필드 입력·타이머 정지) 포함
  - `DEC-INPUT-010`(회피·대시) `DEC-INPUT-011`(연속 발사)은 **보류이므로 구현하지 않는다**
- [x] ⑤ 캔버스 최소 루프 (시간이 남으면)
  - 사각형 플레이어가 WASD로 움직이고 마우스 방향으로 조준선이 돈다 + 카메라
  - 이게 있으면 8/2 김민주의 재배 로직을 화면에서 바로 확인할 수 있다
  - `move_speed`·`collision_radius`는 `player_base_stats.csv`에서 온다 (`DEC-CONTENT-019`).
    승인 전이라 임시 값으로 움직여 볼 수는 있으나 **승인되는 즉시 데이터 경유로 교체**하고,
    그때까지 임시 값은 한 곳에 모아 둔다. 시스템 곳곳에 흩어지면 못 찾는다

### 3-3. 김민주 — 착수 전 준비

- [ ] 저장소 pull, `npm i`, `npm run dev` 동작 확인, Git 사용자 이름 설정 확인 (팀규칙 1절)
- [ ] `.env.example` → `.env` 복사
- [ ] `git switch develop` — 8/2부터 모든 작업은 develop에서
- [ ] 최수정 ②번 커밋이 올라오면 pull해서 타입만 훑어보기

---

## 4. 8/2 ~ 8/5 일자별 작업

각 항목의 **완료 조건**은 "다음 사람이 이걸 전제로 작업을 시작할 수 있는가"로 적었다.

### 8/2 (일) — 김민주 풀타임

**김민주**
- [x] `src/data/loader.ts` — `manifest.json` + 테이블 JSON 적재. **필수 데이터 누락 시 기본값으로 넘기지 않고 오류로 보고** (`DEC-UI-024`)
  - 선행 조건: 맵·작물 4종이 `data/approved/`에 올라와 `npm run data:build`가 성공해야 한다 (3-1절)
  - 승인이 늦어지면 이 항목을 뒤로 미루고 `farming.ts`의 상태 전이부터 먼저 만든다. **초안 CSV를 임시로 승인 폴더에 복사하지 않는다**
  - 완료 조건: 승인된 테이블이 로드되고, 일부러 파일을 지우면 오류 화면이 뜬다
  - 실제로는 **승인 단위가 5종이 아니라 12종**이었다. `DEC-CONTENT-003`이 모든 승인 작물에 base 레시피를 요구해서, 작물을 승인하는 순간 3~5단계까지 함께 승인돼야 통과한다 (8/3 확인)
- [x] `src/systems/farming.ts` — 파종(`spawn_weight` 추첨, 씨앗 단계에서 종류 비공개) / 씨앗→성장→수확 가능 / 재배 단계에서만 성장 / 수확 시 `base_yield` 지급 후 빈 경작지 (`DEC-FARM-001~005`)
  - 완료 조건: 캔버스에서 `E`로 심고 시간이 지나면 자라고 수확하면 보관함 수량이 는다
- [x] 재배 단계 제한시간 (`DEC-RUN-004`). `DEC-RUN-005`(조기 종료)는 보류이므로 조건을 임의로 만들지 않는다 — `src/systems/stage-timer.ts`
- [x] 경작지·작물 3단계 플레이스홀더 렌더 (논리 에셋 ID 참조, 실제 파일 경로 금지)

**최수정 (가용 시간 확인 후)** — **가용 시간이 안 나 세 항목 모두 8/3 밤으로 이월**

- ~~전투 기반 — 낫(우클릭), 투척(좌클릭, 발사 성공 시 1개 소비), `direct`/`area` 판정, 자해·아군 오사 없음 (`DEC-INPUT-004~007`, `DEC-CONTENT-005`)~~ → **8/3 밤 완료** (`src/systems/combat.ts`)
- ~~전투 효과 — `damage_over_time`, `movement_slow`. 같은 효과 비중첩, `effect_rank` 우선, 동급은 최신으로 교체 (`DEC-CONTENT-013`)~~ → **8/3 밤 완료** (같은 파일)
- ~~야생동물 스폰·행동 — `crop_first`는 수확 가능 작물 우선, `player_only`는 플레이어만. 씨앗·성장 중 작물은 공격하지 않음. 재배 종료 시 전부 제거 (`DEC-FARM-006`, `DEC-FARM-010`, `DEC-CONTENT-007`)~~ → **8/3 밤 이월분에 있음**

### 8/3 (월) — 김민주 ~22시 / 최수정 22시~

**김민주**
- [x] `src/systems/economy.ts` — 판매·구매·제작. **거래 확정과 제작은 원자적으로** (`DEC-RESOURCE-011`, `DEC-CRAFT-004`)
- [x] 작물 숙련도 누적과 레시피 해금 (`DEC-CRAFT-007`, `DEC-CRAFT-008`)
- [x] `src/ui/layout.css` — UI 수치 변수 파일 신설 (2절)
- [x] `src/ui/hud.ts` — 체력, 일차·단계, 남은 시간, 투척 퀵슬롯, 회복 아이템, 상호작용 안내, 습격 예고
  - 습격 예고 자리는 **비워 둔 채** 진행했다. `DEC-RUN-011`이 문구를 코드에 두는 것을 금지하는데 `raid_notices.csv`가 없었다. 스키마는 8/3 밤에 올라갔고 문구 3줄 작성·승인이 남아 있다
- [x] 1~12단계 콘텐츠 32종 승인 — 계획에 없던 항목. 이걸로 8/3~8/4 최수정 작업의 데이터 선행 조건이 한꺼번에 풀렸다
- ~~정비 허브 (`DEC-UI-020`) — 셔터 + 보관함 상시 영역 + 기능 버튼 4개~~ → **8/4 이월**
- ~~`src/ui/shop-modal.ts` (`DEC-UI-005`) — 보유 수량·단가·총액, 거래 후 예상 잔액 미리 표시, 불가능한 거래 비활성화~~ → **8/4 이월**
- ~~`src/ui/craft-modal.ts` (`DEC-UI-006`) — 해금·잠김 분리, 레시피 해금 진행도, 최대 제작 횟수~~ → **8/4 이월**
- ~~보관함·퀵슬롯 편성 팝업 (`DEC-UI-021`) — 4분류 표시, 슬롯→무기 2단계 편성~~ → **8/4 이월**
- ~~투척 퀵슬롯 피드백 (`DEC-UI-002`)~~ → **부분 완료.** 선택 강조·소진 비활성화·`투척 무기 없음`은 HUD에 있다. 소진 자동 전환 강조와 빈 발사 안내만 **8/4 이월** (전투 시스템 대기였고 8/3 밤에 붙었다)

**최수정 (22:00~)**

**8/2 이월분 — 이걸 먼저 한다.** 아래 원래 분량이 여기 걸려 있다.

- [x] 전투 기반 — 낫·투척·`direct`/`area` (`DEC-INPUT-004~007`, `DEC-CONTENT-005`) — `combat.ts`
- [x] 전투 효과 — `damage_over_time`·`movement_slow` (`DEC-CONTENT-013`) — 같은 파일
- [x] 야생동물 스폰·행동 (`DEC-FARM-006`, `DEC-FARM-010`, `DEC-CONTENT-007`) — `wildlife.ts`

**8/3 원래 분량 — 전부 완료**

- [x] 주민 전투 — `melee_chase` / `ranged_chase`, 예고선·조준선 없음, `projectile_max_range ≥ attack_range` (`DEC-CONTENT-008`) — `resident-combat.ts`
- [x] 투항 발동 — 체력 1 이상이고 투항 기준 이하로 **처음** 내려갈 때 전투 정지 (`DEC-RESIDENT-016`, `DEC-RESIDENT-039`)
  - 피해 경로는 `combat.ts`의 `applyDamage()` 하나로 모았다. 낫·투척·지속 피해가 전부 같은 순서를 지난다
- [x] `src/systems/encounter.ts` — 전투 전 대화 판정. 선택지 문장이 아니라 **주민 성격 프로필 + 선택 기능**을 조회해 `조우 해결`/`위축`/`일반`/`격앙` 확정 (`DEC-RESIDENT-049`)
- [x] 자원 협상 — 총수량 부족 시 선택 불가, 수락 시 개별 단위에서 결정적 무작위 차감 + 관계 변경을 원자적으로 (`DEC-RESIDENT-050`, `DEC-CONTENT-009`)

**배선** — 만든 날 붙여서 화면에서 확인한다. 계획에 항목으로 없던 것인데, 8/3 밤에
`combat.ts`가 있는데도 `onSickle`이 `console.info` 한 줄이라 낫 쿨타임이 안 도는 것을
플레이 테스트로 발견했다. 단위 테스트 51개가 통과해도 화면에서는 아무 일도 없었다.

- [x] 재배 모드 — 낫·투척·야생동물을 `main.ts`에 붙였다. 야생동물 렌더(체력 막대, 공격 예고, 효과 표시)도 함께
- [x] 습격 모드 — `resident-combat.ts`·`encounter.ts` 배선, 적대 주민 생성, 투항 발동 → `surrender_dialogue` 오버레이
- [x] **김민주 8/4 UI를 대신하는 개발 통로 2종** (최수정 작업). 원래 계획에 없던 항목이다
  - `__dev.fillThrowables(n)` — 제작 모달·편성 팝업을 우회해 무기 보관함과 퀵슬롯을 채운다. 제작 비용과 숙련도 해금을 거치지 않으므로 **밸런스 확인에는 쓸 수 없다**
  - `__dev.startRaid(i)` — 대화 모달을 우회해 선택지를 확정한다. **판정은 우회하지 않는다** — 실제 `encounter.judge()`를 부르고 그 결과가 지정한 전투 보정으로 주민을 세운다 (`DEC-RESIDENT-049`)
  - 둘 다 개발 빌드 전용이고 콘솔 경고를 남긴다. `usePlaceholderStats()`·`enterFieldPreview()`와 같은 취급이다
- [x] `DEC-UI-026` 구현 — 확정된 지 하루 됐는데 코드 주석이 "보류"인 채로 남아 미뤄져 있었다 (아래 11-2)
- [x] 22:00 이전에 김민주가 push, 최수정은 **pull부터** 시작 (CI 봇 커밋 때문)
- [x] `schema/tables/raid_notices.json`·`night_result_texts.json` 신설 + `schema_version` 3→4 (`DEC-CONTENT-021`) — 계획에 없던 항목. 김민주 HUD가 막혀 있었다
- [x] `Esc`로 정비 허브가 닫히던 것 수정 (`DEC-UI-022`, `DEC-RUN-006`)
- [x] `relationship_state` 6키 확정 반영 (`DEC-CONTENT-022`)

### 8/4 (화) — 김민주 ~22시 / 최수정 22시~

**김민주**

> **8/4 에 push 된 커밋이 없어 저장소 기준으로는 전부 이월이다.** 로컬에 있으면
> push 만 하면 이월 목록에서 뺀다.

**8/3 이월분 — 이걸 먼저 한다.** 8/3에 economy·HUD·32종 승인이 들어가면서 정비 UI가 통째로 밀렸다. 로직은 `economy.ts`에 13개 테스트로 고정돼 있고 실패를 `{ok:false, reason}`으로 돌려주므로, 남은 것은 그것을 부르고 버튼을 끄는 UI다.

- [x] 정비 허브 (`DEC-UI-020`) — 셔터 + 보관함 상시 영역 + 기능 버튼 4개 — `ui/maintenance-hub.ts`
  - 팝업을 `scenes` 오버레이로 등록하지 않았다. `DEC-UI-020`이 "정비 허브 내부 화면이며 `DEC-UI-022`의 오버레이 층으로 취급하지 않는다"고 명시한다. 오버레이로 올리면 `DEC-UI-026`의 "기능 오버레이는 하나만" 제한에 걸려 정비 허브 자체가 닫힌다
  - 표시 여부는 `scenes.inputOwner()`로 판단한다. `openOverlays().includes()`로 보면 일시정지가 겹쳤을 때 허브가 계속 입력을 받는다
  - 하단 진행 버튼 문구는 `DEC-RUN-006`의 두 가지를 습격 여부로 고른다. 별도 확인 창을 두지 않는다
- ~~`src/ui/shop-modal.ts` (`DEC-UI-005`) — 보유 수량·단가·총액, 거래 후 예상 잔액 미리 표시, 불가능한 거래 비활성화~~ → **8/5 이월**
- ~~`src/ui/craft-modal.ts` (`DEC-UI-006`) — 해금·잠김 분리, 레시피 해금 진행도, 최대 제작 횟수~~ → **8/5 이월**
- 보관함·퀵슬롯 편성 팝업 (`DEC-UI-021`) — **보관함만 완료, 편성 팝업은 8/5 이월**
  - [x] 보관함 4분류(수확물·재료·무기·소모품) 상시 표시 — 정비 허브 좌측 영역. 팝업이 열려 있어도 계속 보인다
  - [x] 소지금 표시 — 필드 HUD가 아니라 정비 단계에서만 (`DEC-UI-017`)
  - ~~슬롯→무기 2단계 편성 팝업~~ → **8/5 이월**
- ~~투척 퀵슬롯 피드백 나머지 (`DEC-UI-002`) — 소진 자동 전환 강조, 빈 발사 안내~~ → **8/5 이월.** 로직은 8/4 새벽에 최수정이 배선해 콘솔로 확인했고 화면 표시만 남았다

> **위 항목이 막고 있던 최수정의 확인 작업은 8/3 밤에 최수정이 개발 통로로 대신 처리했다.**
> ~~craft-modal·편성 팝업이 없어 최수정이 투척을 확인할 수 없다~~ → `__dev.fillThrowables()`
> ~~대화 모달이 없어 최수정이 조우 판정·습격·투항을 확인할 수 없다~~ → `__dev.startRaid()`
>
> **UI 자체는 여전히 김민주 몫이다.** 개발 통로는 제출 빌드에서 아예 동작하지 않고
> 제작 비용·숙련도 해금·마우스 선택을 전부 우회하므로 위 항목을 대체하지 못한다.
> 두 UI가 들어오면 최수정이 통로를 지운다 (11-2).
- [x] `raid_notices.csv` 문구 3줄 작성·승인 (`none`/`raid`/`final_raid`)
  - **CSV 승인까지다. HUD 배선은 아직 안 됐다** — `main.ts` 의 `hudView()`·`hubView()` 가 여전히 `raidNoticeLabel: null` 을 넘기고 주석도 "승인되면 여기에 들어간다" 인 채다. 8/5 에 이 줄이 "자리가 채워졌다" 로 적혀 있었는데 화면에는 아무것도 안 나온다. 11-1 의 31번은 `뼈대` 로 맞게 남아 있었다. 배선은 `main.ts` 라 최수정 (8/5)
- [x] **계획에 없던 콘텐츠 3종도 같이 작성·승인.** 8/4~8/5 화면이 이걸 읽어야 해서 먼저 처리했다
  - `night_result_texts.csv` — **한 행만 썼다.** 여럿 중 무엇을 고를지가 `DEC-CONTENT-018` 보류라 코드가 고를 근거가 없다
  - `fear_increments.csv` — `DEC-RESIDENT-048`의 위협 1·퇴각 3·처치 6. 승인돼서 `resolution.ts`의 공포도가 0에서 풀렸다
  - `journal_fallbacks.csv` — 9행(구간 3 × 방향 3). **`down` 3행은 도달 불가다** — `DEC-RESIDENT-046`이 공포도를 감소하지 않는 누적으로 정했는데 `covers_all_bands_and_directions` 규칙이 `down`을 요구한다. **전성민 8/5 판단 — 알고 남긴다** (11-2)
- [x] `resident_combat_modifiers.csv`의 `display_name`을 화면 문구로 (`DEC-UI-009`) — "위축 보정"→"위축됨". 이 열이 습격 HUD에 그대로 노출된다. `content_version` 2
- [x] `hud.ts`의 "회복 없음"을 `DEC-RESOURCE-017` 원문인 "회복 아이템 없음"으로 (전성민 지적)
- [x] `layout.css` 주석 3곳 갱신 — `DEC-UI-025`(1920×1080)·`DEC-ART-001` 확정 반영. **색·여백은 이제 임시가 아니라 여기가 원본**이라고 명시
- [x] 재배 이벤트 4종 발행 — `farm.planted`·`farm.plotReady`·`farm.harvested`·`farm.timeExpired`. 선언만 있고 아무도 안 쏘던 계약을 살렸다. `farm.plotReady`는 최수정 판단 요청에 대한 답이며 **유지가 맞다** — `DEC-UI-004`가 효과음을 요구하고 오디오는 별도 서브시스템이라 버스가 맞는 통로다

**8/4 원래 분량**

- ~~`src/ui/dialogue-modal.ts` — 시작 대사, 선택지 3개(기능 태그 표시), 반응 대사, 자원 요구 수량 표시 (`DEC-RESIDENT-014`, `DEC-UI-007/008`)~~ → **8/5 이월**
- ~~`src/ui/surrender-modal.ts` — 투항 3선택지(수용·영입 / 대가 요구·퇴각 / 거부·전투 계속), 전투 정지 표시~~ → **8/5 이월**
- ~~결과 화면 4종 — **파일을 나눠 만든다** (`DEC-RUN-015`, `DEC-UI-014`, `DEC-UI-023`)~~ → **8/5 이월. 단 4종 중 2종만 김민주다** — `run-failed.ts`·`ending.ts` 는 11-1 의 27·28번이 최수정이라 그쪽으로 맞췄다
  - 김민주 — `encounter-result.ts` (조우 결과) / `night-result.ts` (밤 결과)
  - 최수정 — `run-failed.ts` / `ending.ts` (11-1의 27·28번이 최수정이라 그쪽으로 맞췄다)
  - 밤 결과는 그날 실제 기록을 담지 않고 **승인된 고정 문구만** 표시한다 (`DEC-RUN-015`). 문구 목록·선택 방식은 후속 콘텐츠 기획 대기
  - 런 실패와 엔딩은 공유 템플릿으로 묶지 않는다 (`DEC-UI-014` 명시)
- ~~전성민에게 받은 UI 사진 대조 → `layout.css` 값 조정, 폰트·아트 플레이스홀더 교체~~ → **8/5 이월**
  - 폰트는 **웹 임베딩 허용 여부**를 먼저 확인하고 `docs/submission/CREDITS.md`에 같은 커밋으로 기록 (팀규칙 6절)

**최수정 (22:00~)**
- ~~판정 엔진 ↔ 대화 UI 연결. 조우 최종 결과 5키 확정 (`DEC-RESIDENT-052`)~~ → **부분 완료.** 5키 확정과 이벤트 계약(`encounter.finished`·`reward.granted`)은 됐다. 실제 UI 배선만 **8/5 이월** (김민주 대화·투항 모달 대기)
- [x] 관계 5상태 + 공포도 누적 + 해결된 주민 재등장 방지 (`DEC-RESIDENT-012`, `046`, `043`) — `systems/resolution.ts`. **공포도는 자리까지. 증가량은 `fear_increments.csv` 승인 대기라 0으로 멈춰 있다**
- [x] 퇴각·처치 보상 묶음 지급을 원자적으로, 중복 지급 방지 (`DEC-RESIDENT-042`) — 플레이 확인
- [x] 엔딩 판정 — 공포도 구간 + 조건 + 우선순위 + 대표 작물 (`DEC-CONTENT-011`) — `systems/ending.ts`. 5일차 마지막 습격에서 플레이 확인
- [x] `api/ending.ts`, `api/journal.ts` + `src/llm/` (`DEC-CONTENT-011`, `DEC-JOURNAL-001~003`) — 배포 확인. **LLM 실패가 진행을 막지 않는 것을 실제 배포에서 확인했다** (프리뷰에 키가 없어 폴백 경로가 그대로 검증됐다). `journal.ts` 는 아직 한 번도 안 불렸다
- [x] `schema/ending_prompt_system.md`, `schema/journal_prompt_system.md` 신설 (`DEC-PIPELINE-012`, `DEC-JOURNAL-004`) — edge 에 파일 시스템이 없어 `tools/build-prompts.mjs` 가 빌드 시점에 모듈로 굽는다. 원본은 여전히 `.md` 하나다
- [x] `schema_manifest.json`에 `journal_prompt_version` 추가

> **8/4 최수정 결산 — 7항목 중 6 완료, 1 부분(대화 UI 배선 대기).**
> 계획에 없었는데 한 것: 버그 5개(체력 0에도 전투 지속 / 낫 처치 시 조우 미종료 /
> `canAppearAsHostile` 배선 누락 / 조우 종료 후 흐름 미전진 / 투항 개발 통로 가드),
> `fear_increments` 스키마(`DEC-RESIDENT-048` 확정 반영), 배포 4회 수정.
> **전부 11-2에 반영했다.**

### 8/5 (수) — 최수정 풀타임 / 김민주 ~21시

**최수정**

**8/4 이월분**
- [x] 판정 엔진 ↔ 대화 UI 배선 — **김민주가 `dialogue-modal.ts` 와 함께 `main.ts` 에서 배선했다.** 개발 통로 없이 조우 결과까지 플레이로 확인됐다 (11-1 의 18·21번)
- [x] **개발 통로 제거** — 위 배선의 후속이라 줄을 나눴다. 선행 조건(제작·편성·대화 모달)이 전부 도착했으므로 **`__dev.startRaid()`·`__dev.surrender()`·`__dev.fillThrowables()` 셋을 지운다.** ~~남기는 셋은 `devSkipToFarming()`·`enterFieldPreview()`·`__dev.goToDay()` 다~~ → **남기는 것은 `devSkipToFarming()` 하나다** (8/5 밤 수정). 일차 시작·런 실패·엔딩 화면이 1·2번으로 생겨 `goToDay` 를 남길 근거가 사라졌고, 게다가 `syncRunDay()` 에 덮여 조용히 틀린 상태를 만든다 (11-2). `enterFieldPreview()` 는 `devStartRaid()` 가 유일한 호출자라 같이 사라진다. 타이틀·이름 입력·튜토리얼이 아직 뼈대라 `devSkipToFarming()` 만 남는다.
  - **넷 다 지웠다 (8/5).** `__dev` 노출 자체가 사라졌고 `window.__dev` 는 `undefined` 다. 남은 노출은 읽기용 `__scenes`·`__bus`·`__loop` 뿐이다
  - 지운 뒤 정상 경로로 2일차 습격 대화까지 확인했다 — 흐름이 `{at:'raid', day:2}` 이고 런 상태도 2일차다
  - `usePlaceholderStats()` 는 **남긴다.** 승인 데이터 적재가 실패했을 때만 불리는 명시적 선언이라 통로가 아니다 (`bootData()` 의 catch)
  - `scenes.enterFieldPreview()` 는 게임 쪽 호출부가 없어졌지만 API 는 남았다 — `tests/scene-manager.test.ts` 10곳이 픽스처로 쓴다. 11-2 참조 `usePlaceholderStats()` 는 `player_base_stats.csv` 승인 뒤 실제로 불리는지 같이 본다 (11-2)
- [x] `fear_increments.csv` 승인 3줄 반영 — **끝났다.** 김민주가 8/4 에 승인했고 `main.ts` 가 `readFearIncrements()` 로 읽는다. 공포도가 실제로 누적되므로 **"항상 온기 기본 엔딩" 은 이제 사실이 아니다.** 8/5 에 이 줄이 `전성민 확인 대기` 인 채로 남아 있었다 — 11-1 의 26번도 같은 문구라 함께 고쳤다

**8/5 원래 분량**
- [x] 전 모듈 통합. **두 경로 모두** 완주 1회 성공
  - **8/5 밤 완주.** 타이틀 → 이름 입력 → 튜토리얼 → 1일차부터 5일차 이장 영입 → 엔딩(`밭두렁의 이웃`)까지 개발 통로 없이 화면으로만 밟았다
  - 엔딩 화면이 `DEC-UI-023` 대로 다 나왔다 — 제목·확정 요약·기록문·`타이틀로`. 공포도는 개발 빌드에서만 (`[개발] 공포도 0 · 온기`)
  - 기록문은 승인된 폴백이다 (프리뷰에 키가 없다). `DEC-CONTENT-011` 상 정상 경로
  - 습격일: 타이틀 → 이름 입력 → 일차 시작 → 재배 → 정비 → 전투 전 대화 → 습격 → 조우 결과 → (마지막이면) 엔딩
  - 비습격일: 일차 시작 → 재배 → 정비(`아침까지 잔다`) → 밤 결과 → 다음 일차
- [ ] 튜토리얼 (본 런과 시간·체력·자원 비공유, `DEC-RUN-003`)
  - **`tutorial_steps.csv` 대기.** `DEC-UI-015`·`DEC-UI-029` 가 안내 문구를 데이터 쪽으로 확정해 코드에 둘 수 없다. 화면과 단계 전환은 문구 없이 만들 수 있다
  - **`completion_key` 일곱 개를 코드가 판정한다** (`DEC-CONTENT-023`) — `plant_crop`·`harvest_crop`·`use_sickle`·`use_throwable`·`sell_crop`·`buy_material`·`craft_item`. 고정 키라 새로 만들 수 없다. 더 필요하면 스키마·구현 변경으로 올린다
  - ~~**`src/data/types.ts` 의 `Kind` 에 `tutorial_step` 이 빠져 있다**~~ → **8/5에 넣었다.** `enums.json` 에는 8/4부터 있었다 (김민주 발견)
- [x] 일차 시작 연출 + 일지 표시 (1일차 아침은 표시하지 않음, `DEC-JOURNAL-001`)
  - `src/ui/day-start.ts` · `src/llm/journal.ts` · `main.ts` 배선. **2일차 아침을 플레이로 확인했다** — 일차, 습격 예고 문장(`raid_notices.opening_text`), 일지, 진행 버튼
  - 일지 대기 표시도 확인했다. 로컬은 폴백이 즉시 떨어져 안 보여서 `/api/journal` 요청을 붙잡아 두고 봤다
  - **`down` 방향은 코드에서 안 나온다.** 공포도가 감소하지 않아서다(`DEC-RESIDENT-046`). 비교는 그대로 두고 `down` 을 지어내지 않는다 — 이미 **전성민이 8/5에 "알고 남긴다" 로 판단**했고 11-2에 기록돼 있다
  - **`journal_input_schema_version` 이 없다.** 엔딩 입력에는 있는데 일지 입력에는 매니페스트에 대응 항목이 없다. 새로 만들지 않고 `journal_prompt_version` 만 넣었다. 물어야 할 것은 버전이 아니라 **일지 생성 기록을 남길 것인가** 다 — 11-2 참조
  - LLM 성공 경로는 아직 실제 키로 못 봤다. 폴백 경로만 확인했다 (키 문제는 11-2 8/5 행)
- [x] 런 실패 **판정** — 재배·습격 중 체력 0이면 즉시 전체 런 실패 (`DEC-RUN-008`).
      8/4 새벽에 넣었고 8/5 에 습격 경로를 한 번 더 막았다. 양쪽에서 플레이 확인
- [x] `run-failed.ts` **화면** — 흐름은 `run_failed` 단계로 가는데 그릴 화면이 없다 (11-1의 28번)
  - 실패 사실·도달한 일차·타이틀로 돌아가는 입력 셋만이다. 엔딩 제목·요약·기록문·런 통계는 `DEC-UI-023` 이 금지한다
  - 2일차에 체력 0으로 떨어뜨려 플레이 확인. `타이틀로` 가 실제로 `title` 단계까지 간다
- [x] `ending.ts` **화면** — 판정과 기록문은 8/5 새벽에 붙었고 표시할 화면이 없다 (11-1의 27번).
      런 실패와 공유 템플릿으로 묶지 않는다 (`DEC-UI-014` 명시)
  - 제목·요약·기록문. 기록문 대기 표시, 폴백·생성분 구분 없음, 재생성 입력 없음 (`DEC-UI-023`)
  - 공포도는 개발 빌드에서만 붙는다 (`DEC-RESIDENT-047`) — 11-1 의 26번 `구간별 분기 미확인` 을 완주에서 눈으로 볼 수단이다
  - **화면을 플레이로 못 봤다.** 엔딩에 닿으려면 5일차 이장(체력 200)과 실제로 싸워야 하고 이장은 평화 해결 경로가 없다(`chief` 성격은 세 선택 다 전투). 5번 완주에서 확인한다
- [x] `VITE_BUILD_MODE=submission`에서 공포도 수치·검증 오류 표시가 숨는지 확인 (`DEC-RESIDENT-047`, `DEC-UI-024`)
  - **게이트는 정상이다.** 제출 빌드에서 `__scenes`·`__dev` 가 노출되지 않고 개발 로그도 안 나온다
  - **그런데 그 빌드로는 진행이 안 된다.** 11-2 참조 — 타이틀·이름 입력·튜토리얼이 뼈대이고 `devSkipToFarming()` 은 개발 빌드 전용이라 첫 화면에서 못 나간다
- [x] **`surrender_opening_story_info_id` 기록** — `DEC-CONTENT-017` 이 *"투항 대화가 시작되면 `surrender_opening_text` 를 표시하고 연결된 `core` 사연 정보를 확인한 정보로 기록한다"* 로 **원래부터 확정**이었는데 기록하는 곳이 없다. 김민주는 precombat 만 비었다고 알렸으나 실제로는 양쪽 다 빠져 있었다. `precombat` 쪽은 오늘 `DEC-CONTENT-024` 로 확정됐다 (`basic` 정보)
  - 안 하면 **싸워서 전부 죽인 런은 엔딩 LLM 에 넘길 사실이 하나도 없다** (전성민 8/5)
  - **양쪽 다 넣었다 (8/5).** 대화가 열리는 순간 기록한다 — 선택보다 앞이라 무엇을 고르든, 싸워서 죽이든 남는다
  - 2일차 영순으로 플레이 확인. `basic`(전투 전)·`core`(투항) 둘 다 조우 결과의 `알게 된 것` 에 실제로 떴다
  - `recordRevealedStoryInfo()` 에 개발 빌드 로그를 넣었다 — 기록됐는지가 조우 결과까지 가야 드러나서 그 전에는 "한다" 와 "안 한다" 가 같아 보였다
- [ ] **`develop` → `main` 병합** (`--no-ff`) + `git tag v0.1-build1` + 프로덕션 URL 동작 확인 → 팀 공유
  - 이 시점부터 main은 8/8 촬영용 안정판이다. 이후 수정은 develop에서 하고 검증 후 다시 병합한다

**8/5 오후 이관분 — 김민주 코드 작업**

> **왜 옮겼나.** 김민주 작업 시간대가 마일스톤과 어긋나(새벽) 뒤 작업이 밀린다.
> `src/` 는 팀규칙 4절에서 원래 최수정 영역이므로 **코드는 전부 최수정이 가져오고,
> CSV 작성·승인과 기획 판단만 김민주에게 남긴다.** 플레이 테스트는 가져올 수 있지만
> 일부러 남겼다 — 사유는 김민주 항목에 적었다.

- [x] 승인 CSV 전량 `npm run data:validate` → `npm run data:build` → 클라이언트 바인딩 검증
  - `data:validate` 문제 없음 (36/38 작성 · 미작성은 `content_assets`·`tutorial_steps` 둘이며 알려진 것). `data:build` 25개 + 매니페스트
  - **코드가 한 번도 안 읽는 승인 테이블은 `resident_support_attack_profiles` 하나다** (4행). 이미 11-2에 있는 영입 주민 지원 공격 미구현과 같은 건이라 새 줄을 만들지 않았다
  - 연결 CSV 는 부모에 병합돼 **다른 이름으로** 읽힌다. `points`·`farm_plots`·`inputs`·`entries`·`choice_outcomes`·`responses`·`days`·`journal_fallbacks`·`conditions`·`mastery_unlock` 전부 읽히는 것을 확인했다
  - 검증 방법: 매니페스트의 `files` 로 최상위 테이블을, `data/types.ts` 의 자식 배열 이름으로 연결 테이블을 각각 `src/` 에서 찾는다. **테이블 이름으로만 찾으면 연결 CSV 가 전부 미사용으로 보인다** — 처음에 그렇게 세어 다섯 개를 잘못 의심했다
- [x] `DEC-UI-025` 기준 해상도 — `index.html`·`layout.css`·`field.ts`. `index.html` 주석의 "보류" 표기도 같이 정리한다
  - `#stage` 하나가 1920×1080 을 갖고 그 상자만 비율 유지로 확대·축소한다. **필드와 UI 를 서로 다른 비율로 늘리지 않는다**는 확정문 때문에 둘을 같은 상자에 넣었다
  - 5:4 · 세로 · 울트라와이드에서 비율 1.778 유지와 여백을 확인했다. 캔버스는 언제나 1920×1080 이라 마우스 좌표도 안 깨진다
  - 크기 숫자는 `src/render/stage.ts` 한 곳에만 둔다 — CSS 에도 적으면 두 곳이 갈라진다
- [ ] **A1 에셋 통합** — `assets/final/` 19종.
  - **전성민 8/5 17:28 회신 (캡처 3장에 대한 답).** ① 정비 화면은 배치 그대로 ② **대화창은 초상화 ID 를 넣을 거라 배치가 약간 바뀐다 — 별도 메시지 대기** ③ **캡처의 UI 요소가 작은데 아트는 그보다 큼직하게 나온다**
  - 실측 (`assets/final/`, `DEC-ART-001` 이 월드 1단위 = 화면 1픽셀로 정해 에셋 크기가 곧 화면 크기다)

    | 요소 | 에셋 | 현재 `layout.css` |
    |---|---|---|
    | 퀵슬롯 칸 | 154×145 | `--hud-slot-size: 48px` |
    | 회복 칸 | 162×166 | 48px |
    | 플레이어 카드 | 585×161 | `--hud-health-width: 220px` |
    | 재배 타이머 | 911×83 | 텍스트 |
    | 표지판 | 253×174 | 텍스트 |
    | 설정 버튼 | 138×122 | 텍스트 버튼 |
    | 경작지·작물 | 175×160 | 렌더 상수 `PLOT_HALF_SIZE 45` |

    퀵슬롯 5칸(770) + 회복(162) = 932px 이라 1920 하단에 들어간다.
  - **`farm_plots.csv` 좌표가 아직 옛 간격(140×120)이다.** 스프라이트가 175×160 이라 그대로 그리면 겹친다. 전성민이 새 좌표(200×180)를 김민주에게 넘겼고 반영 전이다. **코드는 데이터를 읽으므로 좌표가 오면 자동으로 벌어진다** — 하드코딩으로 우회하지 않는다
  - **대화 모달 재배치는 전성민 메시지가 온 뒤에 한다.** 지금 손대면 두 번 일한다 이건 원래 "UI 사진 대조 → `layout.css` 값 조정" 이었는데 실물이 도착하면서 범위가 달라졌다
  - 그리는 순서 셋: `asset.background.prototype_field` → 경작지·작물·캐릭터·동물 → `asset.ui.field_frame_front` → HUD
  - **배경은 월드 좌표가 아니라 화면 중앙 기준**이다. 월드는 1600×900인데 배경은 밭 바깥까지 포함해 1920×1080이라 `DEC-UI-025` 작업과 같이 봐야 한다
  - `field_frame_front` 는 야생동물보다 위에 그린다 — 동물이 그 밑에서 나오는 것처럼 보이게 하는 것이 목적이다
  - `asset.ui.plot_highlight` 로 `DEC-UI-018` 대상 경작지를 강조한다
  - **행동 안내(`DEC-UI-018`)는 에셋 없이 글자만 띄운다.** 문구("파종"/"수확")도 코드에 둔다 — 경작지 네 상태가 이미 외형으로 구분되니 새 정보가 아니라 확인용 라벨이고 `DEC-UI-029` 기준으로 확인 버튼과 같은 갈래다 (전성민 8/5 판단). 색·여백은 `layout.css`
- [ ] **`hud.ts` DOM 재배치** — A1 확정 배치가 현재 `hud.ts` 와 **일곱 중 여섯**이 다르다. `layout.css` 값만으로는 안 된다
  - 일차 → 상단좌측 표지판 위칸 / 습격 예고 → 같은 표지판 아래칸 (지금은 `topLeft`·`topCenter` 로 갈려 있다)
  - 재배 시간 → 상단중앙 가로 게이지, **숫자 표시 없음**. 텍스트에서 게이지로 바뀌어 `HudView` 타입과 `main.ts` 의 `hudView()` 가 같이 걸린다
  - 체력 → 하단좌측 플레이어 카드 (초상화·이름·체력 바·바 오른쪽 끝 수치)
  - 퀵슬롯 5칸 → 하단우측, 그 오른쪽에 회복 칸이 화면 끝에 닿게
  - 설정 버튼(상단우측)만 그대로다
  - **플레이어 이름은 아트 장식으로 처리한다.** `DEC-UI-017` 의 공통 요소 여덟 개에 이름이 없으나 `DEC-UI-015` 이름 입력에서 오는 값이라 새 정보가 아니다. 근거는 아트 디렉션 14.2이며 `DEC-UI-017` 을 폐기·대체하지 않는다. **이름 입력 화면이 아직 뼈대라 런 상태에 이름이 실제로 들어가는지 이때 확인한다**
  - `raid_notices` HUD 배선을 여기서 같이 한다 — 지금 `topCenter` 의 `raidNotice` div 에 배선하면 그 div 는 표지판으로 합쳐지며 사라진다
- [x] `schema/enums.json` 에 UI 에셋 ID **12개** 고정 목록 신설 — `open_questions` 가 *"실제 화면과 에셋이 나올 때 스키마 변경으로 넣는다"* 로 미뤄둔 조건이 충족됐다. `close_button`·`resident_hp_gauge` 는 B단계라 아직 파일이 없다
  - **11개가 아니라 12개다.** `assets/final/ui/` 에 파일이 10개 있고 B단계 둘을 더하면 12다. 전성민이 "12개" 라고 하면서 13줄을 적었고 11-2 가 거기서 배경 하나를 빼 11로 옮겨 적었다. 파일 기준으로 맞췄다
  - `asset.background.prototype_field` 는 `content_assets.csv` 쪽이라 이 목록에 없다 (11-2 · 전성민 판단)
  - `logo`·`hud`·`font` 구간은 파일도 확정 ID 도 없어 넣지 않았다. 필요할 때 같은 절차로 추가한다
- [x] 전성민에게 `maintenance-hub`·`dialogue-modal` 화면 캡처 전달 — A3·A4 목업이 이것 때문에 막혀 있다
  - `docs/planning/captures/` 에 3장. 보관함 항목 행이 안 보여 무기가 든 정비 허브를 한 장 더 넣었다
  - 같은 폴더 `README.md` 에 **HUD·필드는 목업 기준이 아니다** 를 적었다 — 8번(A1 통합)·9번(`hud.ts` 재배치) 전이라 지금 캡처의 그 두 부분은 곧 바뀐다
- [ ] `--ui-danger: #e8613c` 는 **그대로 둔다.** 게이지가 차 있는 면은 아트 디렉션 4.1 판정 색 규약의 예외다 (틀과 판은 계속 무채색)

**실행 순서 (8/5 오후 확정)**

```
0  전성민에게 캡처 2장            ← 전성민이 이것 때문에 막혀 있다  [완료 · 3장]
1  day_start 화면 + 일지 + 습격 예고  ← 완주의 마지막 구멍
2  run-failed.ts · ending.ts 화면
3  개발 통로 제거 (셋 + goToDay)        [완료 · 8/5 밤에 4번과 맞바꿨다]
3-1 승인 CSV 전량 validate → build → 클라이언트 바인딩 검증   [완료]
4  DEC-CONTENT-018 가드 · DEC-CONTENT-024 story_info 기록   [완료]
   + 김민주가 확인 요청한 syncRunDay() 설계와 types.ts 추가분
5  완주 2경로   [완료 · 8/5 밤 엔딩까지 확인]
7  enums.json UI 에셋 ID 12개   [완료]
8  DEC-UI-025 [완료] + 3층 렌더 + 스프라이트
9  hud.ts DOM 재배치 + raid_notices 배선
10 타이틀 · 이름 입력 · 튜토리얼   [화면 완료 · 튜토리얼 안내는 CSV 대기]
11 캡처 3장 재촬영 + README 경고 문장 갱신   ← 8·9번이 캡처를 낡게 만든다
12 submission 빌드로 완주(엔딩 포함) → develop → main --no-ff + v0.1-build1
───────────────────────────────── 12번을 지나면 8/8 촬영용 안정판
```

> **11번을 빼먹으면 캡처가 거짓말을 한다.** 0번에서 보낸 캡처의 README 는
> *"HUD 과 필드는 목업 기준이 아니다 — 8·9번에서 바뀐다"* 라고 적혀 있다.
> 8·9번이 끝나는 순간 그 문장이 **반대로** 된다. 그때가 확정 배치인데 문서는
> 계속 아니라고 말한다. 캡처를 다시 찍고 그 문단을 지우는 것까지가 한 작업이다.

> **6번을 12번에 합쳤다 (8/5 밤).** 원래는 완주 → 병합 → 아트였는데, 아트를 얹으면
> 어차피 완주를 다시 밟아야 해서 같은 확인을 두 번 하게 된다. 담당자 제안으로
> 병합을 제출 빌드 완주 뒤로 옮겼다. **2경로의 흐름 전환 자체는 이미 확인됐다** —
> 비습격일은 끝까지, 습격일은 4일차 조우 결과 → 다음 일차까지 밟았다.
>
> **남은 위험은 엔딩 화면이 아트 뒤에 처음 열린다는 것이다.** 그 화면은 만든 사람이
> 한 번도 플레이로 못 봤다. 거기서 깨지면 원인이 화면인지 아트인지 갈라내기 어렵다
> — 8/4에 네 번 난 "배선 안 하고 완료 보고" 와 같은 모양이다. 8번 시작 전에 최수정이
> 엔딩 화면만 따로 열어 보고, 안 되면 그 사실을 여기 적어 12번에서 먼저 보게 한다.

> **5번은 10번에 막혀 있었다.** 5번의 습격일 경로가 *"타이틀 → 이름 입력 → 일차
> 시작 → …"* 로 시작하는데 그 두 화면이 뼈대였다. 그래서 8/5 밤까지 밟은 완주는
> 전부 `devSkipToFarming()` 이 대신 넘긴 것이고 **"전 모듈 통합" 이 아니었다.**
> 담당자가 플레이 테스트 중에 짚어 줬다. 10번을 앞으로 당겨 8/5에 끝냈다.

> **3과 4를 맞바꾼 이유.** 통로가 남아 있는 동안은 테스트 결과를 믿을 수 없다.
> 실제로 담당자가 `__dev.goToDay(5)` 로 이장까지 이겼는데 엔딩이 안 떠서 엔딩
> 화면을 의심했고, 원인은 통로가 런 상태만 옮겨 5일차 습격이 1일차로 판정된
> 것이었다 (11-2). 통로를 먼저 지우면 그 뒤 확인이 전부 정상 경로로만 돈다.
> 4번은 데이터·기록 작업이라 순서를 바꿔도 잃는 것이 없다.

> **3-1 은 왜 소수점인가.** 김민주 이관분 중 "승인 CSV 전량 `data:validate` →
> `data:build` → 클라이언트 바인딩 검증" 이 실행 순서를 짤 때 안 옮겨졌다.
> 8/5 밤에 발견해 끼워 넣었는데, 뒤 번호를 전부 밀면 팀이 이미 번호로 이야기하고
> 있어서 대화가 어긋난다. `validate`·`build` 는 계속 돌고 있고 남은 것은
> **승인된 테이블이 실제로 코드에서 읽히는지** 확인하는 쪽이다.

> **3번이 넷이 된 이유.** `__dev.goToDay()` 를 남기기로 한 근거가 사라졌다.
> 원래 이유는 *"정비 허브와 결과 화면이 없어서 하루를 넘길 수단이 없다"* 였는데
> 실행 순서 1·2번으로 일차 시작·런 실패·엔딩 화면이 다 생겼다. 게다가 지금은
> `syncRunDay()` 에 덮여 **조용히 틀린 상태를 만든다** (11-2). 남는 것은
> `devSkipToFarming()` 하나이며 타이틀·이름 입력·튜토리얼이 아직 뼈대라서다.
> `enterFieldPreview()` 는 `devStartRaid()` 가 유일한 호출자라 같이 사라진다.

> **아트를 병합 뒤에 두는 이유.** `v0.1-build1` 의 기준은 *"언제 열어도 시연 가능"* 이지
> 완성된 그림이 아니다. 지금은 `day_start` 화면이 없어 **2일차로 넘어가지 못한다** —
> 아트가 아무리 좋아도 완주가 안 되면 시연할 것이 없다. 아트는 `v0.2` 로 간다.

> **`farm_plots.csv` 좌표.** 김민주가 5번 전에 올리면 그 좌표로 한 번에 확인하고,
> 늦으면 지금 좌표로 완주를 보고 좌표는 8번에서 같이 본다. 어느 쪽이든 진행은 막히지 않는다.

**김민주**

**8/4 이월분 — 8/4 커밋이 없어 아래가 통째로 넘어왔다.** 11-1에서 `없음`·`뼈대`로 남은
줄이 아홉이고 그중 **조우 결과 화면·정비 모달이 P0** 다. 특히 조우 결과 화면이 없으면
`확인`을 누를 수단이 없어 **엔딩 화면에 도달하지 못한다** — 판정과 기록문은 8/5 새벽에
붙었다.

- [x] 정비 허브 (`DEC-UI-020`) · `shop-modal.ts` (`DEC-UI-005`) · `craft-modal.ts` (`DEC-UI-006`) · 편성 팝업 (`DEC-UI-021`)
  - 기능 버튼 네 개가 다 살았다 (8/5)
- [x] `dialogue-modal.ts` (`DEC-UI-007/008`) · `surrender-modal.ts`
  - **두 대화를 한 파일(`dialogue-modal.ts`)로 만들었다.** `DEC-UI-010` 이 "투항 대화는 전투 전 대화와 같은 표시·입력 규칙을 사용한다"고 확정했고, 규칙이 같다고 못 박은 것을 두 벌로 만들면 한쪽만 고쳐진다 (`AGENTS.md` 2절 — 로드맵보다 결정 로그가 위다). `surrender-modal.ts` 는 만들지 않았다
- [x] 결과 화면 **2종** — `encounter-result.ts` (조우 결과) · `night-result.ts` (밤 결과)
  - 밤 결과는 그날 실제 기록을 담지 않고 **승인된 고정 문구만** 표시한다 (`DEC-RUN-015`).
    일차 숫자도 넣지 않았다 — `DEC-UI-023` 이 "고정 문구만"이라고 확정했다
  - 여러 문구 중 무엇을 고를지는 `DEC-CONTENT-018` 보류라 **승인 행이 정확히 하나일 때만**
    표시한다. 여럿이면 첫 행을 쓰지 않고 데이터 오류로 올린다 — 그 순간 코드가 보류 결정을
    대신 확정하는 것이 된다
  - 조우 결과는 `DEC-UI-011`·`DEC-UI-013` 을 그대로 따랐다. 보상이 없으면 보상 영역을
    만들지 않고, 공포도는 **개발 빌드에서만** 나온다
  - 두 화면은 클래스도 공유하지 않는다 (`DEC-UI-023` — 공유 템플릿으로 묶지 않는다)
  - **`run-failed.ts`·`ending.ts` 는 최수정이 만든다.** 4절 8/4 는 결과 화면 4종을 전부
    김민주로 적었는데 11-1 의 27·28번은 최수정이다. 8/5 에 그 불일치를 발견해 11-1 쪽으로
    맞췄다 — 나누지 않으면 아무도 안 만들거나 같은 파일을 둘이 만든다
- [x] 투척 퀵슬롯 피드백 나머지 (`DEC-UI-002`) — 소진 자동 전환 강조, 빈 발사 안내. **로직은 8/4 새벽에 최수정이 배선해 콘솔로 확인했고 화면 표시만 남았다**
  - 화면 표시 완료 (8/5). 전환 강조는 배경, 상시 선택 강조는 테두리로 나눴다 — 같은 모양이면 자동 전환이 일어난 것을 알 수 없다
  - **빈 발사음은 없다.** 오디오가 별도 서브시스템이고 6절 P2 다. 안내만 나간다
- [x] `raid_notices.csv` 3행 · `night_result_texts.csv` **1행만** (`DEC-CONTENT-018` 보류라 여러 행이면 코드가 고를 근거가 없다)
- ~~전성민에게 받은 UI 사진 대조 → `layout.css` 값 조정, 폰트·아트 플레이스홀더 교체~~ → **최수정으로 이관**
  - 8/5 오후에 A1 확정 에셋 19종이 도착해 실제로 할 수 있게 됐고, `hud.ts` 의 DOM 그룹을 다시 짜야 해서 `layout.css` 값 조정 범위를 넘는다. 아래 이관 사유 참조
  - **폰트만 남는다** — `assets/final/font/` 가 아직 비어 있고 아트 디렉션 14.1이 폰트 종수를 미정으로 둔다. 파일이 오면 **웹 임베딩 허용 여부**를 확인하고 `docs/submission/CREDITS.md` 에 같은 커밋으로 기록한다 (팀규칙 6절). 전성민 대기라 담당을 옮기지 않는다
- [x] `layout.css` 주석 3곳 — 확정된 `DEC-UI-025`(1920×1080)·`DEC-ART-001` 반영 완료 (8/4)
  - **`index.html` 주석은 아직 "보류"라고 적고 있다.** 기준 해상도 구현과 같이 걸려 있어 아래 줄에서 본다
- ~~`DEC-UI-025` 기준 해상도 — `index.html`·`layout.css`·`field.ts` 가 걸려 **최수정과 같이 본다**~~ → **최수정으로 이관**

**8/5 원래 분량**
- ~~승인 CSV 전량 `npm run data:validate` → `npm run data:build` → 클라이언트 바인딩 검증~~ → **최수정으로 이관**
- [ ] 플레이 테스트 라운드 (완주 시도, 막히는 지점·수치 이상 목록화)
  - **이건 옮기지 않는다.** 11-2의 "만든 것을 배선하지 않고 완료로 보고한 4회" 가 넷 다 단위 테스트 통과 중이었고 넷 다 **만든 사람이 아닌 사람이 눌러서** 나왔다. 최수정이 오늘 화면 3종을 새로 만드는데 완주 확인까지 최수정이 하면 그 안전망이 사라진다
- [ ] 21시 이전에 발견 사항 push

**8/5 오후 추가 — 전성민 A1 산출물에서 나온 CSV 작업**

아래는 전부 콘텐츠 데이터라 최수정이 가져갈 수 없다 (`AGENTS.md` 4절 — AI·개발이 `approved` 를 확정하지 않는다).

- [ ] `farm_plots.csv` 좌표 6줄 — 스프라이트 174×157 대 현재 간격 140×120 이라 인접 칸이 가로 34·세로 37px 겹친다. 공통 열이 없는 연결 테이블이라 `content_version` 없음. 좌표만 바꾸고 `npm run data:validate`
- [ ] `night_result_texts.csv` — `DEC-CONTENT-018` 이 8/5 확정(무작위 선택)이라 **행을 늘려도 된다.** 기존 행 `design_intent` 의 "`DEC-CONTENT-018` 보류라" 문장 삭제
- [ ] `content_assets.csv` 신설 — `asset.farm_plot.empty`(`maps.csv` 의 `map.prototype_field` 행에 `farm_plot` 역할) · `asset.crop_seed.common`(네 작물이 전부 이 하나) · `asset.crop_growing.{chili,tomato,glutinous_rice}` · `asset.crop_ready.{chili,tomato,glutinous_rice}`
  - **`asset.background.prototype_field` 도 여기다.** 전성민이 이걸 `enums.json` 고정 목록 쪽에 넣었는데, `content_assets.json` 의 `asset_role` 에 `background` 가 이미 있고 `maps.csv` 가 부모 후보다. `content_asset.id_section_matches_role` 규칙과도 맞는다. 전성민에게 확인 후 넣는다
  - 바나나 자리는 비운다
- [ ] `tutorial_steps.csv` — 스키마는 8/4에 있다 (`schema_version` 5). `step_order`·`stage`(farming/combat/maintenance)·안내 문구. `DEC-UI-015` 와 `DEC-UI-029` 가 튜토리얼 안내를 데이터 쪽으로 확정해 **코드에 둘 수 없다.** 최수정 튜토리얼 작업(10번)이 이걸 기다린다
- [ ] 토란 전환 — ID 를 바꿀지 표시 이름만 바꿀지 결정. 파급 목록은 아트 디렉션 14.5
  - **표시 이름만 바꾸는 쪽을 권한다** (전성민·최수정 같은 의견). 코드는 데이터를 읽으니 ID 를 바꿔도 동작은 같지만, 승인 CSV 여덟 개를 건드리는 것보다 `crops.csv` 한 열이 안전하고 오늘 완주 확인 중에 데이터가 흔들리지 않는다

---

## 5. 아직 답이 없는 것

미확정 항목이다. 정해지기 전까지 임의로 채우지 않는다.

**8/5 오후 기준 결정 로그에 남은 `보류`는 여덟 개뿐이다.** 아래 표를 그 여덟 개로 맞췄다.

| 항목 | 상태 | 개발에 미치는 영향 |
|---|---|---|
| `DEC-RUN-005` 재배 조기 종료 | 보류 | 조기 종료 조건을 코드로 만들지 않는다 |
| `DEC-INPUT-010/011` 대시·연사 | 보류 | 구현하지 않는다 |
| `DEC-FARM-009/011` | 보류 | 11-3의 8/5 오전 QA 조건부 판단 대상 |
| `DEC-RESIDENT-011` 사연 시나리오 선택 | 보류 | 주민마다 승인 사연을 정확히 하나만 쓰는 1차 규칙(`DEC-CONTENT-017`)으로 진행한다. 선택 방식을 코드가 만들지 않는다 |
| `DEC-CONTENT-014/020` | 보류 | 11-3의 8/5 오전 QA 조건부 판단 대상 |
| ~~`DEC-ART-001` 아트 규격~~ | **확정 (8/4)** · 에셋 도착 (8/5) | `assets/final/` 19종. 통합은 4절 8/5 이관분 |
| ~~`DEC-UI-001~028`~~ | **전부 확정** | 8/5 오후 기준 `보류`인 `DEC-UI-*` 는 하나도 없다. `DEC-UI-029` 가 8/5에 신설·확정됐다 |
| ~~`DEC-ENDING-001` 엔딩 콘텐츠~~ | **확정** | — |
| ~~밤 결과 고정 문구~~ | **해결됨 (8/5)** | `DEC-CONTENT-018` 확정 — 승인 행 중 무작위 하나, 화면 열 때 한 번. ~~`night-result.ts` 의 "여러 행이면 데이터 오류" 가드가 이제 규칙과 반대다~~ → **8/5에 고쳤다.** 승인 행 중 무작위 하나를 화면 열 때 한 번 고른다. 행이 늘거나 줄어도 코드를 안 고친다 — 김민주가 행을 늘려도 막히지 않는다 |
| ~~밸런스 수치 DEC 다수~~ | **`CONTENT-014` 만 남음** | `FARM-007/008`, `CRAFT-009/010`, `RESIDENT-035/038/040/044/045/048` 은 전부 확정됐다 |
| ~~플레이어 시작 체력·소지금~~ | **해결됨 (8/2)** | 아래 참조 |

### 5-1. 해결됨 — 플레이어 기본 수치 (`DEC-CONTENT-019`)

"새 런을 시작하면 소지금과 보관함을 초기화한다"(`DEC-RESOURCE-004`)는 확정인데 초기값이
데이터 어디에도 없던 문제는 **`player_base_stats.csv` 신설로 해결됐다** (`schema_version` 2→3).

| 필드 | 쓰는 곳 |
|---|---|
| `max_health` | 런 시작 시 현재 체력 초기화, HUD |
| `starting_money` | 런 시작 시 소지금 초기화 (`DEC-RESOURCE-004`) |
| `move_speed` | 플레이어 이동. `DEC-INPUT-002` 대각선 보정의 기준값 |
| `collision_radius` | 야생동물·주민 충돌·접촉 판정 |
| `sickle_damage` / `sickle_range` / `sickle_cooldown_seconds` | 낫 공격 (`DEC-INPUT-004`) |

**구현할 때 지킬 것**

- 승인 행은 정확히 하나다. 로더에서 그 하나를 읽어 **`src/data/run-config.ts` 한 통로로** 노출하고,
  `resetState()`·HUD·이동·전투는 이 통로만 참조한다. 여러 곳에서 각자 읽지 않는다
- 이 값들은 **런 중 변하지 않는 원본**이다. 현재 체력·위치 같은 런타임 상태를 여기에 섞지 않는다
- 행이 없으면 **오류로 보고**한다. 임시 기본값으로 채워 넘기지 않는다 (`AGENTS.md` 6절)
- 낫 수치가 여기 묶였으므로 `combat.ts`도 이 통로를 쓴다. 무기 데이터와 출처가 다르다는 점에 주의

---

## 6. 우선순위 티어와 컷 트리거

늦어질 때 **무엇을 먼저 버릴지 지금 정해 둔다.** 당일에 정하면 늦다.

**P0 — 이게 없으면 1차 빌드가 아니다**
화면 전환 / 재배·수확 / 정비 거래·제작 / 습격 전투 / 전투 전 대화 판정 / **조우 결과·밤 결과** / 엔딩 판정 / 데이터 로딩 / 배포

**P1 — 제출 품질에 직결**
HUD 완성도, 상점·대화 UI 마감, 아트·폰트 적용, 엔딩 기록문 LLM 실호출, 투항 대화, 보상 지급, 튜토리얼

**P2 — 늦으면 먼저 버린다**
일지 LLM 실호출(→ `journal_fallbacks` 폴백 텍스트만 표시), 오디오,
~~회복 퀵메뉴 롱프레스~~ → **8/5에 P2에서 뺐다 — 만들기로 확정.** `DEC-INPUT-008`·`DEC-UI-001` 이 둘 다 확정이고, 자동 선택만 남기면 회복량이 큰 것부터 강제로 쓰게 된다 (담당자 판단). 엔딩 화면의 일지 회고 스크롤(`DEC-UI-028`)

> **~~영입 주민 지원 공격(`DEC-RESIDENT-021/045`)~~ 은 8/5에 P2에서 뺐다 — 넣기로 확정.**
> 빼려면 `DEC-UI-011`·`DEC-UI-012` 두 확정 DEC를 폐기·대체해야 하는데 **그게 구현보다 비싸다.**
> `DEC-RESIDENT-021`이 경로탐색·체력·충돌·아군 오사를 전부 없앤 형태로 정의해 두었고
> `resident_support_attack_profiles.csv`도 이미 승인돼 있다. `ending.new_family`가 핵심 경로라
> 지원이 없으면 영입의 보상이 엔딩 조건뿐이다. 지금 조우 결과 화면은 "다음 습격부터 지원한다"고
> 표시하면서 실제로는 아무 일도 일어나지 않는다 — **화면이 사실이 아닌 것을 말하고 있다.**
> 8/6 QA까지 안 되면 그때 다시 판단한다 (전성민 8/5 회신).

**컷 트리거**
- **8/4 22:00** — P0에 미완이 남아 있으면 그 자리에서 P2 전체를 뺀다
- **8/5 18:00** — 완주가 한 번도 안 됐으면 아트 미세조정·연출을 멈추고 완주 안정화에만 붙는다
- P2를 뺀 자리는 폴백 경로로 대체하고, 뺐다는 사실을 세션 로그에 남긴다 (2차 복구용)

---

## 7. 충돌 방지와 협업 규칙

### 7-1. 브랜치

```
develop  → 개발은 전부 여기서. Vercel 프리뷰 URL이 붙는다
main     → 1차 빌드 병합 후 "언제 열어도 시연 가능한 상태". Vercel 프로덕션 URL
```

- **브랜치가 충돌을 줄여주지는 않는다.** 두 사람이 develop 하나에 커밋하면 충돌 양상은 main 하나 쓸 때와 같다. 충돌을 막는 건 아래 파일 소유 규칙이다
- 도입 이유는 **main을 안정판으로 고정**하는 것이다. 8/8 촬영 중 누가 develop에 push해도 촬영용 URL이 안 깨진다
- 병합은 `develop → main` 방향만. **`--no-ff`를 쓰고 squash·force push는 금지**한다 (제출 요건: 커밋 기록 유지)
- 병합 시점에 태그를 단다 (`v0.1-build1` 등). 되돌릴 지점이 명확해진다
- 급한 수정도 main에 직접 커밋하지 않는다. develop을 거치지 않으면 다음 병합에서 갈라진다
- 중간 상태가 깨지는 큰 작업만 develop에서 짧은 feature 브랜치를 판다. 하루 안에 되돌린다

**CI 설정을 같이 바꿔야 한다.** 현재 `ci.yml`은 `push: branches: [main]`이라
**develop에 push해도 검증이 돌지 않는다.**

- `push.branches`에 `develop` 추가 → `data:validate` / `docs:check` / `test`가 개발 중에도 돈다
- `progress` job은 main 전용으로 제한 → 개발 중 `PROGRESS.md` 봇 커밋이 생기지 않아 pull 충돌이 준다.
  작업 기록은 main 병합 시점에 한 번에 갱신된다
- `docs/team-rules.md` 3절("main 직접 작업으로 충분")도 같이 고친다

### 7-2. 파일 소유

| | 최수정 | 김민주 |
|---|---|---|
| 전담 | `src/core/`, `src/state/`, `src/scenes/`, `src/input/`, `src/render/`, `src/systems/combat.ts`, `resident-combat.ts`, `encounter.ts`, `wildlife.ts`, `src/llm/`, `api/`, `schema/`, `tools/`, `.github/` | `src/systems/farming.ts`, `economy.ts`, `stage-timer.ts`, `src/data/loader.ts`, `src/ui/` 전체 |
| 공용 | `index.html`, `package.json`, `src/main.ts`, `src/data/types.ts`, `src/data/run-config.ts`, `src/state/types.ts`, `data/approved/` — 건드리기 전에 말한다 | 좌동 |

타입 파일은 **최수정이 8/1 밤에 확정해서 커밋**한다. 이후 변경이 필요하면 상대에게 먼저 말한다.
말없이 타입을 고치면 상대 작업이 통째로 안 켜진다.

> 이 분담은 팀규칙 4절의 기본 담당(김민주 = `data/`)에 개발 영역을 **더한** 것이다.
> **이번 스프린트 한정**이며, 콘텐츠 CSV 작성·승인도 김민주가 계속 맡는다 (8/2 확인).
> 전성민은 기획 문서·결정 로그와 UI/아트 규격을 맡는다.
>
> 한 사람이 콘텐츠와 UI를 함께 지고 있으므로 **승인 CSV가 늦어지면 그 지연이 곧 UI 지연이다.**
> 6절 컷 트리거를 판단할 때 이 점을 같이 본다.

### 7-3. 작업 규칙

- 작업 시작 전 항상 `git pull`. 상대가 밤사이 올린 커밋 위에서 시작해야 한다.
  main에서는 CI 봇 커밋(`PROGRESS.md`)도 생기므로 병합 전후로 특히 주의한다
- 커밋 태그는 팀규칙 2절: `[Feat] [Fix] [Data] [Docs] [Art] [Refactor] [Test] [Chore]`
- 22:00 바통 터치 — 김민주가 push하고 한 줄로 "어디까지 됐고 뭐가 안 됨"을 알린다. 최수정은 pull부터
- 리소스를 추가하는 커밋에서 `docs/submission/CREDITS.md`를 같이 갱신한다 (몰아서 하면 프롬프트를 기억 못 한다)
- `.env`, `generated/runtime/*.json`은 커밋하지 않는다
- 규칙이 없거나 모순되면 임의로 CSV 열을 늘리거나 코드에 예외를 만들지 말고 멈추고 보고한다 (`AGENTS.md` 5절)
- 작업 단락이 끝나면 `docs/progress/sessions/<본인>.md`에 **왜 그렇게 했는지**를 남긴다

---

## 8. 8/6 이후 (범위 밖, 자리만 잡아 둠)

| 날짜 | 내용 |
|---|---|
| 8/6 (목) | 버그 수정, 밸런스 수치 조정(CSV만 고치고 재빌드), 아트 교체, **영입 주민 지원 공격**(6절에서 P2 해제 · `DEC-RESIDENT-021/045`), 빈 발사음 QA 판단(P2 유지) |
| 8/7 (금) | 제출 문서 — 「AI 활용 기술 문서」, 「팀원 롤 기술서」, `CREDITS.md` 최종 |
| 8/8 (토) | 시연 영상 촬영 |
| 8/9 (일) ~ 8/10 (월) | 예비일, 제출 |

8/6 이후 수정도 develop에서 하고, 검증한 뒤 main으로 병합한다.
촬영 직전에는 main을 건드리지 않는다 (7-1절).

「팀원 롤 기술서」의 근거가 Git 이력이므로, 각자 자기 영역을 자기 계정으로 커밋하는 것이
이 기간 내내 유효하다. 한 사람이 몰아서 커밋하면 나중에 복원이 안 된다.

---

## 9. 구현 세부 보완 사항

일자별 작업에 다 적기 어려운, 빠뜨리기 쉬운 구현 지점이다. 해당 작업을 할 때 이 항목을 같이 본다.

### 9-1. 카메라와 좌표 변환 (`src/render/camera.ts`) — 8/1 밤 ~ 8/2, 최수정

**고정 카메라다** (`DEC-CONTENT-016`). 전체 밭이 한 화면에 보이는 단일 직사각형 맵이고
스크롤·확대·축소·회전을 쓰지 않는다. `DEC-UI-004`가 "화면 밖 작물을 위한 알림과 미니맵을
사용하지 않는다"고 확정한 것도 같은 전제 위에 있다.

> 이 절은 8/1 시점에 "뷰포트보다 클 수 있다 / 플레이어 추적 카메라"로 적혀 있었고
> 구현이 그대로 따라갔다. 어긋난 것은 로드맵과 확정 DEC 쪽이라 8/3에 DEC에 맞춰 고쳤다.
> 스크롤 카메라가 필요하다고 판단되면 `DEC-CONTENT-016`을 폐기하고 대체 DEC를 쓰는
> 절차로 간다 — 로드맵을 고쳐서 우회하지 않는다.

월드 → 스크린 변환을 렌더 진입점에 둔다.

- 월드 단위 ↔ 화면 픽셀 배율은 아트 규격(`DEC-ART-001`, 보류)이 오기 전까지 이 파일의
  임시 상수 하나로 둔다 (2절). 규격이 오면 이 한 줄만 바꾼다.
  현재 `WORLD_TO_PIXEL = 1` — 승인 맵이 1600×900이고 데이터의 거리 수치(경작지 간격,
  `attack_range`)가 1월드단위 = 1픽셀 전제로 작성돼 있다
- 뷰포트가 월드보다 크면 남는 쪽은 월드를 가운데에 둔다. 이 규칙 덕분에 배율만 맞으면
  추적 카메라가 사실상 고정 카메라로 동작한다
- 같은 좌표계를 쓰는 것: `farm_plots`의 `x/y`, `farm_interaction_radius`,
  `wildlife_spawn_edge_margin`, `wildlife_spawn_min_player_distance`, `attack_range`, `projectile_max_range`
- **변환 함수는 한 곳에만 둔다.** 시스템마다 따로 계산하면 상호작용 판정과 렌더 위치가 어긋난다

### 9-2. 대화 중 입력·진행 정지 (`DEC-INPUT-009`, `DEC-RESIDENT-039`) — 8/3~8/4

대화 모달이 열리면 `src/input/`의 입력 잠금을 켜고, **필드 시뮬레이션 자체를 정지**한다.

정지 대상을 정확히 하면 다음과 같다.

- 플레이어 이동·조준·공격·상호작용 입력
- 주민 AI 추적과 공격, 비행 중인 투사체, 상태이상 지속 틱
- 진행 중인 회복 — 회복은 대화 전환 시 **취소**된다(소비 없음). 정지 후 재개가 아니다
- 재배 제한시간 타이머는 재배 단계 전용이라 대화 시점에는 이미 멈춰 있다. 정비 단계에는 시간제한이 없다

투항 대화는 전투 중간에 끼어들므로 **정지 → 선택 → 재개** 경로가 실제로 필요하다 (`DEC-RESIDENT-039`).
정지 시점의 적 위치·체력·상태이상이 그대로 복원돼야 한다.

`DEC-UI-014`가 확정한 것 두 가지를 같이 지킨다.

- 전투 전 대화와 투항 대화는 **필수 화면이라 `Esc`로 닫을 수 없다**
- 어떤 화면에서도 `Esc`는 일시정지를 연다. 필수 대화 중이라면 **대화는 그대로 둔 채 일시정지만** 겹친다

> 중첩 UI의 표시 우선순위(`DEC-UI-026`)는 여전히 보류다. 위 두 줄 외에 잠금 스택을 임의로
> 설계하지 말고 "대화 열림 = 필드 정지" 규칙만 구현한다.

### 9-3. LLM 실패 처리 — 환경 분기를 만들지 않는다 (`DEC-JOURNAL-003`, `DEC-CONTENT-011`) — 8/4

> 8/1 팀 합의: 로컬 전용 모킹 분기 대신 아래 방식으로 간다.

로컬에서 `/api/*`가 없어도 게임이 멈추면 안 된다는 요구는 그대로 만족시키되, **환경 분기로 풀지 않는다.**

- 확정 규칙은 "실패 시 같은 입력으로 **한 번 재시도**하고, 그래도 실패하면 폴백"이다.
  로컬만 재시도를 건너뛰면 프로덕션에서 처음 도는 경로가 생긴다
- `.env.example`이 이미 답을 적어 두었다 — 키가 비어 있으면 "사용할 수 없는 LLM 환경"으로 처리되어
  폴백을 쓰는 것이 **기획서상 정상 경로**다. 로컬 404도 같은 실패로 취급하면 끝이고 별도 분기가 필요 없다
- 즉 `src/llm/client.ts`는 환경을 구분하지 않는다. 호출 실패(네트워크·404·5xx·타임아웃·형식 위반)를
  전부 같은 실패로 보고 → 1회 재시도 → 폴백. 그러면 로컬에서도 자동으로 폴백이 뜨고 재시도 경로도 같이 검증된다
- 폴백 출처: 일지는 `journal_fallbacks`(공포도 구간 × 변화 방향), 엔딩은 `endings.fallback_record_text`.
  둘 다 승인 데이터에서 오며 코드에 문장을 두지 않는다
- 로컬에서 실제 LLM 경로까지 확인하려면 `vercel dev`로 실행한다. 코드에 넣을 문제가 아니라 실행 방법 문제다

### 9-4. 일지 프롬프트와 매니페스트 (`DEC-PIPELINE-012`, `DEC-JOURNAL-004`) — 8/4

`schema/journal_prompt_system.md`를 만들 때 `schema_manifest.json`도 같이 손댄다. 세 곳이다.

- `"journal_prompt_version": 1` 추가
- `version_change_reasons`에 언제 올리는지 한 줄 추가 (엔딩 프롬프트 항목과 같은 형식)
- `history`에 이번 변경 기록 추가

현재 저장소에는 `ending_prompt_system.md`도 없다. 두 파일 모두 8/4에 새로 만든다.

### 9-5. 런 리셋 (`DEC-RESOURCE-004`, `DEC-RESIDENT-047`) — 8/5

새 런 시작과 런 실패 후 타이틀 복귀에서 전역 상태를 원자적으로 되돌린다.

리셋 대상: 소지금 / 보관함 4종(수확물·재료·무기·소모품) / 투척 퀵슬롯 편성 / 회복 파우치 /
작물 숙련도 / 해금된 레시피 / 주민 런 상태와 관계 / 공포도 / 경작지 상태 / 현재 일차·단계 /
주민별 선택된 사연 시나리오 / 생성된 일지 기록

주의할 점:

- **초기값은 데이터에서 읽는다.** 시작 체력·소지금이 아직 미정이다 (5-1절). 정해지기 전에는 코드에 숫자를 넣지 않는다
- 튜토리얼 자원은 본 런으로 넘어오지 않는다 (`DEC-RESOURCE-004`). 튜토리얼은 별도 상태로 돌린다
- 부분 리셋이 남으면 두 번째 런에서만 재현되는 버그가 된다. 상태 객체를 새로 만들어 통째로 교체하는 편이 안전하다
- 엔딩 수집 같은 런 밖 영구 데이터는 이번 범위가 아니다 (`DEC-RESOURCE-004` 마지막 줄)

### 9-6. 자원 변경의 원자성 — 8/3~8/4

"검증 → 차감 → 지급"을 분리하지 않고 사전 검증 후 한 번에 반영한다. 적용 대상과 근거는 다음과 같다.

| 처리 | 근거 |
|---|---|
| 상점 판매·구매 | `DEC-RESOURCE-011` — 조건 미달이면 소지금과 보관함을 **모두** 변경하지 않는다 |
| 거래 확정 | `DEC-RESOURCE-013` — 버튼을 누르는 순간 현재 소지금·보유 수량을 **다시 검증**한다 |
| 제작 | `DEC-CRAFT-004` — 여러 개 제작 시 부분 성공을 만들지 않는다 |
| 자원 협상 | `DEC-RESIDENT-033` — 수확물 차감과 관계 변경을 함께 반영한다. 거절이면 아무것도 소비하지 않는다 |
| 퇴각·처치 보상 | `DEC-RESIDENT-042` — 지급과 주민 상태 변경을 함께, 중복 지급 방지 |

구현 형태는 "현재 상태 사본에 전부 적용해 보고, 전부 성공했을 때만 실제 상태로 교체"가 가장 단순하다.
`DEC-RESOURCE-002`(수량의 단일 원본)와도 맞는다 — 퀵슬롯·파우치는 수량을 복사하지 않으므로
보관함 하나만 원자적으로 바꾸면 된다.

---

## 10. 매일 쓰는 프롬프트

`AGENTS.md`와 결정 로그는 AI가 알아서 읽는다. **여기에 규칙을 다시 적지 않는다.**
아래 절차를 문서에 두었으므로 매일 칠 것은 한 줄이면 된다.

### 10-1. 시작

```
2026-08-03, 최수정. 개발 로드맵 10-1대로 시작하자.
```

이 한 줄을 받으면 AI는 다음을 순서대로 한다.

1. `git pull`, 현재 브랜치가 `develop`인지 확인
2. 이 문서 4절에서 **그 날짜의 해당 작업자 항목**을 읽는다
3. `git log`로 마지막 세션 이후 상대가 올린 커밋을 확인한다
4. `data/approved/`와 `generated/runtime/` 상태를 확인한다 — 오늘 작업의 선행 데이터가 있는가
5. `docs/progress/sessions/` 최근 기록에서 **하다 만 것**을 확인한다
6. 오늘 작업에 걸린 DEC의 상태를 확인한다

그리고 **작업을 시작하기 전에** 다음 세 가지를 보고하고 확인을 받는다.

- 오늘 할 일 (4절 기준)
- 지난 세션 이후 달라진 것
- **결정되지 않아 막히는 것** — 있으면 임의로 채우지 않고 먼저 묻는다

마지막 항목이 핵심이다. 이게 없으면 AI가 보류 DEC를 자기 판단으로 채우고 시작한다.

오프라인에서 정해진 것(구두 합의, 기획팀 회신 등)은 AI가 알 수 없으므로 그것만 한 줄 덧붙인다.
예: `player_base_stats.csv 승인 행 올라갔어.`

### 10-2. 마무리

```
오늘 작업 마무리. 로드맵 10-2대로.
```

AI가 하는 일.

1. 변경된 범위에 해당하는 검증 실행 (`data:validate` / `docs:check` / `test`)
2. 실행하지 못한 검증과 남은 위험 보고
3. `docs/progress/sessions/<본인>.md`에 세션 기록 — 커밋에 남는 "무엇을"이 아니라
   **왜 그렇게 했는지, 접은 대안, 결정로그에 답이 없어 막힌 것**
4. 4절 완료 조건 대비 어디까지 됐는지, 못 맞춘 채 넘어간 것이 있는지 명시
5. **11절 점검** — 오늘 작업에서 계획에 없던 필요 작업이 드러났으면 11-2 표에 추가하고,
   완주 경로(11-1)에서 오늘 상태가 바뀐 줄을 갱신한다
6. 커밋 메시지 초안 제안 (팀규칙 2절 태그)

3번이 8/7 「AI 활용 기술 문서」와 「팀원 롤 기술서」의 재료다. 그날 안 쓰면 대부분 잃어버린다.
4번을 빠뜨리면 다음 작업이 안 된 것을 전제로 깔고 쌓인다.
5번을 빠뜨리면 마지막 날에 한꺼번에 드러난다.

### 10-3. 막혔을 때

```
결정로그에 답이 없는 것 같다. AGENTS.md 5.1 형식으로 정리해줘. 임의로 채우지 말고.
```

CSV에 열을 추가하거나 코드에 예외를 만들어 우회하는 것이 가장 흔한 사고다.
막혔다고 느끼면 진행시키지 말고 이 한 줄을 쓴다.

---

## 11. 누락 점검

4절은 **하기로 한 일**의 목록이라, 애초에 아무도 적지 않은 작업은 잡아내지 못한다.
마지막 날에 터지는 것은 대개 미룬 작업이 아니라 **적어두지 않은 작업**이다.
아래 두 표를 매일 갱신해서 그 격차를 드러낸다.

### 11-1. 완주 경로 체크리스트

`DEC-RUN-014` 흐름과 `DEC-UI-014` 화면 목록을 합쳐 편 것이다.
**여기 있는 줄이 전부 `됨`이 되어야 1차 빌드다.**
시스템이 아무리 많이 만들어져도 이 표에 `없음`이 남아 있으면 완주가 안 된다.

상태 표기: `없음` / `뼈대` (전환은 되나 내용 없음) / `됨` / `막힘` (선행 조건 대기)

| # | 화면·단계 | 층위 | 담당 | 계획 | 상태 |
|---|---|---|---|---|---|
| 1 | 타이틀 | 독립 | 최수정 | 8/5 | **됨** (제목 + 시작 하나. 부팅이 더는 흐름을 밀지 않는다) |
| 2 | 이름 입력 | 독립 | 최수정 | 8/5 | **됨** (키보드 입력·빈 이름 진행 불가·최대 12자·줄바꿈 불가. `playerName` 이 실제로 런에 들어간다) |
| 3 | 튜토리얼 | 독립 | 최수정 | 8/5 | 뼈대 (화면과 건너뛰기는 됨 · **안내 문구는 `tutorial_steps.csv` 대기** — `DEC-UI-015` 가 코드에 두는 것을 금지) |
| 4 | 런 시작 — 데이터 로드·상태 초기화 | — | 김민주/최수정 | 8/2 | **됨** (이름 확정 시 `startNewRun()` 이 런 상태를 통째로 교체 — 로드맵 9-5) |
| 5 | 일차 시작 연출 + 습격 예고 | 독립 | 최수정 | 8/5 | **됨** (2일차 아침 플레이 확인. 예고는 `opening_text` 문장 · HUD 짧은 표지는 9번 몫) |
| 6 | 일차 시작 — 일지 표시 | 독립 | 최수정 | 8/5 | **됨** (폴백·대기 표시 플레이 확인. **LLM 성공 경로는 실제 키로 미확인**) |
| 7 | 필드 — 재배 모드 진입·이탈 | 베이스 | 최수정 | 8/1~2 | 뼈대 |
| 8 | 재배 — 파종·성장·수확 | 베이스 | 김민주 | 8/2 | **됨** |
| 9 | 재배 — 야생동물 | 베이스 | 최수정 | 8/2~3 | **됨** (출현·목표·먹기·예고 플레이 확인) |
| 10 | 재배 — 전투(낫·투척·효과) | 베이스 | 최수정 | 8/2~3 | **됨** (낫·투척·퀵슬롯·소진 자동 전환 플레이 확인. `area` 다중 명중은 미확인) |
| 11 | 재배 제한시간 종료 → 셔터 내려옴 | 전환 | 김민주 | 8/2 | **됨** |
| 12 | 정비 허브 (셔터 + 패널) | 오버레이 | 김민주 | 8/3 | **됨** (`maintenance-hub.ts` · 보관함 상시 영역 + 기능 버튼 4개 · 8/5 배선 확인. 습격 예고 표시는 31번과 같이 비어 있다) |
| 13 | 정비 — 상점 판매·구매 | 오버레이 | 김민주 | 8/3 | **됨** (`shop-modal.ts` · 소지금·보관함 즉시 갱신 플레이 확인) |
| 14 | 정비 — 제작·숙련도 해금 | 오버레이 | 김민주 | 8/3 | **됨** (`craft-modal.ts` · 플레이 확인. 숙련도 해금은 미확인) |
| 15 | 정비 — 보관함·투척 퀵슬롯 편성 | 오버레이 | 김민주 | 8/3 | **됨** (`quickslot-modal.ts` · 편성 후 투척까지 플레이 확인) |
| 16 | 정비 종료 분기 — `밭을 정찰하러 간다` / `아침까지 잔다` | 전환 | 최수정 | 8/4 | 뼈대 |
| 17 | 필드 — 습격 모드 (셔터 올라감) | 베이스 | 최수정 | 8/3 | 뼈대 |
| 18 | 전투 전 대화 — UI | 오버레이 | 김민주 | 8/4 | **됨** (`dialogue-modal.ts` · 시작 대사·선택지 3개·반응 대사 플레이 확인) |
| 19 | 전투 전 대화 — 판정·자원 협상 | — | 최수정 | 8/3 | **됨** (판정·위협·협상 거절 배선 플레이 확인. UI는 18번) |
| 20 | 습격 전투 — 주민 AI | 베이스 | 최수정 | 8/3 | **됨** (추적·공격·전투 보정 플레이 확인. 원거리 주민도 3일차 만복으로 확인 — 투사체가 안 그려지던 것을 8/5에 고쳤다) |
| 21 | 투항 대화 — 정지·선택·습격 모드 복귀 | 오버레이 | 최수정/김민주 | 8/3~4 | **됨** (발동·정지·3선택지 결과는 8/4 · 선택지 UI 는 `dialogue-modal.ts` 로 8/5 플레이 확인) |
| 22 | 보상 지급·주민 상태 변경 | — | 최수정 | 8/4 | **됨** (퇴각·처치 보상 원자적 지급, 관계 5상태, 재등장 방지 플레이 확인) |
| 23 | **조우 결과** 화면 (습격일) | 독립 | 김민주 | 8/4 | **됨** (`DEC-UI-011`·`013` 전 항목 · 결과 확정 시점 스냅샷 · 플레이 확인) |
| 24 | **밤 결과** 화면 (비습격일, 고정 문구) | 독립 | 김민주 | 8/4 | **됨** (승인 고정 문구만 · 플레이 확인) |
| 25 | 결과 → 다음 일차 시작 | 전환 | 최수정 | 8/4 | 뼈대 |
| 26 | 엔딩 판정 (공포도·대표 작물) | — | 최수정 | 8/4 | **됨** (5일차 마지막 습격에서 판정 플레이 확인. `fear_increments.csv` 승인으로 공포도가 실제 누적된다 — **구간별 분기는 아직 플레이로 안 봤다**) |
| 27 | 엔딩 화면 + 기록문 LLM·폴백 | 독립 | 최수정 | 8/4 | **됨** (8/5 밤 5일차 이장 영입 → 엔딩 플레이 확인. 기록문은 폴백 — 실제 키로는 미확인) |
| 28 | 런 실패 화면 (엔딩과 분리) | 독립 | 최수정 | 8/5 | **됨** (2일차 체력 0 → 화면 → `타이틀로` 까지 플레이 확인) |
| 29 | 일시정지 (`Esc`, 어느 화면에서든) | 오버레이 | 최수정 | 8/1~5 | 뼈대 |
| 30 | 데이터 오류 화면 (`DEC-UI-024`) | 독립 | 김민주 | 8/2 | 뼈대 |
| 31 | HUD (재배·습격 공통) | 오버레이 | 김민주 | 8/3 | 뼈대 (습격 예고 자리 비어 있음 — **CSV 는 8/4 승인됐고 `main.ts` 배선이 안 됐다.** 최수정 8/5) |

23·24는 **하루에 하나만** 뜬다 (`DEC-RUN-015`). 둘 다 없으면 하루가 안 끝나고,
둘 다 뜨면 결과 화면이 두 번 나온다 — 폐기된 `DEC-RUN-012`에서 문제가 됐던 지점이다.

이 표가 6절 컷 트리거의 판단 근거다. **8/4 22:00에 `없음`이 몇 줄인지로 P2를 뺄지 정한다.**

### 11-2. 계획에 없었는데 필요한 것으로 드러난 것

작업하다 나온 것을 그때그때 여기 적는다. 기억해 두었다가 나중에 적으면 대부분 잃어버린다.

| 발견일 | 항목 | 왜 필요한가 | 담당 | 언제 | 티어 |
|---|---|---|---|---|---|
| 8/2 | LLM 모델 ID를 어디에 둘지 | `DEC-PIPELINE-012`가 "생성 모델 변경은 프롬프트 버전 사유가 아니고 `generator_model_id`로 구분"이라고 정해 모델은 교체 가능해야 한다. 코드에 박으면 그 취지가 깨진다. `ANTHROPIC_MODEL` 환경변수 안이 유력 | 최수정 | 8/4 | P1 |
| 8/2 | 승인 데이터 도착 시 임시 통로 제거 | `usePlaceholderStats()`·`enterFieldPreview()`·`devSkipToFarming()`에 8/3 밤 `__dev.fillThrowables()`·`__dev.startRaid()`가 더해져 **다섯 개**다. 전부 콘솔 경고를 남기지만 안 지우면 임시 경로로 계속 돈다. 뒤의 둘은 김민주의 제작·편성·대화 모달이 들어오는 즉시 지운다 | 최수정 | 각 선행 조건 도착 즉시 | P0 |
| 8/4 | **확정된 DEC를 "보류"라고 적은 주석이 코드에 남는다** | `DEC-UI-022`·`025`·`026`이 8/3에 확정됐는데 코드 주석 5곳이 여전히 "보류라 구현하지 않는다"였다. `DEC-UI-026`은 그래서 **확정 하루 뒤까지 미구현**이었다(일시정지 최상위·기능 오버레이 동시 개수·입력 소유권·포커스 이탈 시 퀵메뉴 정리). 주석은 고쳤고 구현도 넣었다. **DEC가 확정될 때 코드의 보류 주석을 같이 훑는 절차가 없다** | 최수정 | 8/3 **완료** · 절차는 8/4 | P0 |
| 8/4 | `DEC-UI-025` 기준 해상도 미구현 | 기준 해상도 1920×1080과 "비율 유지 확대·축소 + 남는 영역 여백"이 확정됐는데 캔버스가 뷰포트를 그냥 채운다. 창 비율이 16:9가 아니면 필드가 늘어난다. `index.html`·`field.ts`·`layout.css`가 함께 걸려 **김민주와 같이 봐야 한다** | 최수정+김민주 | 8/4 | P1 |
| 8/4 | 낫 재사용 대기의 화면 표시 규칙이 없다 | `DEC-UI-017`·`018`·`019` 어디에도 없다. 투척은 퀵슬롯 칸이 있는데 낫은 자리가 없다. 지금은 개발 빌드 전용 표시라 제출 빌드에서는 플레이어가 대기 중인지 알 수 없다. 0.5초라 의도적으로 뺐을 수도 있다 | 전성민 | 8/4 | P2 |
| 8/4 | **만든 것을 배선하지 않고 "완료"로 보고하는 일이 네 번 반복됐다** | 낫(쿨타임 없음)·퀵슬롯(무기 변경 안 됨)·결과 이벤트(듣는 쪽 없음)·습격 중 낫(대상 선택이 두 군데). **넷 다 단위 테스트는 통과 중이었고 넷 다 최수정이 실행해서 찾았다.** 앞으로 시스템을 만들면 ① 배선했는가 ② 결과가 화면이나 콘솔에 나오는가 둘을 확인하고 보고한다 | 최수정 | 8/4~ | P0 |
| 8/4 | 플레이로 아직 확인 못 한 것 2건 | ① `area` 무기의 다중 명중 — 주민이 한 명뿐이라 차이가 안 보인다. 재배에서 야생동물이 몰렸을 때 확인 ~~② `ranged_chase` 주민 — 원거리 주민이 배치된 일차를 아직 안 봤다~~ → **8/5 확인. 그리고 거기서 투사체 미렌더 버그가 나왔다** (아래 행). (소진 자동 전환은 8/4 확인 완료) | 최수정 | 8/4 | P1 |
| 8/2 | `schema/enums.json`의 `kind`에 `player_base_stats` 누락 | `DEC-CONTENT-019`로 신설된 테이블의 `kind`가 고정 허용 목록에 없다. 검증기는 테이블 정의의 `kind`를 보므로 지금은 안 막히지만, enums가 "고정 허용 목록"이라고 선언해 놓고 실제와 다르다 | 최수정 | 8/2 **완료** | P2 |
| 8/3 | `raid_notices`·`night_result_texts` 스키마 신설 | `DEC-CONTENT-021`이 구조를 확정했는데 `schema/tables/`에 정의가 없어 김민주가 HUD의 습격 예고 자리를 비워 둔 채 진행했다. `DEC-RUN-011`이 문구를 코드에 두는 것을 금지하므로 우회할 방법이 없었다 | 최수정 | 8/3 **완료** | P0 |
| 8/3 | ~~보관함이 둘이다~~ **철회** | 최수정이 pull 직전 상태를 보고 P0으로 올렸으나, 김민주가 같은 날 `main.ts`에서 `run.resources.crops`로 이어 두었다. `farming.harvested`는 재배 시스템 안의 집계이고 판매·제작·협상이 보는 원본은 런 상태라는 두 층 구조이며 주석도 있다 | — | — | — |
| 8/3 | 재배 이벤트가 버스에 안 붙어 있다 | `farm.planted`·`farm.harvested`가 `events.ts`에 선언만 돼 있고 `main.ts`가 직접 상태를 고친다. 지금은 배선이 짧아 문제가 없지만 UI가 붙을 자리가 비어 있다 | 최수정 | 8/4 | P2 |
| 8/3 | 경작지 단계 키 불일치 (`harvestable` ↔ `ready`) | 계약(`state/types.ts`)과 구현(`systems/farming.ts`)이 갈려서 계약 쪽 타입을 아무도 쓰지 않았다. 타입이 있는데 안 쓰이면 다음 사람이 그것을 믿고 짠다 | 최수정 | 8/3 **완료** | P1 |
| 8/3 | `docs:check`가 스키마와 기획 문서 사이에서 교착한다 | 새 테이블을 만들면 "스키마에 있는데 준비도 문서에 없다"로 주의가 뜨고, 문서를 먼저 고치면 반대 방향으로 뜬다. 이번엔 두 줄을 넣어 풀었지만 **다음 테이블 신설 때 또 난다.** 실제 원인은 `tools/check-docs.mjs`가 차단만 exit 1로 구분하는데 `tests/check-docs.test.mjs`는 `문제 없음`을 요구해 주의까지 똑같이 취급하는 것이다 | 전성민 판단 후 최수정 | 8/4 | P1 |
| 8/3 | 튜토리얼 안내 문구 테이블이 없다 | `DEC-UI-015`가 "`DEC-CONTENT-021`이 정한 화면 고정 문구와 같은 방식으로 콘텐츠 데이터에서 공급한다"로 확정했는데 `DEC-CONTENT-021`에는 튜토리얼 테이블의 열·`kind`·ID 형식이 없다. `raid_notices`와 같은 상황이고 구조 결정이라 개발이 채울 수 없다. 튜토리얼은 8/5라 아직 막히진 않는다 | 전성민 | 8/4 | P1 |
| 8/3 | `#ui > *`가 캔버스 클릭을 가로챘다 | 8/2에 최수정이 심었고 그날은 `#ui`가 비어 있어 증상이 없었다. 8/3에 김민주의 `.hud`(`inset: 0`)가 들어오자 좌·우클릭이 죽었다. 명시도 (1,0,0) 대 (0,1,0). 자식 전체를 되살리는 규칙을 지우고 각 요소가 선언하는 방식으로 바꿨다 | 최수정 | 8/3 **완료** | P0 |
| 8/3 | **만든 시스템을 그날 `main.ts`에 붙이지 않았다** | 4절 어느 날에도 "필드에 붙이기"가 항목으로 없고 8/5 오전 통합에 몰려 있었다. 그래서 `combat.ts`가 있는데도 `onSickle`이 `console.info` 한 줄이라 낫 쿨타임이 안 돌았고, **플레이 테스트로만 발견됐다.** 단위 테스트 51개는 통과 중이었다. 재배 모드는 8/3 밤에 붙였고 습격 모드는 8/4다. **8/4 이후로는 만든 날 붙인다** | 최수정 | 8/3 재배 **완료** · 습격 8/4 | P0 |
| 8/4 | ~~낫 재사용 대기 표시 규칙이 없다~~ **닫힘** | 전성민 회신 — `DEC-UI-017` 의 닫힌 목록에 없으므로 표시하지 않는 것이 이미 확정 상태다. 0.5초라 게이지를 띄우면 전투 내내 깜빡인다. 개발 빌드 전용 표시는 유지 | — | 8/4 **완료** | — |
| 8/5 | **개발 통로가 흐름과 런 상태를 갈라놓는다** | `enterFieldPreview()` 가 상태기계를 우회해 `scenes.step().at` 과 실제 화면이 어긋나고, `__dev.goToDay()` 는 `run.dayNumber` 만 바꿔 흐름의 일차와 갈린다. 그래서 조우가 끝나도 `encounter_finished` 를 보낼 수 없다. 정상 흐름에서는 안 나지만 **개발 통로로 관찰한 결과를 믿을 수 없게 만든다** | 최수정 | 김민주 UI 도착 시 통로와 함께 제거 | P1 |
| 8/5 | ~~`api/` 두 개가 nodejs 런타임이다~~ **해결** | edge 로 되돌리고 프롬프트를 빌드 시점에 구워 넣었다. 배포 확인 완료 — 200 응답, 키 없으면 폴백 | 최수정 | 8/5 **완료** | — |
| 8/5 | **`api/` 는 edge 제약이 있다 — 다음 사람이 또 밟는다** | 배포에서 네 번 걸렸다. ① `node:*` 내장 모듈을 쓰는 의존성을 넣으면 안 된다(그래서 `@anthropic-ai/sdk` 대신 `fetch`) ② import 에 `.ts` 확장자를 붙이면 안 된다(`src/` 는 Vite 가 처리해서 되는데 `api/` 는 다른 번들러다) ③ 파일 시스템이 없다. **로컬에서 확인 가능한 조건이므로 `api/` 를 건드리기 전에 이 셋을 먼저 본다** | 최수정 | 기록 완료 | P2 |
| 8/5 | LLM 키를 누구 것으로 할지 정해지지 않았다 | 키가 없으면 폴백이 정상 경로라 게임은 완주된다. 다만 8/8 촬영에서 기록문 생성을 보여주려면 Vercel Production 에 실제 키가 있어야 한다. 개인 키·팀 공용 키·폴백 시연 중 팀이 정할 문제 | 팀 | 8/5 | P1 |
| 8/3 | `economy.ts`도 붙어 있지 않다 | 부를 UI(상점·제작 모달)가 없어서다. 위와 달리 의도된 순서이고 김민주 8/4 모달과 같이 붙는다 | 김민주 | 8/4 | P1 |
| 8/5 | **`run.dayNumber` 가 1에 멈춰 있었다** | 흐름(`scenes/flow.ts`)은 일차를 옮기는데 런 상태를 잇는 곳이 아무 데도 없었다. 그래서 2일차 정비를 끝내면 흐름은 습격으로 가려는데 정비 허브는 1일차 기준으로 `아침까지 잔다` 를 보내 **흐름이 데이터 오류로 떨어진다.** 화면에는 "정비를 끝냈는데 오류가 났다" 로만 보인다. HUD 일차·야생동물 출현 프로필·적대 주민 배치·엔딩 `finalDay` 가 전부 이 값을 읽는다. `main.ts` 에 `syncRunDay()` 를 넣어 흐름을 단일 원본으로 삼았다. **김민주가 "반대 설계(런 상태가 원본)도 가능한데 그건 제가 정할 게 아니었습니다" 로 최수정 확인을 요청했고 아직 답하지 않았다** — 실행 순서 3번에서 `types.ts` 확인과 같이 본다 | 김민주 | 8/5 **완료** · 설계 확인 미답 | P0 |
| 8/5 | `src/data/types.ts` 에 `raid_notices`·`night_result_texts` 타입이 없었다 | 스키마와 `schema/enums.json` 에는 8/3에 들어갔고 런타임 JSON 도 생성되는데 `Kind` 와 `RuntimeData` 에만 빠져 있어 UI 가 승인 문구를 타입으로 읽을 수 없었다. 밤 결과 화면 때문에 추가했다. **공용 파일이라 최수정 확인이 필요하다** — 추가만 했고 기존 정의는 건드리지 않았다. `tutorial_step` 은 아직 빠져 있다 (최수정 8/5 튜토리얼) | 김민주 | 8/5 **완료** | P1 |
| 8/5 | 조우 결과가 "다음 습격부터 지원한다"고 말하는데 **지원이 구현돼 있지 않다** | `DEC-UI-011` 이 영입 주민에게 그 사실을 표시하라고 확정해서 화면은 그렇게 그린다. 그런데 영입 주민 지원 공격(`DEC-RESIDENT-021`·`DEC-UI-012`)은 6절 P2 라 아직 없다. 지금 영입하면 화면이 사실이 아닌 것을 말한다. 화면을 고칠 문제가 아니라 P2 를 뺄지 넣을지의 문제다 · **8/5 결정 — 넣는다.** 빼려면 `DEC-UI-011`·`DEC-UI-012` 를 폐기·대체해야 하는데 그게 구현보다 비싸고, `DEC-RESIDENT-021` 이 경로탐색·체력·충돌·아군 오사를 없앤 형태로 이미 정의돼 있으며 프로필 CSV 도 승인돼 있다. 6절 P2 에서 뺐다 | 최수정 | 8/6 | P1 |
| 8/5 | **재배 제한시간이 2일차부터 다시 시작하지 않았다** | `stage-timer.ts` 의 `reset()` 을 8/3에 만들어 두고 부르는 곳을 안 붙였다. 2일차 재배는 남은 시간 0 · 만료 통지 소진 상태로 시작해 `tick()` 이 영영 true 를 안 돌려주고 **재배가 끝나지 않는다.** 타이머 테스트 6개는 계속 통과 중이었다 — 타이머는 맞고 부르는 곳만 없었다. **1일차만 눌러 보면 안 보인다.** `field.entered` 에서 부르게 고쳤다. 밭(경작지 상태·남은 성장 시간)이 다음 날로 넘어가는 것은 `DEC-FARM-003` 확정이라 버그가 아니다 | 김민주 | 8/5 **완료** | P0 |
| 8/5 | 결과 화면 다음이 **일차 시작 화면이라 흐름이 거기서 멈춘다** | 밤 결과·조우 결과에서 `확인` 을 누르면 `day_start` 로 가는데 그릴 화면이 없다(11-1 의 5·6번). 결과 화면 2종이 들어와도 **완주는 여기서 끊긴다.** 4절 8/5 최수정 항목에 이미 있지만, 이제 이게 완주 경로의 마지막 구멍이라는 점이 드러났다 | 최수정 | 8/5 | P0 |
| 8/5 | **승인된 CSV 를 코드가 아직 안 읽는다 — `raid_notices`** | `raid_notices.csv` 는 8/4 에 승인됐는데 `main.ts` 의 `hudView()`·`hubView()` 가 `raidNoticeLabel: null` 을 그대로 넘긴다. 주석도 "승인되면 여기에 들어간다" 인 채라 **다음 사람이 읽으면 아직 CSV 가 없는 줄 안다.** 4절 8/4 김민주 항목에는 "자리가 채워졌다" 로 적혀 있었다 — 데이터 승인과 배선을 한 줄에 적으면 이렇게 갈린다. **앞으로 CSV 승인과 그것을 읽는 배선은 항목을 나눠 적는다** | 최수정 | 8/5 | P1 |
| 8/5 | `journal_fallbacks` 의 `down` 3행은 **알고 남긴다** | `DEC-JOURNAL-003` 의 `covers_all_bands_and_directions` 가 하강 방향을 요구하는데 `DEC-RESIDENT-046` 이 공포도를 감소하지 않는 누적으로 확정해 런타임에서 절대 안 나온다. 확정 DEC 두 개의 불일치가 맞다. 지우려면 `DEC-JOURNAL-003` 폐기·대체 + 스키마 규칙 수정 + 승인 CSV 3행 삭제 + `content_version` + 재검증이라 세 사람이 다 움직이는데 얻는 것은 죽은 행 3개 제거뿐이고 게임 동작은 그대로다. **몰라서 안 고친 것이 아니다.** 공포도가 감소하는 규칙이 생기면 문구를 그대로 쓴다 (김민주 발견 · 전성민 판단) | — | 남긴다 | — |
| 8/5 | **김민주의 코드 작업을 최수정이 가져왔다** | 김민주 작업 시간대가 마일스톤과 어긋나(새벽) 완료가 뒤 일정으로 계속 밀렸다. `src/` 는 팀규칙 4절에서 원래 최수정 영역이므로 담당 이동 자체는 규칙 안이다. **김민주에게 남기는 것은 CSV 작성·승인, 기획 판단, 플레이 테스트 셋뿐이다.** 플레이 테스트는 가져올 수 있었지만 일부러 남겼다 — 위의 "만든 것을 배선 안 하고 완료 보고 4회" 가 넷 다 만든 사람이 아닌 사람이 눌러서 나왔다 | 최수정 | 8/5 | P0 |
| 8/5 | **A1 확정 배치가 `hud.ts` 와 일곱 중 여섯 다르다** | 일차·습격 예고가 표지판 하나로 합쳐지고 체력이 하단 카드로 내려가며 재배 시간이 텍스트에서 게이지가 된다. `layout.css` 값 조정 범위가 아니라 **DOM 그룹 재구성 + `HudView` 타입 변경**이다. 4절 8/4 헤더에 "CSS 값만 고친다" 로 적어둔 범위를 넘는다 | 최수정 | 8/5 병합 후 | P1 |
| 8/5 | **이미지 로딩·스프라이트 렌더가 로드맵 어디에도 없다** | 지금까지 필드는 플레이스홀더 도형만 그렸다. A1 에셋 19종이 도착하면서 3층 그리기 순서와 화면 중앙 기준 배경(1920×1080 대 월드 1600×900)이 필요해졌고, 이게 `DEC-UI-025` 기준 해상도와 같은 작업이다. **아트 도착을 "값 조정" 으로만 적어둔 탓에 분량이 드러나지 않았다** | 최수정 | 8/5 병합 후 | P1 |
| 8/5 | `schema/enums.json` UI 에셋 ID 고정 목록 | `open_questions` 가 *"지금은 `src/` 와 `schema/tables/` 어디에도 에셋을 참조하는 코드·필드가 없어 막히는 것이 없으므로, 실제 화면과 에셋이 나올 때 스키마 변경으로 넣는다"* 로 미뤄둔 조건이 8/5에 충족됐다. 11개(`close_button`·`resident_hp_gauge` 는 B단계라 파일 없음) | 최수정 | 8/5 병합 후 | P1 |
| 8/5 | `asset.background.prototype_field` 자리가 두 곳으로 갈렸다 | 전성민이 `enums.json` UI 고정 목록 12개에 넣었는데, `content_assets.json` 의 `asset_role` 에 `background` 가 이미 있고 `maps.csv` 가 부모 후보이며 `content_asset.id_section_matches_role` 규칙과도 맞는다. **`content_assets.csv` 행이 맞고 고정 목록은 `asset.ui.*` 뿐이다.** ~~11개~~ → **12개** (8/5 정정 — `assets/final/ui/` 파일 10개 + B단계 2개. 전성민이 "12개" 라고 쓰며 13줄을 적었고 여기서 배경만 빼 11로 옮겨 적었다) 반대로 `asset.ui.*` 는 `ui` 구간이 `asset_role` 에 없어 CSV 에 못 넣으므로 고정 목록이 맞다 | 전성민 확인 | 8/5 **반영** (7번) | — |
| 8/5 | `RunState.phase`(`RunPhase`)를 **아무도 안 읽고 쓴다** | `state/types.ts` 에 선언돼 있는데 단계의 실제 원본은 흐름(`scenes/flow.ts`)이다. `syncRunDay()` 로 일차만 동기화했고 단계는 손대지 않았다. **계약에 있는데 안 쓰이는 타입은 다음 사람이 그것을 믿고 짠다** — 8/3의 경작지 단계 키 불일치와 같은 모양이다. 동기화하든 지우든 하나를 골라야 한다 (김민주 발견) | 최수정 | 8/6 | P2 |
| 8/5 | **일지 생성 기록을 남길 것인가** | 일지 입력에 스키마 버전이 없다. `DEC-PIPELINE-012` 는 **7/31 확정**이라 일지 시스템(8/1 신설)보다 앞서고 거기 세 버전은 전부 엔딩 것이다. 엔딩이 입력에 `input_schema_version` 을 넣는 이유는 `DEC-CONTENT-011` 이 **엔딩 생성 런타임 기록 9필드**를 정하면서 버전을 남기라고 했기 때문인데, 일지에는 그 결정이 없다 — 지금 남는 것은 `journalEntries` 의 일차·본문·폴백 여부 셋뿐이다. 그러니 **대칭이 아닌 것이 정상일 수 있다.** 매니페스트에 열을 늘리는 것은 스키마 변경이라 코드가 만들지 않았고 `journal_prompt_version` 만 넘긴다. 생성 기록을 남기지 않기로 하면 이 줄은 닫힌다 | 전성민 | 8/6 | P2 |
| 8/5 | **폴백 일지 `온기 × 유지` 가 습격일에 "조용했다" 고 단정한다** | 승인 문구가 *"어제와 다르지 않은 하루였다. 밭은 조용했고 나도 조용했다."* 인데, 공포도를 올리는 원인은 위협·퇴각 대가·처치 셋뿐이라 **공감·협상·영입으로 해결한 습격일은 거의 다 `온기 × 유지` 로 떨어진다.** 2일차에 영순을 영입하고 3일차 아침에 이 문구가 나온 것을 플레이로 확인했다. `terror × same` 의 *"이제 아무도 밭 가까이 오지 않는다"* 도 같은 성질이다. **코드가 고칠 수 없다** — 폴백은 `DEC-JOURNAL-003` 대로 구간·방향 두 값만 보고 고르며 어제 무슨 일이 있었는지 볼 수단이 없다. LLM 이 살아 있으면 조우가 반영되므로 폴백 문구만의 문제다. 습격 여부와 무관하게 읽히는 문장으로 바꾸는 것이 최소 수정이다 (승인 CSV 라 사람만 고친다) | 김민주·전성민 | 8/6 | P2 |
| 8/5 | **HUD 가 독립 화면 위로 비쳤다** | `DEC-UI-017` 이 HUD 를 *필드 공통* 요소로 정했고 `DEC-UI-014` 는 독립 화면을 필드를 대체하는 전환으로 정했는데, `hud.ts` 에 숨기는 수단 자체가 없어 매 프레임 그려지고 있었다. 독립 화면 배경이 완전 불투명이 아니라 **런 실패 화면 위에 `100 / 100 2일차` 가 보였다.** 내가 만든 두 화면만이 아니라 결과 화면 2종·일차 시작도 같았다. `Hud.setVisible()` 을 넣고 `syncScreens()` 가 필드 모드 유무로 끈다. 정비 허브는 오버레이라 그대로 남는다. **캔버스 필드도 같은 이유로 옅게 비치는데 그건 안 건드렸다** — 0.96 배경 뒤라 거의 안 보이고, 끄려면 렌더 루프를 화면 층위로 나눠야 해서 범위가 다르다 | 최수정 | 8/5 **완료** | — |
| 8/5 | **`__dev.goToDay()` 가 `syncRunDay()` 에 덮인다** | `goToDay(5)` 로 5일차로 옮긴 뒤 `startRaid()` 를 부르면 `enterFieldPreview()` 가 `field.entered` 를 쏘고 `syncRunDay()` 가 **흐름의 일차로 되돌린다.** 헤드리스로 재현했다 — HUD 가 `5일차` 였다가 `1일차` 로 돌아온다. 이장은 정상적으로 나오는데(`devStartRaid` 가 일차를 지역 변수로 먼저 읽는다) 조우가 끝날 때 `raidTypeByDay.get(1)` 을 보므로 **엔딩 판정이 아예 안 돈다.** 화면 문제가 아니라 통로가 흐름과 런 상태를 갈라놓은 것이고, 8/2 에 적어둔 그 항목의 실제 사례다. `syncRunDay()` 쪽이 옳으므로(일차의 단일 원본은 흐름이다) **통로를 지우는 것이 맞다** — 4번 범위에 넣었다. 정상 경로(5일차 정비 → 밭을 정찰하러 간다)는 영향 없다 | 최수정 | 8/5 **완료** (3번) | — |
| 8/5 | 실행 순서에 **승인 CSV 검증이 빠져 있었다** | 4절 8/5 김민주 이관분의 *"승인 CSV 전량 `data:validate` → `data:build` → 클라이언트 바인딩 검증"* 이 실행 순서로 안 옮겨졌다. 11개 이관 항목 중 이것 하나만 빠졌다. 옮겨 적을 때 목록을 위에서 아래로 훑지 않고 기억으로 재구성한 결과다. `3-1` 로 끼웠다 — 뒤 번호를 밀면 팀이 쓰던 번호가 어긋난다 | 최수정 | 8/5 (3-1) | P1 |
| 8/5 | `enterFieldPreview()` 가 **테스트 픽스처로만 남았다** | 3번에서 게임 쪽 호출부(`devStartRaid`)를 지워 이제 호출자는 `tests/scene-manager.test.ts` 10곳뿐이다. 거기서는 오버레이와 정지 사유를 보려고 필드 상태를 만드는 데 쓴다. **정상 흐름으로 바꾸면 습격 모드가 전투 전 대화를 자동으로 열어**(`manager.ts` 의 `raid` → `precombat_dialogue`) 그 테스트들이 보려는 상태와 달라진다. 그래서 같이 지우지 않았고 주석을 `테스트 전용` 으로 고쳤다. 게임에서 이 로그가 보이면 지운 통로가 되살아난 것이다 | 최수정 | 8/6 | P2 |
| 8/5 | **`syncRunDay()` 설계 확인 요청에 답한다** | 김민주가 *"일차의 단일 원본을 흐름으로 두고 런 상태가 따라가게 했는데 반대 설계도 가능하다. 내가 정할 게 아니었다"* 로 올렸다. **흐름이 원본인 쪽이 맞다.** ① 일차 전환 규칙(습격 여부 대조, `totalDays` 초과 검사)이 `scenes/flow.ts` 한 곳에 순수 함수로 모여 있다. 런 상태가 원본이면 그 규칙을 두 곳에서 지켜야 하고 어긋나도 아무도 모른다 ② 오늘 실제 사례가 나왔다 — `__dev.goToDay()` 가 런 상태만 옮겼더니 화면은 5일차 습격인데 판정은 1일차로 돌았다. 반대 설계였다면 시스템이 그 상태를 정상으로 받아들였을 것이다 ③ `RunState.phase` 를 아무도 안 읽는 것도 같은 이유다 — 단계의 원본도 흐름이다. 일차만 잇고 단계는 안 이은 것은 일관적이며, `phase` 는 지우는 쪽이 맞다(별도 항목) | 최수정 → 김민주 | 8/5 **회신** | — |
| 8/5 | **포커스를 잃으면 대화·정비 허브가 사라진다** | 담당자가 플레이 중 브라우저 저장 대화상자를 띄우자 대화창이 통째로 사라졌다. 자동 일시정지(`DEC-UI-022`)가 겹치면서 `inputOwner()` 가 `pause` 가 됐는데 **표시 판단이 그 값을 보고 있었다.** `DEC-UI-026` 은 *"가장 위 오버레이가 입력을 독점하고 그 아래 층위는 표시만 한다"* 이고 `DEC-UI-014` 는 대화를 *"필수 화면이며 `Esc` 로 닫을 수 없다"* 로 확정했으므로 사라지면 안 된다. 정비 허브도 같았고 거기서는 **열려 있던 상점·제작 팝업까지 버려졌다.** 표시는 `openOverlays().includes()`, 입력은 `inputOwner()` 로 나누고 모달에 `setInteractive()` 를 넣었다. 일시정지 화면이 아직 뼈대라 덮어 주는 것이 없어서 흐림으로 "지금 못 누른다" 를 직접 드러낸다 | 최수정 | 8/5 **완료** | — |
| 8/5 | **제출 빌드는 첫 화면에서 못 나간다** | `VITE_BUILD_MODE=submission` 으로 빌드해 확인했다. 개발 표시 게이트 자체는 정상이다 — `__scenes`·`__dev` 미노출, 개발 로그 없음. 그런데 타이틀·이름 입력·튜토리얼이 전부 뼈대라 진행 입력이 없고 `devSkipToFarming()` 은 개발 빌드에서만 돈다. **화면에는 필드 도형만 보이고 아무것도 안 눌린다** (캔버스는 화면 층위와 무관하게 계속 그린다). 지금 배포는 `vercel.json` 에 `VITE_BUILD_MODE` 가 없어 dev 로 빌드되므로 프리뷰는 정상이다. 즉 **8/8에 제출 빌드로 바꾸는 순간 못 논다.** `DEC-RESIDENT-047` 이 제출 빌드에서 공포도를 숨기라고 확정했으니 심사용은 submission 이어야 한다 | 최수정 | 8/5 **해결** (10번) | — |
| 8/5 | 타이틀·이름 입력 화면이 **실행 순서에 없다** | 11-1 의 1·2번이 담당 최수정·계획 8/5 인데 실행 순서 0~11 어디에도 없다. 튜토리얼만 10번에 있다. 4절 8/5 최수정 목록에도 없다. **오늘 세 번째 같은 종류의 누락이다** (승인 CSV 검증, `down` 3행 중복에 이어). 위 P0 의 원인이기도 하다 | 최수정 | 8/5 **해결** (10번) | — |
| 8/5 | `run.playerName` 이 **빈 문자열로 LLM 에 간다** | 이름 입력 화면이 뼈대라 `createRunState({ playerName: '' })` 그대로다. 엔딩 기록문과 일지 입력이 그 값을 `player_name` 으로 넘긴다 (`DEC-CONTENT-011`, `DEC-JOURNAL-002` — 이름으로 개인화할 수 있다고 확정). 지금은 이름 없는 일지가 생성된다. 4절 8/5 이관분에 *"이름이 실제로 들어가는지 이때 확인한다"* 로만 적혀 있었는데 **확인 결과 안 들어간다** → **8/5 해결.** 이름 입력 화면이 생겨 `startNewRun(name)` 이 런 상태에 넣는다 | 최수정 | 8/5 **해결** (10번) | — |
| 8/5 | **주민 투사체가 화면에 안 그려졌다** | 담당자가 3일차 만복(`ranged_chase`)과 싸우며 *"만복이 안 움직이는데 내 피만 달았다"* 고 알려왔다. `main.ts` 가 `combat.projectiles`(플레이어 투척)만 렌더에 넘기고 있었다 — 주민 투사체는 `residentCombat` 이 따로 들고 있어서 **아무것도 안 보이는데 체력만 줄었다.** 반지름 원본도 다르다(투척은 `collision_radius`, 주민은 프로필의 `projectile_radius`)라 한 맵에서 둘 다 찾으면 주민 쪽이 늘 기본값으로 떨어진다. 둘을 각각 넘기게 고쳤다. 3일차까지 플레이로 밟아 **적대 투사체 색 픽셀이 60표본 중 20에서 잡히는 것**을 확인했다 — 비행 0.73초 / 대기 2.2초 비율과 맞는다 | 최수정 | 8/5 **완료** | — |
| 8/5 | **적대 주민 공격에 예고가 없다 — `DEC-CONTENT-020` 재검토 조건이 충족됐다** | 담당자가 *"영역이 어느 정돈지도 안 뜨나"* 라고 물었다. **예고가 없는 것은 확정 규칙이다** — `DEC-CONTENT-008` 이 *"적대 주민의 공격은 공격 전 대기시간, 바닥 위험 범위, 근접 부채꼴, 원거리 조준선과 예고 진행 표시를 사용하지 않는다"* 로 정했다. 그리고 `DEC-CONTENT-020`(보류)의 재검토 조건이 *"1차 프로토타입 QA에서 적대 주민의 공격을 예고 없이 받는 것이 불합리하거나 습격 전투의 난도가 읽히지 않는다고 판단될 때"* 다. **만드는 사람이 아닌 사람이 그 판단을 실제로 했으므로 조건이 충족됐다.** 다만 투사체가 안 보이던 버그와 겹쳐 있었으므로, 투사체가 보이는 상태에서 다시 한 번 플레이하고 판단하는 것이 맞다. 도입하기로 하면 `DEC-CONTENT-008` 폐기·대체 + 프로필의 예고 시간 필드 + 습격 HUD 표시 설계가 함께 온다 | 전성민 | 8/6 QA | P1 |
| 8/5 | **타이틀로 돌아가도 필드가 화면에 남는다** | 담당자가 5일차에 런 실패 후 `타이틀로` 를 눌렀더니 **죽은 플레이어와 이장이 그대로 보였다.** 캔버스가 화면 층위와 무관하게 매 프레임 그리고 있었다. `DEC-UI-014` 는 필드를 베이스 화면으로, 독립 화면을 그것을 **대체하는 전환**으로 정했다. HUD 를 고칠 때 "캔버스도 같은 이유로 비치는데 범위가 달라 안 건드렸다" 로 남겨 뒀는데, 런 실패 뒤에는 그냥 둘 수준이 아니었다. `FieldRenderer.clear()` 를 넣고 필드 모드가 없으면 지운다. **오버레이 숨김은 계속 돌게 남긴다** — 조기 반환하면 정비 허브가 열린 채 밤 결과로 넘어갔을 때 허브가 화면에 남는다. 타이틀 복귀 후 바탕이 아닌 캔버스 픽셀이 0 인 것을 확인했다 | 최수정 | 8/5 **완료** | — |
| 8/5 | **회복 파우치가 통째로 배선되지 않았다** | 담당자가 꿀바나나를 만들어 소모품 보관함에 넣었는데 HUD 가 `회복 아이템 없음` 이라고 말했다. 세 겹이었다 — ① `DEC-RESOURCE-017` 의 *"`회복 아이템 없음` 상태에서 회복 가능 아이템을 획득하면 자동 선택한다"* 가 미구현이라 `pouch.selectedId` 를 아무도 채우지 않았다 ② HUD 가 표시 이름이 아니라 **ID** 를 넘기고 있어서, 선택돼도 `recovery_item.honey_banana` 가 뜰 자리였다 ③ **회복 사용 자체가 미구현**이었다 — `Q` 짧게 누르기가 `console.info` 한 줄이었고 `RecoveryInProgress` 타입만 있었다. `src/systems/recovery.ts` 를 만들어 자동 선택·순서(`DEC-UI-001`)·게이지·소비·취소를 넣었다. 회복 퀵메뉴(`Q` 길게)는 6절에서 P2 로 컷된 항목이라 그대로 둔다 | 최수정 | 8/5 **완료** | — |
| 8/5 | `rules.mjs` 주석이 **없는 `open_questions` 항목을 가리킨다** | *"specific_resident_relationship 은 관계 키 목록이 결정로그에 없어 검사하지 않는다. `schema/enums.json` 의 `open_questions` 참조"* 라고 적혀 있는데 그 배열에는 UI 에셋 항목 하나뿐이었고 관계 키 이야기가 없다. 7번에서 그 하나를 닫아 배열이 비었으므로 **이제 완전히 헛짚는다.** 주석을 지우거나 관계 키 항목을 실제로 `open_questions` 에 적어야 한다 — 후자면 어떤 관계 키가 허용되는지가 기획 결정이라 전성민 몫이다 | 최수정·전성민 | 8/6 | P2 |
| 8/5 | **회복이 소모되지 않는다** — 원인은 취소일 가능성이 높다 | 담당자가 꿀바나나를 쓰면 *"속도는 느려지는데 소모가 안 되고 보관함에 그대로 남는다"* 고 알려왔다. **`DEC-INPUT-005` 가 "회복이 완료되기 전에 공격받으면 취소되며 아이템은 소비하지 않는다" 로 확정했고**, 꿀바나나 사용 시간이 2.5초인데 이장 공격 주기가 1.8초·영순 1.4초라 **습격 중에는 거의 항상 취소된다.** 즉 규칙대로일 가능성이 높다. 8/5까지 회복 사용 게이지(`DEC-UI-017`)가 미구현이라 취소가 화면에 드러나지 않았고, 그래서 "안 먹힌다" 로만 보였다. 게이지와 `Q — 취소` 힌트를 붙였으므로 다음 플레이에서 갈린다. **헤드리스로는 재현하지 못했다** — 가만히 서 있으면 야생동물에게 맞아 런이 실패해 회복까지 가지 못한다 | 담당자 확인 | 8/5 | P1 |
| 8/5 | 야생동물 피해에는 **회복 취소가 안 걸려 있었다** | `DEC-INPUT-005` 의 "공격받으면 취소" 를 습격의 주민 피해 자리에만 넣고 재배의 야생동물 피해를 빠뜨렸다. 재배에서는 맞아도 회복이 계속 진행됐다. 피해를 주는 쪽마다 부르는 구조라 하나를 빠뜨린 것이고, `combat.playerDamaged` 이벤트 한 곳에서 듣도록 바꿨다 — 피해 경로가 늘어도 다시 빠뜨리지 않는다 | 최수정 | 8/5 **완료** | — |
| 8/5 | **엔딩 화면은 끝내 못 봤다** — 12번에서 먼저 본다 | 8번 시작 전에 스크립트로 5일차까지 밟아 보려다 **3일차 만복에게서 죽었다.** 원인이 데이터에 있다 — `personality_choice_outcomes` 에서 만복(`pragmatic`)은 **공감이 `combat_normal`** 이라 보정 없이 체력 80·원거리 12피해를 그대로 상대해야 한다. 무혈 경로는 **자원 협상뿐이고 그건 수확물이 있어야 열린다.** 즉 만복을 편하게 넘기려면 1~2일차에 실제로 재배해 수확물을 모아야 한다. 스크립트로는 그 재배(60초 × 이동 × 야생동물 회피)까지 감당이 안 된다. **엔딩 화면은 만든 사람이 한 번도 못 본 상태로 남는다** — 12번에서 그 지점을 먼저 확인한다 | 담당자 | 8/5 **확인 완료** — 이장 영입으로 엔딩 도달 | — |
| 8/5 | 만복은 **수확물 없이 만나면 원거리 전투를 그대로 해야 한다** | 위 시도에서 나온 플레이 정보다. 영순(`timid`)·금례(`stubborn`)·이장(`chief`) 과 달리 만복만 공감이 `combat_normal` 이고, 협상은 수확물이 있을 때만 `resolve` 다. 3일차 전에 수확물을 모아 두는 것이 사실상 필수 경로다. **밸런스 문제로 올리는 것이 아니라 완주 경로 정보로 남긴다** — 데이터는 의도대로 동작한다 | — | 기록 | — |
| 8/5 | **회복 퀵메뉴가 컷이 아니라 버그였다** | 담당자가 *"퀵메뉴가 콘솔에는 뜨는데 화면에 안 떠서 회복 아이템 변경이 안 된다"* 고 알려왔다. `Q` 롱프레스가 `recovery_quickmenu` 오버레이를 여는데 **오버레이가 열리면 시뮬레이션이 정지하고**(`syncSimulation`) 그릴 UI 는 없다 — 즉 롱프레스하면 **게임이 멈춘 채 빈 화면**이 됐다. 6절의 컷 원문은 *"롱프레스(→ `Q` 짧게 누르기만)"* 이므로 롱프레스가 아무 일도 안 해야 맞다. 오버레이를 열지 않게 고쳤다. **되살릴 때는 오버레이만으로 부족하다** — `DEC-INPUT-008` 이 퀵메뉴를 *"게임 전체의 속도를 크게 낮춘다"* 로 정했으므로 정지가 아니라 감속(`loop.setTimeScale`, 지금 아무도 안 부른다)이고 `syncSimulation` 이 퀵메뉴를 다른 오버레이와 갈라야 한다 | 최수정 | 8/5 **완료** | — |
| 8/5 | 회복 아이템을 **고를 수 없다** — P2 컷의 결과 | 퀵메뉴가 없으므로 선택은 `DEC-RESOURCE-018` 의 자동 선택뿐이다. 순서는 제작 회복 아이템 → 생식 수확물, 각 ID 오름차순이라 `fruit_honey_bowl`(70) → `honey_banana`(35) → `honey_tomato`(15) 순으로 **회복량이 큰 것부터** 뽑힌다. 아껴 쓰는 전략은 못 쓴다. 되살릴지는 8·9·11번을 끝낸 뒤 시간을 보고 정한다 | 최수정 | 8/5 **완료** — 만들었다 | — |
| 8/5 | `content_assets.csv` 부모 후보에 **`player_base_stats.csv` 가 없었다** | 전성민이 아트 디렉션 14.7 로 올린 규칙 누락이다 — 대화 화면에 플레이어 상반신 일러스트를 넣기로 하면서 드러났다. 부모 후보 아홉에 플레이어가 없어 `field_sprite` 와 `portrait` 를 어디에도 못 붙인다. `enums.json` 의 UI 고정 목록도 답이 아니다(`DEC-ART-001` 이 그 범주를 배경·로고·셔터·패널·버튼·아이콘·HUD 틀·폰트로 한정했다). **부모 후보에 더했다** — 승인 행이 정확히 하나라 참조가 모호해지지 않고 `DEC-ART-001` 은 역할×테이블을 정하지 않았으므로 DEC 변경 없이 스키마 수정으로 끝난다. `data:validate` 통과 | 최수정 | 8/5 **완료** | — |
| 8/5 | **12fps 까지 떨어졌다** — 원인 둘 | 담당자가 *"게임 자체가 되게 렉 걸린다"* 고 알려왔다. 실측하니 프레임 간격 중앙값 **83.3ms(12fps)** 였다. ① **내가 만든 것** — 기준 해상도 무대를 넣으면서 캔버스 백킹을 `1920 × devicePixelRatio` 로 잡았다. 1280×720 창·DPR 1.25 에서 **2400×1350 을 그리고 브라우저가 다시 줄이고** 있었다. 변환이 반영된 `getBoundingClientRect()` 로 실제 크기(1600×900)를 읽게 고쳤다 ② **원래 있던 것** — `drawWorldGrid` 가 `WORLD_TO_PIXEL`(=1) 간격으로 돌아 **매 프레임 3000줄**을 그었다. `DEC-ART-001` 이 월드 1단위 = 1픽셀로 확정하면서 격자 간격이 1px 가 된 것이다. 배경 이미지가 덮을 자리라 지웠다. **고친 뒤 16.7ms(60fps)** | 최수정 | 8/5 **완료** | — |
| 8/5 | **A3·A4 목업이 최수정 캡처를 기다린다** | 전성민이 정비 허브·대화 목업을 이미 구현된 배치에 맞춰 뽑겠다며 `maintenance-hub`·`dialogue-modal` 화면 캡처를 요청했다. 5분이면 되는데 안 보내면 **전성민이 오늘 남은 시간을 못 쓴다.** 실행 순서 0번에 넣었다 | 최수정 | 8/5 **완료** (`docs/planning/captures/`) | — |

적을 때 판단할 것은 **티어 하나**뿐이다 (6절 기준).
P0면 그날 일정을 다시 짜고, P2면 목록에만 남기고 넘어간다.

### 11-3. 점검 리듬

| 시점 | 하는 일 |
|---|---|
| 매일 마무리 | 11-1에서 오늘 바뀐 줄 갱신, 11-2에 발견 항목 추가 (10-2의 5번) |
| **8/3 밤 (중간 전수 점검)** | 11-1 전체를 훑고 `없음`이 남은 줄의 담당·날짜를 다시 배정한다. 여기서 한 번 걸러야 8/5에 몰리지 않는다 |
| 8/4 22:00 | 컷 트리거 판단 (6절) |
| 8/5 오전 | 통합 전 마지막 확인. `없음`이 남은 줄은 폴백 경로라도 넣어 흐름이 끊기지 않게 한다. **완주 플레이에서 QA 조건부 보류 7건(`DEC-RUN-005`, `DEC-INPUT-010`·`011`, `DEC-FARM-009`·`011`, `DEC-CONTENT-014`·`020`)을 같이 판단해 전성민에게 넘긴다** |
