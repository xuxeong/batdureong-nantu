# 남은 작업 — 2026-08-07

작성: 최수정 · **8/7 저녁**
이전 문서: `handoff-2026-08-06.md` (경위는 거기 남는다. **현재 상태는 이 문서다**)

8/6 낮부터 8/7 저녁 사이에 디스코드로 오간 결정이 문서에 하나도 안 들어가 있었다.
이 문서는 그 결정들을 반영하고 **8/8 촬영까지 남은 것만** 정리한 것이다.

---

## 0. 지금 상태

`develop` = `24b99df` (8/7 밤). 검증 전부 통과 — `data:validate` 38/38 ·
`docs:check` 문제 없음 · `npm test` 207/207 · `tsc --noEmit` · 클린 빌드.

**`main` 이 63커밋 뒤처져 있다.** `main` 은 아직 8/6 새벽의 `v0.2` 라
**프로덕션 URL 에는 캐릭터도 아이콘도 투사체도 없다.** `links.md` 의 플레이 빌드
링크가 비어 있는데, 지금 그 자리에 넣을 URL 이 도형 빌드를 가리킨다.

`main` 에 아직 없는 것 — F·E·C 에셋 31행 연결과 렌더 · 선택지 말풍선 · 항목 안내
hover 이전 · 튜토리얼 P0 둘(`schema_version` 7) · 대화창 초상화 칸 · 수확 반경 90.

---

## 1. 8/6~8/7 에 확정된 것 — 이제 미결이 아니다

| 항목 | 결정 | 출처 |
|---|---|---|
| 튜토리얼 P0 셋 | `assign_quickslot` 신설 · `sell_crop` 삭제 · `starting_money` 5 | 전성민 8/6 · 구현 `83cd36b` |
| **폰트** | **별도 제작하지 않고 시스템 폰트를 쓴다.** 미완료 에셋 목록에서 뺀다 | 전성민 8/7 |
| **좌우 반전** | **적용하지 않는다.** portrait 을 만들 때 좌우 인물이 바라보는 방향을 맞춰서 만든다 | 전성민 8/7 |
| **`effect`** | **빈 에셋 구간이 아니다.** PNG 도 `content_assets.csv` 행도 만들지 않고 `src/render/field.ts` 의 Canvas 2D 에서 코드 도형과 시간값으로 그린다. 넷 — 지속 피해(불씨) · 이동 둔화(찌그러진 고리) · 명중 순간(방사형 충격선) · 낫 휘두름(반원 호). `effect` 역할 자체는 향후 콘텐츠를 위해 스키마에 남긴다 | 전성민 8/6·8/7 |
| **에셋 크기** | **ⓐ 파일을 의도한 크기로 다시 뽑는다.** 전성민이 직접 수정해 올린다. **렌더 배율·판정 반경은 건드리지 않는다.** 논리 에셋 ID 는 그대로 | 전성민 8/7 |
| **숙련도 해금** | **현행 유지** — "작물을 제작에 소비한 수량만큼 작물별 숙련도 증가". 꿀 소비 합산은 제출 이후 별도 안건 | 전성민 8/7 |
| **지원 주민 공격** | 실제 충돌 투사체가 아니라 즉시 확정 피해다 (`DEC-CONTENT-008`). 돌·지원 엽전·부지깽이 PNG 를 만들지 않는다. **적대 만복의 엽전만 실제 투사체**라 `manbok_coin.png` 를 쓴다 | 전성민 8/6 |
| **LLM 키** | **전성민 개인 키를 Vercel Production 에 넣는다** | 전성민 8/6 |

→ `portrait` 5종 파일은 8/7 밤 추가됐고 연결·표시 조정이 남았다. 실제 파일 제작이
남은 에셋 구간은 **`logo` · `cutscene` · `sfx` · `bgm`** 넷이다.

### 1-A. portrait 5종 구현 인계 (8/7 밤)

전성민이 기존 최종 컨셉아트를 상반신으로 크롭한 초상화 5종을 올렸다.

```
assets/final/portrait/player.png
assets/final/portrait/yeongsun.png
assets/final/portrait/manbok.png
assets/final/portrait/geumrye.png
assets/final/portrait/village_head.png
```

**투명 여백을 자르지 않는다.** 다섯 파일은 모두 높이 1080px 캔버스이고, 위쪽
투명 여백과 바닥선은 인물간 키·체격·상대 크기를 유지하기 위한 정렬 정보다.
개별 알파 영역으로 자동 트림하면 이 비례가 깨진다.

현재 `.dialogue__portrait` 은 180×288px 안에 `background-size: contain`으로 그린다.
이 방식은 폭이 넓은 만복·플레이어를 폭에 맞추며 다른 인물보다 작게 만든다.
A4 재배치 때 다음을 같이 적용한다.

- 인물은 목업처럼 화면에 크게 놓고, 대사창이 인물 하단을 가린다.
- 파일의 공통 1080px 높이를 기준으로 같은 배율을 쓴다. `contain`보다
  `background-size: auto 100%` 또는 동일한 효과의 `<img>` 높이 기준 렌더를 쓴다.
- 만복이 잘리거나 작아지지 않게 양쪽 인물 영역의 폭을 충분히 둔다.
- 바닥 정렬은 `bottom center`를 유지한다.
- 좌우 반전은 적용하지 않는다. 플레이어는 왼쪽을 보고 주민 넷은 기존
  컨셉아트의 정면 시점을 그대로 쓴다.

`content_assets.csv`에는 다음 5행을 추가해야 한다. 연결 행 추가는
`DEC-PIPELINE-011`의 부모 버전 규칙을 따라 `player_base_stats.prototype`과 주민 4행의
`content_version`을 각각 1 증가시킨 승인 후보로 처리한다.

```csv
player_base_stats.prototype,portrait,asset.portrait.player
resident.yeongsun,portrait,asset.portrait.yeongsun
resident.manbok,portrait,asset.portrait.manbok
resident.geumrye,portrait,asset.portrait.geumrye
resident.village_head,portrait,asset.portrait.village_head
```

연결 후 `npm run data:validate` → `npm run data:build` → A4 실화면 크기·가림·선택지
배치를 확인한다.

---

## 2. P0 — **UI 에셋 16장이 파일만 있고 코드가 쓸 수 없다**

**8/7 에 찾았다. 아무도 안 적어둔 자리다.**

전성민이 준 A4 조립 지시는 이렇다.

```
대사창       panel_border + panel_texture 9-slice
발화자 이름판  button_normal
일반 선택지    button_normal
비활성 선택지  button_disabled
선택지 외곽    choice_balloon.png
```

그런데 `schema/enums.json` 의 `ui_system_asset_id` 고정 목록은 **13개뿐**이고
`panel_border`·`panel_texture`·`button_*` 이 전부 없다. `src/render/assets.ts` 는
목록에 없는 ID 를 쓰면 **의도적으로 throw 한다** (`DEC-ART-001` — 새 ID 추가는
데이터 행 추가가 아니라 스키마 변경).

즉 **CSS 만 고쳐서는 안 붙는다. 스키마 변경이 선행돼야 한다.**
전성민이 8/7 에 *"대사창과 choice_balloon 에셋 모두 제대로 적용되지 않은 것으로
보인다"* 고 한 것의 원인이 이것이다. 목록에 있는 `choice_balloon` 만 붙었다.

`assets/final/ui/` 28장 중 목록에 없어 코드가 못 쓰는 16장:

```
panel_border   panel_texture                       A4 대사창
button_normal  button_pressed  button_disabled     A4 이름판·선택지 · 정비 허브 버튼
item_slot      item_slot_badge                     보관함 칸과 수량 배지 (14.8)
lock_icon                                          잠긴 레시피 (DEC-UI-006)
money_plate                                        소지금 표시
raid_notice_none  raid_notice_raid  raid_notice_final   습격 예고 세 종류 (DEC-RUN-011)
shutter_left   shutter_right                       ← 인계 3-3 「셔터 연출」의 답이 이미 파일로 와 있다
step_minus     step_plus                           수량 조절
```

**`shutter_left`·`shutter_right` 는 따로 본다.** 8/6 인계 3-3 이 *"「셔터 연출」이라
부르는데 실제로는 어두운 판이 즉시 나타날 뿐이고 아무도 안 적어둔 누락"* 으로
올려 둔 항목인데, **그림이 이미 와 있다.** 방향·시간·나갈 때도 연출인지는 여전히
전성민 결정이지만 "이미지를 쓸지" 는 파일이 답했다.

목록에 있는데 파일이 없는 것은 `resident_hp_gauge` 하나다. `close_button` 은
파일이 왔는데 `layout.css:1283`·`maintenance-hub.ts:297` 주석이 아직
*"B단계라 파일이 없어 글자 버튼"* 이라고 말한다. `enums.json` 의 `$comment` 도 같다.

---

## 3. 지금 할 일 — 순서대로

> **8/7 밤 갱신.** A·C·D 는 끝났고 아래 3-A 에 그날 들어간 것을 모았다.
> 남은 것은 **B(김민주) · E(병합) · F(LLM 키) · G(links)** 넷이다.

| # | 할 일 | 담당 | 선행 | 막혔나 |
|---|---|---|---|---|
| **A** | ~~UI 에셋 ID 등록~~ **8/7 완료** (`85adac0`) — 16장 전부가 아니라 **A4 가 요구한 넷만** 넣었다. 나머지 12장은 배정된 작업이 없어 쓰는 시점에 넣는다 | 최수정 | — | — |
| **B** | **A4 대화 화면 목업 기준 재조립** — 인물을 지금보다 훨씬 크게 · 대사창이 화면 가로 대부분을 차지하며 두 인물 하단을 덮음 · 대사창에 `panel_border`+`panel_texture` · **선택지는 대사창 내부 목록이 아니라 두 인물 사이에 `choice_balloon` 을 띄우고 그 안에 버튼 3개** | 김민주 | A | A 대기 |
| **C** | ~~에셋 파일 재출력~~ **8/7 완료** (`b76b43a`) — 사람 60~107×120~154, 투사체 32×32. 검수 통과: 크기·파일명 규칙·`tsc`·`data:validate`·`docs:check`·테스트 207 | 전성민 | — | — |
| **D** | ~~낫 휘두름 반원 호~~ **8/7 완료** (`9cb4231`) — **이 줄의 "지속 피해·둔화·명중 순간은 이미 있다" 는 틀렸다.** 명중 순간도 없었고 `ed5bf2e` 에서 같이 넣었다 (3-A 참조) | 최수정 | — | — |
| **E** | **`develop` → `main` 병합** (`--no-ff` + 태그). **8/8 촬영 전에 반드시 한다** — 지금 `main` 은 캐릭터도 아이콘도 없는 도형 빌드다. 8/7 에는 미룬다(아트와 A4 가 더 들어올 예정이라 어차피 한 번 더 병합한다). **잊으면 도형 빌드로 촬영한다** | 최수정 | A·B | 아니오 |
| **F** | **LLM 키 Vercel Production 등록** 후 5일차 엔딩까지 밟아 폴백이 아닌지 확인. **화면으로는 구분되지 않으므로**(`DEC-UI-024`) 전날 조우가 일지 문장에 나오는지로 판단한다 | 전성민 → 팀 | — | 아니오 |
| **G** | `links.md` 플레이 빌드 URL · 확인 사항 넷 | 팀 | E | E 대기 |

**남은 순서: B → E → 촬영.** F 는 언제든, G 는 E 뒤다.

### 3-A. 8/7 밤에 들어간 것 (커밋 9개)

`DEC-ART-001` 폐기·`DEC-ART-002` 대체가 캐릭터 모션을 열면서 4-1 미결이 닫혔고,
그 자리에 있던 것들을 한 번에 넣었다. **전부 `src/render/`·`src/main.ts`·
`schema/`·`tools/` 라 김민주의 A4 작업과 겹치지 않는다.**

| 커밋 | 내용 |
|---|---|
| `0559d14` | 디코에 흩어진 확정 8건을 문서로 회수. 이 문서 신설 |
| `85adac0` | A4 대사창·버튼 에셋 ID 넷 등록 — **김민주 B 차단 해제** |
| `9cb4231` | 낫 휘두름 호. 판정 모양 그대로(사거리 안 · 조준 ±90도) |
| `5a496d0` | 없는 곳 가리키던 주석 둘 · P2 목록 중 이미 끝난 둘 확인 |
| `49e5f61` | `DEC-ART-002` 치환 검수 — 뜻이 뒤집힌 주석 하나, 남은 참조 셋 |
| `aa77e8a` | **걷기 bob** — 확정문대로 새 스프라이트 없이 정면 한 장. 걸음 속도를 실제 이동 속도에 비례 |
| `3a94ce8` | **좌·우·공격 교체 스프라이트 배선** — 그림이 오면 `content_assets.csv` 행 추가만으로 켜진다 |
| `ed5bf2e` | 명중 순간 충격선 · 피격 깜빡임 · 일시정지 중 조준선 정지 |
| `24b99df` | 튜토리얼 마지막 단계에서 던지는 것을 보고 넘어간다 (0.9초) |
| `d8b9b04` + 이 커밋 | 이 문서 갱신 · **지원 주민 공격에도 명중 표시** (3-B-1) |

**이펙트 넷이 다 찼다.** 8/6 문서가 "명중 순간은 이미 있다" 고 적었는데
확인해보니 `drawHostile` 에는 둔화 링·지속 피해 링·예고 원·체력 막대뿐이었다.
flash 로 본 것은 `drawAlly` 안의 지원 주민 공격 표시였다.

**조준선은 "일시정지 때 멈춤" 이 아니라 "필드가 입력을 가질 때만 따라감" 으로
짰다.** 일시정지만 막으면 대화·정비에서 같은 문제가 남는데, `DEC-UI-026` 이
이미 *"가장 위 오버레이가 입력을 독점한다"* 로 정해 둬서 셋이 한 번에 풀린다.

**튜토리얼 지연은 `DEC-UI-030` 을 건드리지 않는다.** 확정문이 *"조작을 실제로
성공하면 다음으로 넘어간다"* 인데 지연은 성공 여부가 아니라 알리는 시점만
미룬다. 8/6 에 "다음·이전 버튼" 이 이 확정문과 충돌해 접혔던 것과 같은
자리인데 이번에는 확정문 안에 길이 있었다.

### 3-B. 만든 것을 아무도 안 듣고 있던 자리 셋 — 하루에 셋이 나왔다

로드맵 11-2 의 *"만든 것을 배선하지 않고 완료로 보고한다"* 목록에 8/7 것을
더한다. **셋 다 만든 사람이 쓸 자리를 정확히 알고 있었는데 그 자리가 안
만들어진 채로 남았다.**

| 언제부터 | 무엇 |
|---|---|
| 8/3 | `resident-combat.ts` 의 `attacked` 이벤트 — `main.ts` 가 `playerDamaged` 외에는 버렸다. **주민이 공격하는 순간이 화면에 아무 흔적도 안 남았다** |
| 8/4 | `CombatEvent.damaged` 의 `overTime` — 주석이 *"화면 표시를 가르는 데 쓴다"* 인데 읽는 곳이 없었다. 명중 순간 표시가 없던 이유다 |
| 8/7 | `AssetRole` 에 새 구간 셋 누락 — `enums.json`·`content_assets.json` 에는 들어갔는데 `src/data/types.ts` 가 안 따라왔다. CSV 행은 검증을 통과하는데 코드가 타입 오류로 거절할 상태였다 |
| 8/7 | **지원 주민 공격의 명중 표시** — `applySupportDamage()` 도 `damaged` 를 내는데 `main.ts` 가 그 자리에서 `surrenderOffered` 만 보고 버렸다. 아래 3-B-1 |

세 번째가 특히 반복 패턴이다. **데이터 층이 바뀔 때 `src/data/types.ts` 를 같이
여는 절차가 없다.** 같은 날 UI 에셋 16장과 `DEC-ART-002` 주석도 같은 모양이었다.

#### 3-B-0. 승인 반경과 그림 크기가 갈린다 — 8/6부터 있었다

**담당자가 *"충격선이 안 보인다"* 로 잡았고 원인은 명중 표시가 아니었다.**

승인 충돌 반경은 까마귀 14 · 사람 18~24 · 멧돼지 30 인데, 8/7 리사이즈 뒤
스프라이트는 99~121px 폭이다. `drawHostile` 이 상태 표시를 **판정 반경 기준**
으로 그리고 있어서 전부 캐릭터 몸통 안쪽에 들어갔다. **체력 막대가 가슴팍에
얹혀 있었다** — `at.y - radius - 10` 이면 사람 스프라이트(143px) 기준으로
중심에서 28px 위인데 그림은 71px 위까지 있다.

8/6 에 그림을 붙인 순간부터 그랬고 아무도 안 봤다. 리사이즈로 더 두드러졌다.

| | 처리 |
|---|---|
| 명중 충격선 | **그림 바깥으로** 뺐다. 어두운 선 위에 크림색을 얹어 밝은 흙에서도 보이게 했다 |
| 체력 막대 | **머리 위로** 올렸다 (그림 높이 기준) |
| 둔화 · 지속 피해 · 공격 예고 링 셋 | **판정 반경 그대로 둔다** — 담당자 판단. 상태 표시라 인물에 걸쳐 있어도 읽히고, 밖으로 빼면 발밑 고리가 아니라 후광처럼 보인다 |

#### 3-B-1. 네 번째는 이 목록을 쓴 직후에 나왔다

명중 표시를 넣으면서 낫(`swingSickle()` 반환값)과 투척(`combat.update()`)
두 경로만 연결하고 **완료로 적었다.** 같은 `damaged` 이벤트가 세 번째 경로인
`applySupportDamage()` 에서도 나오는데 `grep` 을 안 하고 아는 것만 확인했다.

**지원 공격은 이것이 유일한 흔적이라 더 걸린다.** `DEC-CONTENT-008` 이 지원
공격을 즉시 확정 피해로 정해서 투사체가 없고, 전성민이 *"돌·엽전·부지깽이 PNG를
추가하지 않는다. 필요한 것은 시각적인 발사·명중 효과뿐"* 으로 확정했다.
발사 쪽(`attackFlash`)만 있고 명중 쪽이 비어 있어서, **투사체도 없고 명중
표시도 없으면 지원 주민이 뭘 하는지 화면에 아무것도 안 남는다.**

담당자가 *"충격선이 뭘 말하는 거야"* 라고 물어서 어디서 켜지는지 다시 세다
나왔다. **위 목록을 문서에 정리한 바로 다음 커밋에서 같은 실수를 했다.**

**그리고 다섯 번째가 바로 이어서 나왔다.** 지원 공격을 붙이고도
`combat.update()` 를 받는 곳이 **둘**이라는 것을 못 봤다 — `updateRaid()` 와
재배 루프가 각각이고 습격 쪽만 연결했다. 야생동물에 던지는 것이 훨씬 자주
일어나는데 거기가 빠져 있었다.

그래서 이제 **`noteHitFlash(event)` 한 곳으로 모았다.** 켜는 조건을 네 군데에
복사해 두면 다음에 호출자가 하나 더 생길 때 또 빠진다. 낫만 예외인데 그건
이벤트가 아니라 `swingSickle()` 반환값이라 구조가 다르다.

---

## 4. 미결 — 구두로 흘러가서 어느 문서에도 안 들어간 것

### 4-0. 8/7 밤에 새로 올라온 미결 셋

| # | 내용 | 담당 |
|---|---|---|
| **조준선 색** | `#ff4d4d` 가 **아트 디렉션 4.2 절과 부딪힌다.** 4.2 가 *"고추와 토마토가 붉은 계열이라 밭 안에서 붉은색을 쓰면 익은 작물과 같은 색으로 읽힌다"* 로 정하면서 **"이 항목은 4.4 절의 UI 예외와 무관하게 유지된다. 밭 안에서 판독이 갈리는 것이 문제였고 띠 위의 게이지는 그 판독에 끼어들지 않는다"** 까지 적어 뒀다. 조준선은 띠 위가 아니라 **밭 안**이고 길이도 반경 4.5 배다. 4.2 가 막은 것이 "적대 표시" 라 형식상 대상은 아니지만 4.2 가 든 이유는 그대로 걸린다. 같은 날 쓴 두 절이 어긋나 있다 | 전성민 |
| **야생동물 bob** | `DEC-ART-002` 는 **스프라이트 예외에서만** 야생동물을 제외했고 bob 은 언급이 없다. 확정문에 없어서 안 넣었다. 까마귀가 통통 튀는 것이 맞는지 판단이 필요하다 — 새는 걷는 것이 아니다. 넣기로 하면 `hostileViews()` 야생동물 쪽에 한 줄 | 전성민 |
| **주민 체력 밸런스** | *"투척 없이 낫로만 쉽게 클리어된다"* 를 승인 데이터로 확인했다. **체력만 올리는 것이 답이 아닐 수 있다** — 아래 4-0-1 | 전성민·김민주 |

#### 4-0-1. 낫만으로 클리어되는 실제 이유

```
낫          10 피해 · 0.5초 · 사거리 60      = 20 dps
플레이어     이동속도 260
주민         영순 235 · 만복 200 · 금례 150 · 이장 190
근접 사거리   영순 50 · 금례 52 · 이장 54
```

**플레이어가 넷 다보다 빠르고 낫 사거리가 주민 근접 사거리보다 길다.**
치고 물러나면 구조적으로 안 맞는다. 체력만 올리면 투항까지 필요한 횟수가
늘 뿐이고(영순 5회 → 이장 18회) **안 맞는 것은 그대로라 지루해지기만 한다.**

손잡이는 셋이다 — 주민 이동속도를 플레이어 위로, 주민 근접 사거리를 낫 위로,
낫 쿨다운을 늘려서. **어느 것을 돌릴지는 밸런스 결정이고 승인 데이터라
사람만 고친다.**

### 4-1. ~~캐릭터 모션이 `DEC-ART-001` 과 충돌한다~~ — **8/7 해결**

> `DEC-ART-001` 폐기 · `DEC-ART-002` 대체로 닫혔다. 프레임 교체를 **셋만**
> 예외로 허용한다 — 낫 스윙 1장, 주민 공격 1장(주민마다), 좌·우 각 1장.
> `field_sprite` 를 쓰는 전투 참여 캐릭터에 한정하고 야생동물은 제외한다.
> **걷기는 예외가 아니라 코드 bob** 으로 못박혔고 8/7 에 구현했다(`aa77e8a`).
> 좌우는 캔버스 반전이 아니라 실제 그림 두 장이다. 스프라이트 15장은 제작 전.
>
> 경위를 남기므로 본문은 그대로 둔다.

#### (경위) 캐릭터 모션이 `DEC-ART-001` 과 충돌한다

8/6 밤 대화에서 셋이 나왔다.

- *"낫을 따로 안 주고 붙은 채로 주고, 휘두를 때 휘두르는 스프라이트로 교체 0.2초"*
- *"주민들도 공격할 때 낫 휘두르는 것처럼 스프라이트가 교체되는 걸로"*
- *"정면 + 낫 휘두름 → 좌·우 → 걷는 애니메이션 순으로 추가"*

**셋 다 `DEC-ART-001` 의 프레임 애니메이션·스프라이트 시트 금지와 정면 충돌한다.**
2프레임이어도 프레임 애니메이션이다. 전성민의 8/7 회신에는 이 건이 없다.

지금 상태로는 근거가 없어 구현할 수 없다. **폐기·대체를 할지 8/8 회의에서
정해야 한다.** 안 정하면 8/7 플레이 테스트에서 나온
*"다 정면만 있어서 캐릭터가 동동 떠다닌다"* 는 못 고친다.

같은 문장이 *"움직임은 코드가 위치·크기·투명도로 표현한다"* 로 열어 둔 범위는
그대로 쓸 수 있다 — 걷기 흔들림, 피격 깜빡임이 그 안이다.

### 4-2. 필드의 조준선이 근거 없이 그려진다

**4-1 과 같이 물어야 한다.** 아트 디렉션 미결정 목록에 이미 올라와 있다.

> **필드의 조준선 | 결정 없음** — `DEC-INPUT-002` 는 *"낫 공격은 마우스 커서
> 방향을 기준으로 한다"* 로 **판정 기준만** 정했고 조준선을 그리라는 규칙이 없다.
> `DEC-UI-017`·`018`·`019` 어디에도 없다. 그런데 `field.ts` 가 **제출 빌드에서도
> 그린다.** `DEC-UI-018` 이 상호작용 반경 도형을 금지한 정신을 따르면 개발 빌드로
> 내리는 것이 맞다

8/7 에 낫 휘두름 호가 들어와서 더 걸린다 — 휘두른 방향은 호가 말해주는데 선이
상시로 같은 방향을 가리킨다. 담당자 플레이 테스트의 *"조준선만 돌아가서
부자연스럽다"* 도 이 자리다.

**낫 그림이 도는 것은 답이 아니다.** 아트 디렉션이 8/6 에 *"낫은 별도
스프라이트가 아니다 — 낫을 들고 있는 모습은 `field_sprite` 안에, 휘두르는
순간은 Canvas 2D 의 호. **낫 스프라이트를 회전시키는 것은 그 목록에 없다**"* 로
정했다. 낫이 `player.png` 안에 그려져 있어 따로 돌릴 수 없다.

### 4-3. 8/6 인계에서 그대로 남은 것

| # | 내용 | 담당 |
|---|---|---|
| 2-7 | **적대 주민 공격 예고** — `DEC-CONTENT-020` 보류의 재검토 조건이 충족됐다 | 전성민 |
| 2-8 | **`docs:check` 교착** — `check-docs.test.mjs` 가 주의까지 차단으로 취급한다 | 전성민 판단 |
| 3-3 | **셔터 연출** 방향·시간·나갈 때도 연출인지 (이미지는 2절대로 이미 왔다) | 전성민 |
| 3-5 | **게이지 홈 여백** 한 줄 확인 | 전성민 |
| 3-6 | `--hub-inventory-width` 목업 비율 | 전성민 |
| 3-7 | 4.5절 색 코드 8종 · 수확 가능 표식 · 습격 예고 세 종류 시각 구분 | 전성민 |
| 5A-1 | `DEC-UI-004` → `DEC-ART-001` 위임이 빈 곳을 가리킨다 | 전성민 |
| 5A-2 | D단계 화면 배경 네 장이 서로 눈에 띄게 달라야 한다 (`DEC-UI-023`) | 전성민 |
| — | **작물 속성 아이콘 4종** — 연결은 됐는데 속성을 표시하는 자리가 화면에 없다 | 전성민 |

### 4-4. 8/6 인계 P2 중 이미 끝나 있던 것

8/7 에 확인해보니 셋 중 둘은 저장소에 아예 없다. **인계 문서가 낡았다.**

| 8/6 # | 실제 |
|---|---|
| 4-4 `RunState.phase` 를 아무도 안 읽고 쓴다 | **이미 지워졌다.** `src/state/` 에 없다 |
| 4-5 `enterFieldPreview()` 가 테스트 픽스처로만 남았다 | **이미 지워졌다.** `tests/scene-manager.test.ts` 주석에 경위만 남는다 |
| 4-6 `rules.mjs` 주석이 없는 `open_questions` 를 가리킨다 | **주석은 8/7 에 고쳤다.** 관계 키 목록을 실제로 만들지는 여전히 기획 결정이다 |

---

## 5. 플레이 테스트에서 나온 것 — 미해결

세 사람이 각자 적어 둔 것을 한곳에 모은다.

| 출처 | 내용 | 상태 |
|---|---|---|
| 김민주 | 제작 정보가 화면 맨 하단에 떠서 거의 못 본다 | ✅ `e34c982` hover 로 해결 |
| 김민주 | **영순이 습격을 지원했다는데 실제로 지원을 했나** | **원인이 이것일 수 있다** — 지원 공격은 투사체가 없고(`DEC-CONTENT-008`) 명중 표시도 없어서 **화면에 아무 흔적이 안 남고 있었다.** 8/7 에 명중 표시를 붙였다(3-B-1). 그래도 안 보이면 그때는 지원 자체가 안 도는 것이다 |
| 김민주 | **지원 기회가 소비됐다는데 다음에 또 지원한다** | `startAllySupport()` 는 실제 런 상태의 `supportUsed` 를 바꾸므로 코드 경로는 정상으로 보인다. **재현 확인이 먼저다** |
| 김민주 | **튜토리얼 창에 가려서 구매 버튼이 안 보인다** | 미해결. 튜토리얼이 P0 였던 만큼 촬영에 그대로 나온다 |
| 김민주 | "던질 무기가 없다" 를 하단 대신 화면 상단이나 플레이어 주변에 | 미해결 |
| 김민주 | 야생동물한테 얻어맞는 느낌 | ✅ **8/7 해결** (`ed5bf2e`) — 맞는 쪽 깜빡임과 때리는 쪽 충격선 둘 다 |
| 김민주 | 미끈 토란 주머니 하나만 나오도록 · 씨앗 결정 속도 | **의미 확인 필요** |
| 최수정 | 다 정면만 있어서 캐릭터가 동동 떠다닌다 | 절반 해결 — bob 은 들어갔고(`aa77e8a`) 좌우는 그림 15장 대기 |
| 최수정 | 낫이 안 돌아간다 — 조준선만 돌아가서 부자연스럽다 | ✅ **8/7 해결** (`9cb4231`). 낫 그림은 원래 안 돈다 — 4-2 참조 |
| 최수정 | 튜토리얼 투척 단계에서 던지는 걸 못 보고 좌클릭 즉시 넘어간다 | ✅ **8/7 해결** (`24b99df`) — 0.9초 뒤 넘어간다 |
| 전성민 | 일시정지 시 조준선 멈추게 | ✅ **8/7 해결** (`ed5bf2e`) — 대화·정비에서도 같이 굳는다 |
| 전성민 | 정비에서 판매 상태일 때 구매 버튼을 누르면 그 창으로 전환 | 미해결. `maintenance-hub.ts` 라 김민주 자리 |
| 전성민 | 조준선을 길고 분명하게 | 8/7 에 ×2.5→×4.5, `#ff4d4d` 로 바꿨다. **더 늘릴지는 4-0 의 4.2 절 충돌과 같이 정한다** |
| 전성민 | 투척 없이 낫만으로 쉽게 클리어된다 | 4-0-1 참조. 승인 데이터라 사람만 |

---

### 5-B. 8/8 아침 플레이 테스트 — 아트 반영 뒤

**고쳐진 것** — 정비 창호지 두 짝이 동시에 뜨고 대화 초상화도 대사창과 같이
뜬다(`preload` 에 초상화·`UI_ASSET` 추가). 좌·우·공격 스프라이트 교체가 파일로
처음 돌았고 정상이다. 만복·영순 키도 같고 초상화가 대역이 아닌 것도 확인했다.
상점 수량 증감과 총액 반영도 정상이다.

| # | 무엇 | 담당 | 원인 |
|---|---|---|---|
| **B-1** | **퀵슬롯 팝업이 기능 버튼을 덮는다.** 퀵슬롯을 열면 판매·구매·제작 버튼과 닫기의 클릭 범위가 좁아져 *"제작 버튼의 위쪽에 맞춰야 눌린다"* | 김민주 | `.hub__popup-layer` 가 `top: --hub-pad` 부터라 헤더·버튼 영역까지 올라온다. 레이어는 `pointer-events: none` 이지만 `.hub__popup` 은 `auto` 라 **팝업 사각형이 겹친 만큼 버튼이 죽는다** |
| **B-2** | **팝업과 말풍선이 내용을 자른다.** 1일차부터 퀵슬롯 창에 스크롤이 아예 없어 무기를 넣을 수 없고, 만복 대화에서는 세 번째 선택지가 화면 밖이다 | 김민주 | 둘 다 고정 크기인데 `overflow` 가 없다. **같은 원인이 두 화면에 있다** |
| **B-3** | **튜토리얼에서 퀵슬롯 편성을 못 한 채 `자러 간다` 를 누르면 빈 초록 화면으로 넘어간다.** 튜토리얼 안내(5/7)와 `건너뛰고 시작` 만 남는다 | 김민주·최수정 | B-1·B-2 때문에 편성이 막힌 상태에서 정비를 끝내면 갈 곳이 없다. **B-1·B-2 를 고치면 사라질 수도 있고 아닐 수도 있어 그 뒤 다시 본다** |
| **B-4** | **튜토리얼에서 수확한 팝업이 1일차 시작하자마자 뜬다.** 고추는 소지품에 안 들어오는데 `고추 +2` 만 나온다 | 최수정 | **원인 확인됨** — `harvestPopups` 가 런 리셋에서 안 지워지고, 그것을 줄이는 `advanceFeedback()` 은 재배 단계에서만 돈다. 튜토리얼 재배에서 쌓인 팝업이 전투·정비 동안 얼어 있다가 **1일차 재배가 시작되는 순간 남은 시간부터 흐른다.** `readyFlashes` 도 같은 함수라 같은 문제를 갖는다 |
| **B-5** | 정비에서 판매·구매·제작 중 **무엇이 선택돼 있는지** 표시가 없다. 아래까지 스크롤해야 안다 | 김민주 | 선택 상태 스타일이 없다 |
| **B-6** | 타이틀 팻말 hover 에 회색 사각형이 남는다 | 김민주 | 기본 버튼 배경이 안 지워졌다 |

---

## 6. 일정

| 날짜 | 할 일 |
|---|---|
| **8/7 (금)** | 3절 A·D · 제출 문서 마무리 |
| **8/8 (토)** | **저녁 6시 팀 회의** · 시연 영상 촬영. **촬영 직전에는 `main` 을 건드리지 않는다** |
| 8/9~8/10 | 예비일, 제출 |

**회의가 촬영 당일 저녁이다.** 그 자리에서 4-1(`DEC-ART-001`)을 정하면
아트 재작업 → 구현 → 병합이 촬영 뒤로 밀린다. **4-1 은 회의 전에 비동기로
정하는 편이 안전하다.**

---

## 7. 제출 준비

`01`~`03` 초안은 `TODO` 없이 채워져 있다. 남은 것은 링크와 갱신이다.

| # | 할 일 | 담당 | 지금 상태 |
|---|---|---|---|
| 7-1 | `links.md` — 플레이 빌드 URL · 영상 URL · 확인 사항 넷 | 팀 | 3-E·촬영 대기 |
| 7-2 | `CREDITS.md` — 리소스를 추가하는 커밋에서 **같이** 기록 (팀규칙 6절) | 전성민 | 계속 갱신 |
| 7-3 | `ART_ITERATION_LOG.md` · `A1_ASSET_INDEX.md` — 새 아트가 오면 같이 | 전성민 | 갱신 필요 |

**「AI 활용 기술 문서」의 재료는 `docs/progress/sessions/` 에 있다.** 특히 AI 가
틀렸고 사람이 잡은 사례가 핵심인데 8/6~8/7 에만 세 건 더 나왔다 —
`content_assets` 42행을 넣고 파일 대조까지 통과했는데 렌더가 안 읽고 있었던 것 ·
아트를 붙이면서 도형이 지고 있던 정보(둔화 채움색)가 사라질 뻔한 것 ·
**UI 에셋 16장이 파일만 있고 고정 목록에 없어 코드가 못 쓴 것**(2절).
