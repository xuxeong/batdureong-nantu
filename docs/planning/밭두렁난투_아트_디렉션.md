# 밭두렁난투 아트 디렉션

갱신일: 2026-08-04

이 문서는 규격이 아니라 **톤**을 정한다. 크기·형식·에셋 ID·경로처럼 바뀌면 코드가 깨지는 것은
`DEC-ART-001`이 확정했고, 이 문서는 그 안에서 **무엇이 어떻게 보여야 하는가**를 정한다.

이 문서는 DEC가 아니다. 시안을 보며 계속 고친다.

이미지 생성 AI에게 지시할 때는 12절의 프롬프트 블록을 **그대로 복사해서** 쓴다.
기억으로 다시 쓰면 세션마다 톤이 어긋난다.

---

## 1. 한 줄 방향

> **선은 굵고, 색은 메마르게, 익은 것만 밝다.**

화면은 전체적으로 채도가 낮고 조용하다. 플레이어가 지금 손대야 할 것 —
수확할 수 있는 작물 — 만 색으로 튀어나온다.

## 2. 레퍼런스와 번역

기준 레퍼런스는 **Don't Starve**다. 그대로 복제하지 않는다.

| 가져올 것 | 버릴 것 |
|---|---|
| 굵고 검은 외곽선으로 오브젝트를 배경에서 떼어내는 방식 | 고딕·음산한 정서 → **한국 시골의 메마른 정서**로 |
| 손으로 그린 듯한 비대칭 형태 | 검정에 잠긴 저명도 → **명도를 올려** 형태가 읽히게 |
| 그라데이션 없는 평면적 명암 | 길고 앙상한 비율 → **뭉툭하고 단단한** 비율로 |
| 종이에 그린 듯한 인상 | 스크래치·해칭 질감 → **면 안쪽은 깨끗하게** |

마지막 행이 가장 중요하다. 원작의 "손그림 느낌"을 **선의 노이즈**로 오해하면 안 된다.
그 스타일의 선은 굵되 **깔끔하고 확신에 찬 한 획**이다.

## 3. 판독 우선 원칙

> **밭의 상태와, 다가오는 것의 정체가 먼저 읽혀야 한다.**

낮에는 *어느 밭이 수확 가능한가*, 밤에는 *다가오는 사람이 누구인가*.
디테일보다 이 두 가지 판독이 우선이다.

- 경작지는 **네 상태**(빈 밭 / 씨앗 / 성장 중 / 수확 가능)가 멀리서도 구분돼야 한다.
- 야생동물 3종과 주민 4명은 **색이 아니라 실루엣**으로 구분돼야 한다.
- 지원 주민과 적대 주민은 즉시 구분돼야 한다 (`DEC-UI-012`).

## 4. 색 규약

### 4.1 판정 색을 보호한다

- **채도 높은 따뜻한 색은 수확 가능한 작물에만 쓴다.**
  성장 중인 작물, 배경, 흙, 주민 옷, 야생동물, UI는 이 색대를 침범하지 않는다.
- 이것이 `DEC-UI-004`가 요구한 **수확 가능 표식**의 실체다.
- 바탕은 저채도의 흙, 마른 풀, 오래된 나무, 빛바랜 천 색이다.

### 4.2 적대 표시에 붉은색을 쓰지 않는다

승인된 작물 넷 중 **고추와 토마토가 붉은 계열**이다. 붉은색을 위험 신호로 쓰면
수확 가능 작물과 판정 색이 겹친다. 적대·위협은 **형태와 자세**로 표시한다.

### 4.3 지원 주민은 밝은 테두리로 구분한다

`DEC-UI-012`가 요구한 지원·적대 구분은 **밝은 외곽 테두리 유무**로 한다.
색상 대비가 아니라 테두리라서 4.1의 판정 색을 침범하지 않는다.

### 4.4 UI는 화면에서 가장 조용하다

패널·버튼·슬롯은 무채색에 가까운 나무·한지 색으로 한다.

**재질은 용도로 나눈다 — 읽는 것은 종이, 누르는 것은 나무.**

| 용도 | 재질 |
|---|---|
| 패널, 모달, 대사창처럼 **읽는 것** | 나무 테두리 + 안쪽은 깨끗한 한지 |
| 버튼, 슬롯처럼 **누르고 담는 것** | 테두리 없이 **나무 판자 통짜** |

버튼에 한지를 넣지 않는다. 글이 많이 들어가는 면만 종이다.

### 4.5 확정 대기 — 시안이 나온 뒤 채운다

시안에서 뽑아낸 실제 값을 여기 적는다. 그 뒤로는 이 표가 기준이다.

| 용도 | 색 코드 |
|---|---|
| 흙 (빈 경작지) | *미정* |
| 마른 풀 (배경 바닥) | *미정* |
| 성장 중 작물 | *미정* |
| **수확 가능 작물 (판정 색)** | *미정* |
| UI 나무판 | *미정* |
| UI 한지 (패널 안쪽) | *미정* |
| 윤곽선 검정 | *미정* |
| 지원 주민 테두리 | *미정* |

## 5. 선과 면

- 윤곽선은 **굵고 검고 깔끔한 한 획**으로 긋는다.
- 선을 여러 번 겹쳐 긋거나 스케치처럼 흐트러뜨리지 않는다. **잔선 금지.**
- 면 안쪽은 단색에 가깝게 채운다. **질감·해칭·노이즈를 넣지 않는다.**
- 명암은 그라데이션 없이 **2~3단계의 면**으로만 나눈다.
- 종이 질감은 **배경 이미지에만** 아주 약하게. 캐릭터·아이콘·UI에는 넣지 않는다.

## 6. 형태 — 장난감처럼

**실사체는 절대 쓰지 않는다.** 목표는 비닐 피규어나 나무 인형에 가까운 인상이다.

- 형태를 **단순한 덩어리**로 정리한다. 구, 원통, 타원의 조합.
- 머리·몸통·팔·다리·모자·도구가 각각 **분명히 나뉜 덩어리**로 읽힌다.
- 표면은 매끈하다. 주름, 근육, 솜털, 혈관 같은 실사 디테일을 넣지 않는다.
- 손가락을 따로 그리지 않는다. 벙어리장갑처럼 뭉뚱그린다.
- 눈은 단순한 점이나 타원. 눈동자 하이라이트를 여러 겹 넣지 않는다.
- 옷 주름은 두세 줄로 끝낸다.

## 7. 비율과 크기

승인된 `player_base_stats.csv`의 `collision_radius`가 **20**이고,
`DEC-ART-001`이 논리 월드 1단위 = 화면 1픽셀로 확정했다.
따라서 필드 캐릭터는 화면에서 **지름 40px 안팎**, 스프라이트 높이 **60~70px**이다.

| 용도 | 등신 |
|---|---|
| 컨셉아트 시트 (원본) | 4~5등신 |
| 필드 스프라이트 (SD) | 2~2.5등신 |
| 초상화 | 얼굴 위주. 컨셉아트의 얼굴 확대샷을 기준으로 |

필드에서 실사 비율로 그리면 이 크기에서 얼굴이 8px이 되어 주민 넷을 구분할 수 없다.
팔다리를 몸통에서 떼어 그려 **실루엣이 뭉치지 않게** 한다.

## 8. 도구 분담

| 도구 | 담당 |
|---|---|
| **NovelAI** | 사람 형태 — 플레이어, 주민 4명. 컨셉아트 시트 · 필드 스프라이트 · 초상화 |
| **GPT Image** | 그 외 전부 — UI, 경작지, 작물, 배경, 야생동물, 투척물, 아이콘, 컷신 |

같은 대상을 두 도구로 중복해서 뽑지 않는다.
야생동물과 작물을 GPT로 보내는 이유는 NovelAI가 인물에 특화돼 있어서다.

## 9. 제작 순서

**UI를 전부 끝낸 뒤 캐릭터로 넘어간다.**

| 단계 | 무엇 | 도구 | 장수 |
|---|---|---|---|
| **A** | **목업 4장** — 낮 필드 · 밤 필드(습격) · 정비 허브 · 대화 | GPT | 4 |
| **B** | 목업을 참조로 UI 부품 개별 생성 | GPT | 약 14 |
| **C** | 아이콘 — 작물 4 · 재료 3 · 투척무기 4 · 회복 3 · 속성 4 | GPT | 18 |
| **D** | 맵 배경 · 빈 경작지 · 씨앗 · 성장 중 4 · 수확 가능 4 | GPT | 11 |
| **E** | 야생동물 3 · 투척물 5 · 이펙트 2 | GPT | 10 |
| **F** | 캐릭터 — 플레이어와 주민 4명. 컨셉아트 시트 → 필드 스프라이트 · 초상화 | NovelAI | — |
| **G** | 엔딩 컷신 | GPT | 6 |

**A가 끝나야 B가 의미 있다.** 목업이 그대로 UI 명세가 되므로(10절) 배치를 여기서
확정하지 않으면 부품을 다 만든 뒤에 다시 배치를 고민하게 된다.
정비 허브와 대화 화면은 버튼·팝업·초상화가 몰려 있어 목업 없이 부품부터 만들면 안 된다.

**목업은 새 부품이나 새 배치가 나오는 화면만 뽑는다.** `DEC-UI-014`의 화면은 13개지만
나머지 아홉은 패널·버튼·배경의 조합이라 B단계 부품으로 조립된다.
투항 대화는 `DEC-UI-010`이 전투 전 대화와 같은 규칙으로 확정해 A4가 덮는다.

다만 `DEC-UI-023`이 조우 결과·밤 결과·런 실패·엔딩 네 화면을
*"공유 템플릿으로 묶지 않는다"* 로 확정했으므로, **D단계의 화면 배경 네 장이
서로 눈에 띄게 달라야 한다.** 목업이 없다고 같은 그림을 돌려 쓰면 안 된다.

**G는 F 뒤에 둔다.** 엔딩 컷신에 주민이 나오는데, 캐릭터를 확정하기 전에 그리면
같은 인물이 다른 사람으로 나온다.

A1 목업에 농부가 들어가지만 **크기와 위치를 잡기 위한 임시**다.
최종 캐릭터는 F에서 NovelAI로 따로 만든다. 목업의 농부로 캐릭터를 판단하지 않는다.

## 10. UI 파이프라인

**목업이 그대로 UI 명세가 된다.** 목업에서 레이아웃을 확정하면 구현 단계에서
배치를 다시 고민할 필요가 없다.

개별 요소는 목업에서 **잘라내지 않는다.** 잘라내면 해상도가 부족하고 가장자리가
지저분하다. **목업을 참조 이미지로 넣고 요소별로 다시 생성**한다.

패널 테두리는 9-slice로 늘어나야 하므로 *"빈 패널 한 장, 여백 넉넉하게"* 로
따로 뽑아 모서리를 잘라낸다 (`DEC-ART-001`).

### 뽑아야 할 UI 부품

확정 DEC에서 뽑은 목록이다. 부품 시트가 이걸 덮는지 확인한다.

| 부품 | 근거 |
|---|---|
| 패널 텍스처 + 9-slice 테두리 | `DEC-ART-001` |
| 버튼 기본·눌림·비활성 3장 | `DEC-ART-001` |
| **닫기 버튼** | `DEC-UI-020` — 팝업은 닫기 버튼으로만 닫는다 |
| 일시정지·설정 아이콘 | `DEC-UI-017` |
| 체력 게이지 틀 + 채우는 바 | `DEC-UI-017` — 플레이어용 |
| **적대 주민 체력 게이지** | `DEC-UI-019` — 이름과 현재·최대 수치가 함께 붙어 플레이어 것과 다르다 |
| **회복 사용 게이지** | `DEC-UI-017`·`DEC-INPUT-012`. 체력 게이지와 별개이며 누르는 동안 찬다 |
| **퀵슬롯 빈 칸 + 선택된 칸 강조** | `DEC-UI-017` — 투척 퀵슬롯 5칸 |
| 초상화 틀 | `DEC-UI-011` |
| 타이틀 로고 | `DEC-ART-001` |
| 정비 셔터 | `DEC-ART-001`·`DEC-UI-014` |
| 화면 배경 | `DEC-ART-001` |

굵게 표시한 셋은 `schema/enums.json`에 넣을 UI·시스템 에셋 ID 제안 목록에서
빠져 있던 것이다. 목록을 확정할 때 함께 넣는다.

목록에 없는 부품이 나중에 드러나도 괜찮다. `DEC-ART-001`이
*"와이어프레임 작업에서 필요한 항목이 더 드러나면 같은 절차로 목록에 추가한다"* 로
추가 경로를 열어뒀다.

## 11. 캐릭터 파이프라인

캐릭터 하나마다 **컨셉아트 시트를 먼저 만든다.** 정면·측면·후면 전신과 얼굴
확대샷, 표정 몇 가지가 한 장에 들어간 흔한 형식이다.

그 뒤 자세·표정·SD 변형이 필요할 때마다 **그 시트를 참조 이미지로 넣어** 뽑는다.
시트 없이 매번 새로 뽑으면 같은 주민이 세션마다 다른 사람이 된다.

```
컨셉아트 시트 1장  →  필드 SD 스프라이트  (2~2.5등신 변형)
                   →  초상화              (얼굴 확대샷 기준)
                   →  자세·표정 변형
```

### 기능 선택

| 하고 싶은 것 | 쓸 기능 |
|---|---|
| 같은 인물, 다른 포즈 | **Vibe Transfer** / Character Reference |
| 표정만 바꾸기 | Director Tools 감정 변경, 또는 인페인트 |
| 2등신 SD 변형 | Vibe Transfer + `chibi` 태그 |
| 구도는 그대로 두고 살짝 손보기 | img2img (강도 낮게) |
| 누끼 | Director Tools 배경 제거 |

**img2img가 아니라 Vibe Transfer가 기본이다.** img2img는 원본 구도를 붙들고 있어서
포즈를 크게 바꾸면 뭉개진다. Vibe Transfer는 참조 이미지에서 인물의 생김새와 화풍만
가져오고 포즈는 새 프롬프트가 정한다.

### 주의

- **시트를 통째로 참조에 넣지 않는다.** 정면·측면·후면이 한 장에 있어서 모델이
  여러 명이 있는 그림으로 읽고, 결과에 사람이 둘셋 나온다.
  **시트에서 정면 전신 한 컷만 잘라내** 참조로 쓴다.
- **4~5등신 → 2등신 변형은 한 번에 안 나온다.** 얼굴·색·옷은 따라오지만 비율은
  프롬프트가 결정하는 영역이라, 참조 강도를 올리면 등신이 안 줄고 내리면 인물이
  달라진다. 몇 번 돌려 중간값을 찾는다.

시트와 잘라낸 정면 컷은 `assets/source/`에 남긴다. 게임에는 안 들어가지만
**다음에 같은 캐릭터를 뽑을 때 반드시 필요하다.**

## 12. 프롬프트 블록

### 12.1 GPT용 고정 블록 — 모든 생성에 그대로 복사

```
[레퍼런스]
Don't Starve의 아트 스타일을 참고한다. 그대로 베끼지 않고 아래 조건으로 변형한다.
가져올 것: 굵고 검은 외곽선, 손그림 특유의 비대칭적 형태, 평면적인 명암,
          종이에 그린 듯한 인상.
버릴 것: 고딕·음산한 정서, 검정에 잠긴 어두움, 길고 앙상한 비율, 거친 스크래치 질감.

[세계관]
흉년이 든 한국 시골 마을. 플레이어는 밭을 지키는 농부다.
낮에는 작물을 키우고 야생동물을 막고, 밤에는 굶주린 마을 사람이 밭을 털러 온다.
담담하고 서늘한 정서. 귀엽고 산뜻한 쪽이 아니다.

[선과 면]
윤곽선은 굵고 검고 깔끔한 한 획으로 그린다. 확신에 찬 선.
선을 여러 번 겹쳐 긋거나 스케치처럼 흐트러뜨리지 않는다.
면 안쪽은 단순하고 깨끗하게 채운다. 질감, 해칭, 잔선, 노이즈를 넣지 않는다.
명암은 그라데이션 없이 두세 단계의 면으로만 나눈다.

[형태]
실사체를 쓰지 않는다. 비닐 인형이나 나무 장난감 같은 인상.
형태를 구, 원통, 타원 같은 단순한 덩어리로 정리한다.
표면은 매끈하다. 주름, 근육, 솜털 같은 실사 디테일을 넣지 않는다.

[색]
채도가 낮고 바랜 색. 흙, 마른 풀, 오래된 나무, 빛바랜 천의 색.
흐린 가을 낮의 빛. 검정에 잠기지 않아 형태가 또렷하게 읽힌다.
채도 높은 따뜻한 색(주황, 노랑, 잘 익은 붉은색)은 수확 가능한 작물에만 쓴다.
그 외의 것에는 쓰지 않는다.
네온, 글로우, 파스텔, 3D 렌더링 금지.
```

### 12.2 GPT — A단계 목업 4장

네 장 모두 고정 블록(12.1) 뒤에 이어 붙인다. **한 번에 한 장씩** 뽑는다.

목업은 배치를 확정하는 것이 목적이므로 **1920×1080 비율**로 뽑는다 (`DEC-ART-001`).

**`(위치)`는 비워둔 채로 GPT에 넣지 않는다.** 확정 DEC는 UI 요소의 목록과 서로 간의
상대 위치만 정했고 화면 어느 구석인지는 정하지 않았다. 빈칸으로 넣으면 GPT가 임의로
정하고, 목업이 그대로 UI 명세가 되므로(10절) 그 임의값이 그대로 굳는다.
손스케치로 채운 뒤 넣는다.

공통으로 덧붙일 것:

```
[출력]
1920x1080 비율의 게임 화면 한 장.
화면에 실제로 들어갈 UI를 배치까지 포함해 그린다.
글자를 넣지 않는다. 문구 자리는 빈 면으로 두거나 알아볼 수 없는 흐린 획으로 대신한다.
```

**A2와 A4는 A1이 확정된 뒤에 뽑는다.** 셋 다 같은 필드 위에 놓이므로 A1에서 정한
밭 크기와 HUD 자리를 그대로 물려받아야 한다. A1을 참조 이미지로 함께 넣는다.

**A1 — 낮 플레이 화면 (재배 모드)**

**필드는 화면을 꽉 채우지 않는다.** 승인된 `maps.csv`의 월드가 1600×900이고
`DEC-ART-001`이 월드 1단위 = 화면 1픽셀로 확정했으므로, 1920×1080 화면 정중앙에
1600×900 필드가 놓이고 **좌우 160px · 상하 90px의 띠가 남는다.**
`src/render/camera.ts`가 *"남는 영역은 여백이 되어 필드 HUD가 쓴다"* 로 적어둔 그 띠다.

필드 안의 좌표는 이미 승인 데이터가 정했다. 화면 좌표는 월드 좌표에 (+160, +90)이다.

| 대상 | 월드 | 화면 |
|---|---|---|
| 경작지 6칸 (3열×2행) | x 660·800·940 / y 480·600 | x 820·960·1100 / y 570·690 |
| 농부 재배 시작 위치 | (800, 420) | (960, 510) |
| 야생동물 출현점 8개 | 전부 가장자리 | — |

HUD 요소의 화면 위치는 아직 정해지지 않았다. `DEC-UI-017`은 요소 목록과
**서로 간의 상대 위치만** 확정했고 화면 어느 구석인지는 정하지 않았다.
아래 프롬프트의 `[HUD 배치]`를 손스케치로 채운 뒤 GPT에 넣는다.
채우지 않은 채로 넣으면 GPT가 임의로 정하고, 그것이 그대로 UI 명세가 된다.

```
[화면]
탑다운 시점의 밭. 흐린 가을 낮.
밭은 화면을 꽉 채우지 않는다. 화면 정중앙에 가로 1600 세로 900 비율의
직사각형 밭이 놓이고 좌우와 위아래에 밭 바깥의 띠가 남는다.
이 띠가 HUD 자리다. 밭과 띠의 경계가 눈에 보이게 한다.

밭 한가운데에 사각형 경작지가 정확히 여섯 칸 있다. 가로 세 칸, 세로 두 칸.
여섯 칸이 네 가지 상태로 나뉘어 보이게 한다 — 빈 흙, 씨앗, 자라는 중, 수확 가능.
수확 가능한 칸만 채도 높은 따뜻한 색이고 나머지는 전부 바랜 색이다.
농부가 경작지 바로 위쪽에 서 있다. 농부의 키는 경작지 한 칸의 높이와 비슷하다.
밭의 바깥 가장자리에서 야생동물 한 마리가 경작지 쪽으로 다가온다.

[밭 안에 함께 그리는 것]
농부와 가장 가까운 경작지 한 칸이 강조돼 있고, 그 칸 바로 옆에 행동 안내가 붙어 있다.
농부 바로 옆에 세로로 짧은 게이지가 절반쯤 차 있다.

[HUD 배치]
아래 요소를 밭 바깥 띠 안에만 배치한다. 밭 위로 넘어오지 않는다.

체력 게이지 바 — (위치)
  가로로 긴 바. 바의 오른쪽 끝에 붙여 수치 자리를 둔다.
현재 일차 — (위치)
남은 재배 시간 — (위치)
오늘 밤 습격 예고 — (위치)
투척 퀵슬롯 다섯 칸 — (위치)
  다섯 칸을 한 줄로 붙여 놓고 그중 한 칸을 선택된 상태로 강조한다.
선택된 회복 아이템 아이콘 한 칸 — 퀵슬롯 바로 옆
일시정지·설정 아이콘 — (위치)

[넣지 않는 것]
소지금, 미니맵, 화면 밖을 가리키는 화살표나 알림.
농부 주위의 상호작용 반경 원.
야생동물과 목표물을 잇는 선이나 화살표.
오늘의 목표나 과제를 적은 패널, 전체 일정표, 남은 일수.
선택지나 버튼에 붙는 숫자.

[재질]
HUD는 무채색에 가까운 나무와 한지로만 만든다. 밭보다 조용해야 한다.
```

**손스케치로 정할 것 여섯 개** — `(위치)` 자리에 넣을 값이다.

1. 체력 게이지 바
2. 현재 일차
3. 남은 재배 시간
4. 습격 예고
5. 투척 퀵슬롯 5칸
6. 일시정지·설정 아이콘

여섯 번째는 **개수도 정해야 한다.** `DEC-UI-017`이 "일시정지·설정 진입 아이콘"이라고만
써서 한 개인지 두 개인지 읽히지 않는다. 여기서 정한 개수가 `schema/enums.json`의
UI 에셋 ID 목록 개수와 맞아야 한다.

띠의 실제 크기는 좌우 160px, 위아래 90px다. 체력 게이지와 퀵슬롯 5칸을
좌우 띠에 세로로 넣기에는 160px가 좁다. 위아래 띠를 쓰는 쪽이 자연스럽다.

**A2 — 밤 필드 (습격 모드)**

A1과 **같은 밭, 같은 HUD 자리**를 쓴다. A1을 참조 이미지로 함께 넣는다.

`DEC-UI-017`이 재배 모드 전용으로 못박은 셋 — 남은 재배 시간, 습격 예고,
상호작용 안내 — 은 이 화면에 **없다.** 대신 `DEC-UI-019`의 적대 주민 정보가 붙는다.

```
[화면]
A1과 같은 구도의 밤 밭. 탑다운. 화면 정중앙에 가로 1600 세로 900 비율의 밭이 놓이고
좌우와 위아래에 HUD 자리인 띠가 남는다. 경작지 여섯 칸도 A1과 같은 자리에 있다.

밤이지만 검정에 잠기지 않는다. 형태가 또렷하게 읽혀야 한다.
농부가 밭 아래쪽에 서 있고, 밭 위쪽에서 마을 사람 하나가 걸어 내려온다.
이 사람은 적대적이지만 붉은색으로 표시하지 않는다. 자세와 실루엣으로 드러낸다.
밭 옆에 다른 마을 사람 하나가 서 있고 이쪽은 밝은 외곽 테두리를 둘러 구분한다.

[밭 안에 함께 그리는 것]
농부 바로 옆에 세로로 짧은 게이지가 절반쯤 차 있다.
날아가는 투척물 하나와 명중한 자리의 짧은 이펙트 하나.

[HUD 배치]
아래 요소를 밭 바깥 띠 안에만 배치한다. 밭 위로 넘어오지 않는다.

체력 게이지 바 — (위치. A1과 같은 자리)
현재 일차 — (위치. A1과 같은 자리)
투척 퀵슬롯 다섯 칸 — (위치. A1과 같은 자리)
선택된 회복 아이템 아이콘 한 칸 — 퀵슬롯 바로 옆
일시정지·설정 아이콘 — (위치. A1과 같은 자리)

적대 주민 체력 게이지 — (위치)
  플레이어 체력 게이지와 생김새가 다르다. 이름이 들어갈 자리가 붙고
  가로로 긴 바 하나에 현재와 최대 두 수치가 함께 들어간다.
  바 위에 어떤 기준선이나 눈금도 긋지 않는다.

[넣지 않는 것]
남은 시간, 타이머, 습격 예고, 상호작용 안내.
소지금, 미니맵, 화면 밖을 가리키는 화살표.
전투 진행도 막대, 남은 적 수, 웨이브 표시.
적대 주민 체력 바 위의 투항 기준선.
붉은색 경고 표시.

[재질]
HUD는 무채색에 가까운 나무와 한지로만 만든다. 밭보다 조용해야 한다.
```

**A2에서 새로 정할 것 하나** — 적대 주민 체력 게이지의 위치다.
나머지는 전부 A1에서 정한 자리를 그대로 쓴다.

**A3 — 정비 허브**

`DEC-UI-020`이 정한 것과 정하지 않은 것이 섞여 있다.
확정: 보관함은 상시 표시 영역이고 기능 버튼은 별도 영역이며, 팝업은 한 번에 하나만
항상 같은 자리에 열리고, **정비 종료 버튼은 허브 하단**이다.
미정: 보관함과 버튼 영역의 좌우, 팝업이 열리는 자리, 소지금과 습격 예고의 자리.

**팝업이 보관함과 소지금을 가리면 안 된다.** `DEC-UI-020`이
*"팝업이 열려 있는 동안에도 보관함과 현재 소지금을 계속 확인할 수 있게 한다"* 로
확정했다. 스케치할 때 이 제약이 팝업 자리를 크게 좁힌다.

```
[화면]
밭이 셔터로 완전히 덮여 있고 그 위에 정비 화면이 놓인다.
나무 셔터가 화면 전체를 덮는 배경이며 밭은 조금도 보이지 않는다.

[배치]
보관함 — (위치)
  상시 표시되는 큰 영역. 수확물·재료·무기·소모품 네 분류로 나뉘고
  각 칸에 아이템 그림 하나와 수량 자리가 들어간다.
기능 버튼 네 개 — (위치)
  판매, 구매, 제작, 퀵슬롯 편성. 네 개를 한 덩어리로 모아 놓는다.
팝업 하나 — (위치)
  오른쪽 위 모서리에 닫기 버튼이 붙어 있다.
  이 팝업이 보관함과 소지금을 가리지 않는다.
현재 소지금 — (위치)
오늘 밤 습격 예고 — (위치)
정비 종료 버튼 — 화면 하단. 가로로 긴 버튼 하나.

[넣지 않는 것]
팝업을 닫는 X 표시 외의 다른 닫기 수단, 바깥을 눌러 닫으라는 안내.
정비 종료를 다시 묻는 확인 창.
남은 일수, 전체 일정표, 오늘의 목표.
선택지나 버튼에 붙는 숫자.

[재질]
보관함과 팝업은 나무 테두리에 안쪽이 깨끗한 한지다.
기능 버튼과 종료 버튼, 보관함 칸은 테두리 없이 나무 판자 하나다.
```

**A4 — 대화**

전투 전 대화로 뽑는다. `DEC-UI-010`이 투항 대화를 *"전투 전 대화와 같은 표시·입력
규칙"* 으로 확정했으므로 투항 대화는 따로 뽑지 않는다.

확정된 것은 **순서**뿐이다 — 대사가 먼저 나오고 그다음 선택지 셋이 동시에 뜬다
(`DEC-UI-008`). 화면 어디인지는 정해지지 않았다.

```
[화면]
A1과 같은 구도의 밤 밭이 배경에 깔려 있고 그 위에 대화창이 겹쳐 있다.
배경의 밭은 멈춰 있음을 알 수 있게 흐리고 조용하다. 대화창이 화면의 주인공이다.

[배치]
대사창 — (위치)
  가로로 긴 면. 한쪽 끝에 정사각형 초상화 틀이 붙어 있고 그 안에 마을 사람의
  얼굴이 들어간다. 틀 옆은 대사가 들어갈 빈 면이다.
선택지 세 개 — (위치)
  셋을 나란히 놓는다. 생김새를 서로 다르게 하되 어느 것이 무슨 기능인지는
  드러나지 않게 한다. 셋 중 하나는 흐리게 처리해 고를 수 없는 상태로 그린다.

[넣지 않는 것]
선택지에 붙는 번호나 기능 이름표.
남은 시간, 타이머, 진행 막대.
이전으로 돌아가는 버튼, 대화를 닫는 버튼.
적대 주민의 체력이나 투항까지 남은 정도.

[재질]
대사창과 선택지는 나무 테두리에 안쪽이 한지다.
초상화 틀은 나무 판자다.
```

### 12.3 GPT — 필드 오브젝트 (작물·경작지·야생동물·투척물)

```
[시점]
위에서 내려다보는 탑다운. 단 지면만 위에서 보고 대상은 정면을 향한다.

[출력]
단일 오브젝트 하나만. 중앙 정렬. 배경은 단색.
그림자, 지면, 다른 오브젝트를 함께 그리지 않는다. 글자를 넣지 않는다.
```

### 12.4 GPT — 아이콘

```
[출력]
정사각형 안에 오브젝트 하나. 중앙 정렬. 배경은 단색.
작은 크기에서도 무엇인지 알아볼 수 있게 형태를 단순화한다. 글자를 넣지 않는다.
```

### 12.5 GPT — UI 부품

```
[출력]
UI 요소만. 캐릭터나 배경 일러스트를 넣지 않는다.
요소를 서로 겹치지 않게 넉넉한 여백을 두고 배치한다.
무채색에 가깝게. 화면에서 가장 조용해야 한다.
글자를 넣지 않는다. 문구는 나중에 코드가 얹는다.

[재질]
패널, 모달, 대사창처럼 글을 읽는 면은 나무 테두리를 두르고 안쪽을 깨끗한 한지로 한다.
버튼과 슬롯처럼 누르고 담는 것은 테두리 없이 나무 판자 하나로 만든다.
버튼 안쪽에 한지를 넣지 않는다.
```

### 12.6 NovelAI — 컨셉아트 시트

NovelAI는 문장이 아니라 **태그**로 지시한다. 아래를 기본으로 두고
캐릭터별 특징만 갈아 끼운다.

```
character reference sheet, multiple views, turnaround,
full body front view, full body side view, full body back view,
face close-up, expression sheet,
korean farmer, rural village, worn hanbok, straw hat,
thick bold black outline, clean confident lineart, flat color,
cel shading, two tone shading, limited palette, muted desaturated colors,
earth tones, faded fabric, toy figurine look, simple rounded shapes,
simple background, white background
```

**Undesired Content (네거티브)**

```
realistic, photorealistic, 3d render, semi-realistic,
sketch, rough lines, hatching, crosshatching, noisy texture, grain,
gradient, glossy, neon, glow, pastel, vibrant saturated colors,
detailed wrinkles, muscle definition, individual fingers,
text, watermark, signature
```

### 12.7 NovelAI — 필드 SD 스프라이트

시트에서 잘라낸 **정면 전신 한 컷**을 Vibe Transfer 참조로 넣고 아래를 더한다.
시트를 통째로 넣으면 사람이 여러 명 나온다 (11절).

```
chibi, super deformed, 2 head tall, big head, small body,
full body, standing, front view, simple background, white background
```

## 13. 후처리와 기록

- 생성 원본은 `assets/source/`, 크롭·누끼를 마친 결과물은 `assets/final/`에 둔다
  (`DEC-ART-001`).
- **누끼는 단색 배경으로 뽑고 지운다.** 이미지 생성 AI에게 처음부터 투명 배경을
  요구하면 가장자리가 지저분하다.
  캐릭터는 NovelAI의 Director Tools 배경 제거로 상당 부분 처리된다.
  GPT로 뽑는 UI·오브젝트는 사람이 직접 지운다.
- 리소스를 추가하는 커밋에서 `docs/submission/CREDITS.md`에 **도구 이름과 프롬프트를
  함께 기록**한다. 몰아서 하면 어느 프롬프트로 뽑았는지 찾지 못한다.
